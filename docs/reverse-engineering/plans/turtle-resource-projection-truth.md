# 玄龟独立资源投影真值方案

适用且唯一任务：`TASK-SETTINGS-225`。本方案补足223资源消费预检中的转换未知，不替代221行为合同、222A原生观察或222B碰撞真值。

## 范围冻结

以222A `expected-visual-states.json`及父222中的完整视觉manifest为独立期望：13符号、372本体、2,440效果、5,856动态、2,904buff状态。源只限恢复pet1/StageCommon及既有调用支架；保持原owner、24fps、方向/scale和全部递归child相位，不从候选输出反推期望集合。

## 源与采样

1. 先记录223的132非零状态、源hash和独立舞台基准，定位原`draw`矩阵/包络/像素相位。屏幕截图完整不等于对象完整，`getBounds`也不保证包含全部滤镜输出。
2. 复用现有源定义闭包与本机AIR。完整对象采样须包含声明signed/scale变换、透明padding与原点，并用扩大包络后的稳定性/边缘非截断检查证明没有遗漏。保持mask/filter、颜色变换和递归帧；不要重写Flash栅格器。
3. 动态支架只增加只读图层观察。保持原事件/源方法、body/bullet/buff时钟和owner顺序；与旧源trace逐字段一致。按真实可组合边界记录各对象像素、父子/depth、矩阵与alpha/filter等合成参数；整fixture截图仅为独立oracle。

## 归一化与双重验证

- 用原坐标原点及明确可逆裁切生成UI Schema manifest，逐项引用父222/222A state ID和源locator/hash；补充字段记录完整像素与组合语义，不复制造成另一套行为事实。
- 独立解码器消费采样图层和规范化字段，在940×590重建全部11,572状态，与未改动的222A原生基准逐像素比较；源显示列表与输出对象集合同时对账。
- 2026-09-17用户明确“允许差异”：只批准225最终独立组合的28状态308像素精确清单，见 `docs/tasks/evidence/TASK-SETTINGS-225/visual-exception-approval.json`。132个旧候选差异仍只是反证输入，不纳入许可；不外推碰撞20案例70像素批准。逐状态、坐标及双方RGBA精确匹配，任何新差异拒绝；双倍屏外内容仍用完整源图/边缘稳定性验收。
- source/state/owner/timing/scale/mask/filter/注册点/裁切/缺帧/缺图层变异必须失败；Schema、独立期望集和重复生成通过后才verified。

## 交接与停止条件

产物及命令写入225 evidence；现代转换代码与资源由223消费，225不写src/public。发现新资料族、另造栅格器或超出两个包时先拆分；未解关键项留draft并明确反证，不能以整fixture贴图结束资源合同。完成后只恢复223，不直接执行224。
