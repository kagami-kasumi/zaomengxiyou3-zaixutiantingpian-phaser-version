# TASK-SLICE-160 正式背包视觉验收

> 2026-08-04 复核说明：本文件自提交 `fbef10b` 起已被 Git 跟踪，历史“文件
> 缺失”是相对路径查找错误。其“原生 UI 通过”结论未覆盖 304 的角色/等级/
> 属性/装备动态 child、628 的 610/358 原生操作弹层，也未发现现代格距
> `43×41` 与原版 `61×60` 不符，现由
> [`TASK-SETTINGS-165B-backpack-review.md`](TASK-SETTINGS-165B-backpack-review.md)
> 取代。以下内容保留为历史执行记录，不再作为 165C 的视觉关闭依据。

## 基准与范围

- 原版基准：恢复包 `assets/backpack1.swf` 的 character 304、246、628，以及
  `EIcon1.swf`、`1_MainLoad__main1.swf`、`MagicWeapon2.swf` 的精确
  SymbolClass/character 导出。
- 现代入口：既有六槽存档进入正式功能 UI，再打开 P1 背包；验收视口
  940×590。
- 静态根页直接复用 `public/assets/ui/feature/backpack/backpack.svg`，不是现代
  重画背景；动态 child 由原按钮、格子和物品 PNG 组合。

## 显示列表与对象差异

| 对象 | 原版事实 | 现代状态 | 差异 |
| --- | --- | --- | --- |
| 根页 | character 304，舞台 940×590 | 同源 SVG，偏移 `(-753.95,-480.7)` 裁回舞台 | 原资源复用 |
| 四分类 | 230/235/240/245，`bpe` 起点 `(516.2,114.35)` | 同坐标、同 up/over/down 资源 | 等价动态重建 |
| 25 格 | character 628，5×5 | 同源 PNG、原列/行间距 | 等价动态重建 |
| 翻页/关闭 | 78/83/31 | 同源三态按钮和原命中区 | 原资源复用 |
| 物品 | 428 个可接入精确 symbol | 428 个稳定 key 按分类/页动态加载 | 原资源复用 |
| 数量 | 堆叠数大于 1 覆盖格子 | 同语义；实例及数量 1 不显示 | 等价动态重建 |
| 三项缺陷 | `fmtstx/scwpqhs5/wc` 原查找或资源缺陷 | 默认排除，不生成占位 | 已确认原版缺陷 |

未使用现代卡片、标题、可见按钮覆盖层或通用占位。物品专属使用效果仍按目录
显式保持待实现，不属于本切片。

## 逐状态证据

- [P1 装备首屏](task-slice-160/p1-equipment-page1.png)
- [P1 道具首屏](task-slice-160/p1-items-page1.png)
- [P1 道具第二页](task-slice-160/p1-items-page2.png)
- [P1 道具末页](task-slice-160/p1-items-page5.png)

内置浏览器确认分类选择、分页、关闭再入、既有 V6 存档读取，console
error/warning 为 0。静态区域直接使用同一基准 SVG，因此以同源资源相等和几何
边缘对齐代替重复栅格化像素差；动态区域逐对象核对 character、坐标、层级与
32×32 图标显示边界。

浏览器输入层无法合成小键盘 `/`，所以 P2 切换和满包拒绝没有伪造运行截图；
它们由 `inventory-resource-catalog-tests.ts` 的双 owner、124→125、满包失败
原子不变和 V6 往返门禁覆盖。现有六槽均为用户存档，未为截图改写槽位种子。

## 结论

正式背包原生化门禁通过：静态根页与按钮使用原资源，动态格、图标、数量和选择
状态按原显示列表重建；428 项可接入图标均有唯一 bundle owner，三项原版缺陷
保持排除，无未经批准的现代可见例外。
