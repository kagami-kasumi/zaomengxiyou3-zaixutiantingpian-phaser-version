# 标注批次目录

本目录保存按 `../batch-template.md` 建立的单资源族调查记录。批次记录说明过程和人工审阅点，CSV 标注事实放在 `../annotations/`。

批次可以在“等待符号定位”“等待选择性导出”或“等待现代接入”状态关闭；不需要在这些阶段长期保持进行中。`等待来源` 只用于恢复语料库中确实没有源文件的条目。当前状态见 `../project-status.md`。
