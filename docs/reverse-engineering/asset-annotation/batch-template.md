# 标注批次：<batch-id>

## 范围

- 资源族：
- 影响的现代切片/代码：
- 本轮包含：
- 本轮排除：

## 输入和证据

- 现代 stableKey 入口：
- AS3 / SymbolClass：
- EVB 源包 / 候选包：
- FFDec 定位命令与结果：
- 现有图片、shape 或报告：
- 人工证据：无 / `<evidence-id>`

## Agent 调查结论

- 已确认：
- 推测：
- 未知：
- 对应标注表：`../annotations/<scope>.csv`

## 人工动作

默认：无，Agent 先完成仓库内调查。

如确实需要人工，逐项写清：

1. 对象：
2. 原因：
3. 最小操作或问题：
4. 需要返回：
5. 没有该输入时可完成到：

## 去向

- 可直接接入：
- 待定位符号：
- 可选择性导出：
- 继续使用占位：
- 等待来源：
- 需要人工消歧：
- 进入拆分评估：

## 关闭检查

- [ ] 每条记录都有 `status`、`confidence` 和 `nextAction`。
- [ ] `export-ready` 已填写 `sourcePackage`，且不是仅凭文件名猜测。
- [ ] 没有把“尚未选择性导出”误写为 `missing-original`。
- [ ] `confirmed` / `probable` 均有证据路径。
- [ ] 未把猜测写成事实。
- [ ] 人工动作已压缩为具体问题。
- [ ] 如建议拆分，已通过拆分判定门。
- [ ] 已说明是否需要后续正式游戏 task。
