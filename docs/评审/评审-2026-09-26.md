# 区间评审：上次归档评审至今的全部开发

## 评审对象

- 范围：`c38a0aad`（上次归档评审 `评审-2026-09-05.md` 的下界提交）到当前工作树 `ae1edc5e`，共 24 个提交，工作树干净（`git status --porcelain` 空）。按主题归类为六块：青龙 1-4 阶（`b6d0dc6a`、`8207f180`、`daaed0ec`、`652feb1c`、`9342b03d`、`85caf2df`）、217 地面环境与关卡接线（`1782b0d8`）、215 受击反馈真值与伤害数字（`dc4b3010`、`7894d990`）、玄龟家族（`4c923e09`、`5bce67f9`、`161f600b`、`fb522091`、`2298a6af`、`66ad2cda`、`66d58122`、`dc9a5915`、`c6431e84`、`ed6eeefa`、`6c95c61d`、`1443223c`）、猴马重做（`a80900c7`）、击退证据与任务转换（`ae1edc5e`）、工作流优化（`03466663`）。
- 规模：`src/` 154 文件 +15,533/−1,592，`tools/` 528 文件 +33,584/−344，`src/` 新增 92 个文件、无删除。
- 基准：`docs/workflow/review-protocol.md`（含 UI 原生化门禁与证据时效条款）、`docs/workflow/reverse-engineering-protocol.md`、`docs/workflow/code-quality-gates.md`、`docs/architecture/src-boundaries.md`、`docs/tasks/feature-lines.md`、`docs/tasks/task-board.md`、`docs/tasks/task-definitions/TASK-SLICE-236.md`、`docs/workflow/problems/PG-017`、`PG-018` 及其效果检查条款。
- 证据时效：本次所有发现均在工作树重新搜索、读取并复算；引用前次评查处标注为遗留发现复核。

## 结论摘要

- 总体判断：本区间的实现以「先冻结原版证据、再由生产消费者消费」为主线，任务合同、证据分级、门禁命令和状态文档基本同步；共享宠物运行时（`PetCombatEntitySession`／`PetAnimationClock`／`PetNormalAttackDecision`）的重构方向清晰，未发现玩法性、数值性或数据丢失类缺陷。
- 是否阻塞继续扩展：**未发现阻塞问题**。发现的 6 项 Medium 都集中在「可复现门禁与可追溯性」而非行为正确性，其中与 `TASK-SLICE-236` 直接相关的是 M5（击退真值链未接自动门禁），建议在 236 开始消费该真值前补齐门禁，而不是先改玩法。
- 评分（1-10，可选维度，说明扣分原因）：行为一致性 8（猴马相位/资格/地面反例已按源重做，击退仍无位移属已登记缺口）；架构边界 7（共享 Session/Behavior 分层好，`HeroPartyRuntimeBridge` 等 5 个既有 warning 文件继续增重）；代码质量 8（共享时钟与会话有明确校验和注释）；测试覆盖 6（默认 `test:systems` 的 87 组不含任何玄龟测试，真值/行为 sidecar 无自动门禁）；功能完整度 7（玄龟 P1T=0、226 整改通过，230..235 公共责任仍 open）；文档同步 7（见 M1）；可持续性 7。

## 主要问题

### Medium：5 个在册 task definition 的「状态更新」仍指向已归档的 TASK-SETTINGS-230

`TASK-SETTINGS-231.md:55`、`TASK-SETTINGS-232.md:47`、`TASK-SETTINGS-235.md:48`、`TASK-SLICE-233.md:49`、`TASK-SLICE-234.md:48` 均写「状态更新：Planned，当前唯一Ready为TASK-SETTINGS-230」。230 已在 `ae1edc5e` 转成 `TASK-SLICE-236`（`TASK-SETTINGS-230.md` 删除、`TASK-SLICE-236.md` 新建），`task-board.md:7` 与 `npm run check:harness` 输出「current recommendations: TASK-SLICE-236」为权威。这些是**在册定义中的当前状态字段**（无日期、按现行状态读），不是历史叙述；同一提交已正确更新 `task-board`、`feature-lines`、`vertical-slices`、`task-history`、`mechanics-index` 与 226 承接矩阵（`docs/reverse-engineering/evidence/TASK-SLICE-226-contract-coverage.md:24,64,94` 已改为「230证据已交接、236实现」），只有这 5 个兄弟定义漏改。`check:workflow` 校验看板/历史/定义数量与链接，不校验定义正文的状态文本，因此门禁绿灯不代表一致。

建议：把 5 处改为 `TASK-SLICE-236`；若要防复发，在 `check-harness` 增加一条「在册 Planned 定义不得引用已归档 task id」的负向样例（成本低，与该工具既有断言风格一致）。

### Medium：玄龟全部测试未接入默认回归网，`check:all` 不执行任何玄龟路径

`tools/` 下有 14 个 `pet-turtle-*.ts`（含 `pet-turtle-family-acceptance-tests`、`pet-turtle-link-acceptance-tests`、`pet-turtle-resource-tests`、`pet-turtle-lifecycle-tests` 等），但 `tools/run-system-tests.mjs` 默认列表 87 组中 `pet-turtle` 命中数为 0，`package.json` 中 `turtle` 命中数为 0。它们只在显式 `npm run check:system-design -- pet <gate>`（`tools/check-system-design.mjs:139,162,181,202` 收集后由 `:766-773` 执行）或手工 `node tools/run-system-tests.mjs pet-turtle-…` 时运行。同时 `requireTest`（`tools/check-system-design.mjs:55-61`）只断言 `tools/<name>.ts` 文件存在，不校验该测试是否被任何命令执行，因此**静态门禁无法发现孤儿测试**。影响：本区间刚宣布闭合的家族没有默认回归保护，后续任何共享改动（如 236 要动共享运动/结算）不会自动覆盖玄龟。

建议：为玄龟提供聚合入口（例如 `test:pet-turtle` 或把低成本组加入默认列表、把需要本地语料的组成显式清单），并在 `run-system-tests.mjs` 的列表旁注明哪些组只能由设计门禁运行时执行；`requireTest` 增加「已被某入口执行」的断言等级。

### Medium：玄龟真值与验收输入不在版本控制内，最强家族门禁在干净副本上不可运行

`.gitignore:37` 忽略 `docs/tasks/evidence/*`；家族真值被显式列为「超出常规托管上限的本地复验输入」（本机实测 `task-settings-222-pet-turtle-family.json` 98MB、`task-settings-222a-pet-turtle-visual.json` 94MB、`task-settings-225-turtle-resource-projection.json` 27MB）。玄龟测试直接消费这些忽略输入：`tools/pet-turtle-resource-tests.ts:18` 读 `docs/tasks/evidence/TASK-SETTINGS-222A/body-native.json.gz`，`tools/pet-turtle-family-acceptance-tests.ts:21` 读 `TASK-SLICE-224C/combat-browser.json`，并且这些测试会 `mkdirSync`+`writeFileSync` 写回 `docs/tasks/evidence/`。`tools/turtle-runtime/README.md` 已注明「复验需本地语料」「原生验收需要本地语料和 docs/tasks/evidence 前置输入」，属**已声明的限制**；但命令行层面没有任何提示：干净副本上 `npm run check:all` 是绿的，而 `check:system-design -- pet P1T` 会因缺少本地输入失败，最容易被误读成「项目坏了」。

同源问题：`npm run check:ui-ground-truth`（无参数）在本机扫描 `manifests/` 顶层时会把本地存在的 222/222a/225（干净副本上不存在）一起纳入，因此该命令的结果依赖本机是否保留这些被忽略的大文件。

建议：在一处集中登记「需要本地语料/前置 evidence 的门禁清单」并在 `run-system-tests.mjs` 或 README 顶部给出命令；或让缺输入时输出可识别的「本地输入缺失，跳过并计为未覆盖」状态，而不是与真实失败同形。

### Medium：`check:ui-ground-truth` 当前为红，且失败输出不指明文件

实测（本机，工作树）：无参数运行 `npm run check:ui-ground-truth` **退出 1**，逐文件定位得到 7 个失败：`207-pet-monkey-family`、`209-pet-horse-family`、`213-pet-dragon-family`、`215-player-pet-incoming-damage-feedback`、`222-pet-turtle-family`、`228-pet-monkey-collision-phase`、`229-pet-horse-collision-phase`。报错形式为「missing required scope/generatedBy/provenance/states/displayObjects/baselines」与「unexpected property declaredContractIds/manifestContractIds/p1rContractIds/taskId」，即这些**行为/碰撞/物理真值被拿去套 UI schema**。validator 与 `ui-ground-truth.schema.json` 在本区间未改动，`207/209` 在区间内也未改动，说明红状态先于本区间；`213/215/222/228/229` 属区间内新增/更新的同类。根因是 `ground-truth/manifests/` 混放 UI 真值与行为/空间真值，而 `tools/validate-ui-ground-truth.mjs:9-11` 默认 `readdirSync` 全部顶层 `*.json`；同时该工具的失败分支不打印 manifest 路径（正常分支反而打印「<path>: schema valid」），产物定位困难。

建议：默认扫描改为按 manifest 声明的 schema/kind 选择校验器（或把行为/空间 manifest 移入已有 `manifests/behavior/` 之类的子目录并从 UI 扫描中排除），并让失败输出带文件名。此项与 M5 同源，建议合并为一个治理动作。

### Medium：230 击退真值链没有自动门禁，schema 对核心入口约束过弱（子代理独立核对后我在当树复验）

- `package.json` 无任何 knockback 脚本；`task-settings-230-monster-knockback.json` 位于 `manifests/behavior/`，默认 UI 扫描不进入该子目录，显式传入又会按 UI schema 报错；`monster-knockback.schema.json` 只由 `generate.py` 内部调用。因此 `generate.py --check`、`verify.py --mutations` 目前只能按合同手工运行。
- schema 约束过弱（我已复验）：`schema/monster-knockback.schema.json:193-198` 的 `entryAndScheduler` 只有 `minItems/maxItems: 23`、**无 `items` 结构**；`:199-205` 的 `naturalTweenOutcome` 只要求 `replace/edgeReturn/zero` 三键存在、不约束值类型。子代理探针确认把条目替换为任意对象仍判为合法；合同的核心结论（成功入口才写速度、去重链、世界顺序）因此缺少结构级校验。
- 可追溯性：432 个方向样本与 1,080 组原始运动只以 `counts={directions:432,motionGroups:1080,motionStates:27000,…}` 标量形式进入 tracked 真值，原始观测留在被忽略的 `docs/tasks/evidence/TASK-SETTINGS-230/native.json`；manifest 只有 270 组 `motion`，`counts` 是源观测规模而非 manifest 数组规模，消费方按 `counts` 校验长度会误判。
- 哈希与行号一致（复验通过）：37 条 `methods` 的 `fileSha256`/行号、2 条 `sources`、2 条 `spatialTruth`、`monsterProfiles` 的 sha256 与磁盘逐项一致；合同六段矩阵的分支计数与 `counts`、`verify.py` 断言吻合；未发现文档声明超出证据（`ae1edc5e` 未越级宣称击退已复现，与合同「230 只交接证据、236 负责实现」一致）。

建议：236 开工前补 `test:monster-knockback-truth`（串 `verify.py --mutations` 与 `generate.py --check`），收紧上述两处 schema，并在 manifest 内附原始观测的 locator+sha256（或把方向样本按等价性合并进 manifest）。

### Medium：聚合门禁的运行条件不稳定，226 收尾记录的是分段通过

`docs/tasks/evidence/TASK-SLICE-226/final-checks.json` 自记：`systemTests.status = "passed-by-prefix-and-remainder"`，首次全量 `test:systems` 因 `incoming-feedback-runtime-tests` 的旧 fixture 缺 `groundEnvironment` 失败，之后按「前缀 + 剩余段」重跑记 0；另记 `failedExpandedRun`（`.tmp/pet226-dead-ground-design.log`，exit 1，4GB 堆 OOM，需手工 `--max-old-space-size=8192`，声明「not acceptance」）。本次评审在当前树重跑 `npm run test:systems` **退出 0**，该 fixture 缺口已实际闭合（`incoming-feedback-runtime-tests` 已随全量运行通过）；但「分段重跑 + 大堆参数」意味着门禁存在假失败与漏记风险，且该记录未在其它状态文档中提示。

建议：在下一个收尾点补一次未经分段的全量聚合运行记录；为 OOM 场景在文档中固定 `NODE_OPTIONS=--max-old-space-size=8192`（或给测试命令加脚本包装），避免把资源不足当成通过或失败。

## 文件级发现

| 文件 | 观察与建议 |
| --- | --- |
| `docs/tasks/task-definitions/TASK-SETTINGS-231.md:55`、`232.md:47`、`235.md:48`、`TASK-SLICE-233.md:49`、`234.md:48` | 状态行仍指 230；改指 `TASK-SLICE-236`（M1） |
| `tools/run-system-tests.mjs:6-101`、`package.json` | 默认 87 组无玄龟测试；14 个 `pet-turtle-*` 只能由设计门禁或手工执行（M2） |
| `tools/check-system-design.mjs:55-61` | `requireTest` 只断言文件存在，无法发现孤儿测试（M2） |
| `.gitignore:37,46-52`、`tools/pet-turtle-resource-tests.ts:18`、`tools/pet-turtle-family-acceptance-tests.ts:21` | 玄龟门禁依赖被忽略的真值/evidence，且测试写回 evidence 目录（M3） |
| `tools/validate-ui-ground-truth.mjs:9-11`、`docs/reverse-engineering/ground-truth/schema/ui-ground-truth.schema.json` | 默认扫描全量顶层 manifest 导致 7 个非 UI 真值报错；失败分支不打印文件名（M4） |
| `docs/reverse-engineering/ground-truth/schema/monster-knockback.schema.json:193-205` | `entryAndScheduler`/`naturalTweenOutcome` 无结构约束（M5） |
| `docs/tasks/evidence/TASK-SLICE-226/final-checks.json` | 记录分段通过与 OOM 退出 1；本次复跑全量已 0（M6） |
| `src/scenes/HeroPartyRuntimeBridge.ts`（区间内 9 次提交，473→535 行，12 个 system import） | 既有 warning 文件继续承载多家族接线；区间内未见拆分或「局部修改理由」记录（L1） |
| `src/scenes/TestScene.ts`、`src/scenes/test-scene/TestSceneWorldBridge.ts`、`src/systems/MagicWeaponSystem.ts`、`tools/system-tests.ts` | 同类：warning 在下界已存在（`MagicWeaponSystem` 1185→1187 行、`TestScene` 975→980 行、`TestSceneWorldBridge` 1083→1088 行），区间内仍被加逻辑（L1） |
| `src/scenes/test-scene/TestSceneEncounterReset.ts:7` | `scene: any` + 字符串字段清单与 `TestScene` 字段无编译期关联，新增遭遇状态易漏重置（L2） |
| `src/systems/IncomingDamageFeedbackSystem.ts:68-69`、`src/scenes/IncomingDamageFeedbackView.ts:28` | `trace`/`processedIds`/`shown` 在 runtime 生命周期内无上界（随结算次数单调增长，仅随 runtime 销毁清空）（L3） |
| `src/assets/PetGroundEnvironmentAssets.ts:2-3`、`src/systems/PetDragonCollisionSystem.ts:1`、`src/systems/PetDragonEffectCollisionSystem.ts:1-4`、`src/assets/IncomingDamageFeedbackProjection.json` | 生产模块从 `docs/tasks/evidence/TASK-SETTINGS-21x/` import 已验证合同，与其它真值的 `ground-truth/manifests/` 位置不一致，靠 `.gitignore:46-52` 白名单保住；白名单一旦失效即删掉生产依赖（L4） |
| `src/systems/PetDragonCollisionSystem.ts:52`、`PetDragonAnimationClock.ts:12,20,28` | 采样器内 `68`（同文件 `:41` 已从契约读出 `maskProjection.size[0]=67`）与 `5/10` 为未命名常量；真值查找用 `!` 断言无显式错误（另有 `verify-dragon23-collision.py` 独立核对，故非当前缺陷）（L5） |
| `tools/monkey-horse-source/run.py:16` 等 10+ 脚本、`tools/monster-knockback-source/capture.py:17-18` | 硬编码 `D:/AIRsdkmanager/sdk/AIRSDK_51.3.4`；合同未写明依赖缺失时的行为（assert 强退）（L5） |

## 关键核对（确认一致，无冲突）

- 击退链路的一次疑似双重坐标换算经核为无害：`PetDragon23ProjectileSystem.emit` 先 `toDragonSourceCoordinate` 再在 `spawn` 重复调用，而该函数是 twip 截断量化 `Math.trunc(value*20)/20`（`PetDragonCollisionSystem.ts:7-10`），幂等，不产生 20× 偏差。
- PG-017 `2026-09-21` 记录的「`PetNormalAttackDecision` 决策后重置 1000ms，与 `BasePet` 连续 `timeCount` 取模不符」已在 `a80900c7` 删除：现类只剩 QLFJ counter（`PetNormalAttackDecision.ts:4-11`，注释「ordinary AI phase/chance belong to the ground Session」），旧不足已闭合。
- `PetAnimationClock`（139 行新增共享时钟）的 enter-before-decrement、条件路由回调、逻辑 keyframe 与 atlas 列分离均有校验与实现前注释支撑，未发现状态机缺陷。
- 任务唯一性：`feature-lines.md:56` 只有一条 `Active`（`LINE-PRE-STAGE-2-3-PRESENTATION`），其 Ready 为 `TASK-SLICE-236`，与 `task-board.md:7`、`check:harness` 输出一致，`WIP=1` 成立。
- `TASK-SLICE-236` 声明的输入全部存在（合同文档、230 manifest、`tools/monster-knockback-source/{capture,verify,generate}.py`、`tools/pet-monster-knockback-preflight.ts`、226 承接矩阵、`pet.md`），规模声明为主工作包 1／验收批次 1／预计 0 次 compact，符合规模门禁；输入资料已明确「正式运行不可依赖忽略的证据目录」。
- `226` 承接矩阵在 `ae1edc5e` 中同步为「230证据已交接、236实现」，未越级宣称击退已复现。
- 破坏性操作核对：`tools/monster-knockback-source/capture.py` 只写 `local-resources/regima/task-outputs/…`（忽略）与 `docs/tasks/evidence/…`（忽略），无删除、无外部任意路径；`generate.py --check` 只比较不写。

## 测试与验证

- 已覆盖（本次评审实跑，均在工作树）：`npm run check:workflow` exit 0（20/20 harness 测试、7 个活跃 PG 合同、唯一执行项 `TASK-SLICE-236`）；`npm run check:workflow:full` exit 0（+ 资源标注 1250 条 + 关卡架构）；`npm run check:structure` exit 0（8 条既有 warning，无 error）；`npm run audit:problems` exit 0；`npm run test:systems` exit 0（默认 87 组，含前次失败的 `incoming-feedback-runtime-tests`）；`npm run build` exit 0。
- 已覆盖（证据侧只读复算）：230 真值链的哈希/行号/计数逐项与磁盘对齐；`check:ui-ground-truth` 逐文件定位失败清单；`monster-knockback.schema.json` 结构约束探针。
- 缺口：本区间未运行 `npm run check:system-design -- pet <P1TA0/P1TA1/P1TA/P1TB/P1T>`（需要 `npm run build` + 4174 preview 浏览器与本地原生语料，且与前一条命令共享临时编译目录，不应并行）；未做 940×590 GPU/视觉对照；未重新执行 AS3 源级复验；未逐行评审四族全部行为实现（本次按共享运行时、结构、任务状态、真值链与门禁优先，行为面依赖设计门禁与既有专项测试）。以上缺口不影响「无阻塞」结论，但意味着**本报告不能替代一次完整 P1T 门禁运行**。
- 建议命令（按序）：`npm run check:all`；随后 `npm run build && npm run preview`（0.0.0.0:4174）再运行 `npm run check:system-design -- pet all`；真值侧 `python tools/monster-knockback-source/verify.py --mutations && python tools/monster-knockback-source/generate.py --check`（需本地语料）。

## 遗留发现复核（对照 评审-2026-09-05.md）

- 已解决（逐条在当前树复核）：
  1. warning 规则分歧——`AGENTS.md:13` 与 `CLAUDE.md`（结构检查条目）、`docs/workflow/agent-protocol.md:109` 现统一引用「AGENTS.md 必须遵守第 7 条」，无第二套拆分条件；
  2. 看板当前推荐夹杂旧 Ready——`task-board.md:7` 仅 `TASK-SLICE-236`，由 `generate:harness` 生成并经 `check:harness` 精确校验；
  3. 执行队列保留游戏 task id——`docs/tasks/execution-queue.md:17` 现为「当前为空」，文件不再持有游戏 task id；
  4. `check:workflow` 混入游戏专项——已拆出 `check:harness`，`check:workflow:full` 承接资源标注与关卡架构，`check:all` 用 full 组合，能力未丢失（本区间 `03466663` 进一步分层，PG-018 V2 已据此再归档，20 项 harness 正负例在本机通过）；
  5. compact 表述不统一——`AGENTS.md:80,112,115` 与 `agent-protocol.md` 收尾规则均明确「compact 次数不作为停止/拆分/新开对话条件」，并有「reject second compact forced handoff」等负向样例；
  6. 收尾需同步多个视图——当前推荐由状态表派生（`npm run generate:harness`），PG-018 关闭标准中的派生门禁已落地。
- 仍存在：无。
- 部分解决或证据不足：前次评审未覆盖游戏实现（明确限定 harness 层），其列出的代码侧观察（`src/` 领域别名 `PlayerSlot`）在本区间仍在（`check:workflow` 每次输出该 warning），属既有项、不在本区间改动范围内，未升级为本次发现。

## 后续任务建议

- 可直接修复（无需新 task）：
  1. 5 个 task definition 的状态行改指 `TASK-SLICE-236`；
  2. `validate-ui-ground-truth.mjs` 的失败分支打印被测文件路径；
  3. `run-system-tests.mjs` 列表旁注明「哪些组只由设计门禁/手工执行」，并在 `tools/turtle-runtime/README.md` 顶部集中列出需要本地语料的命令；
  4. manifest 内补充：`counts` 为源观测尺度的说明、`generatedBy` 的完整链（capture→verify→generate）、230 方向样本的 locator+sha256；
  5. 合同 `:49` 的 `MonsterRuntimeRegistryBridge.ts` 改为完整路径 `src/scenes/MonsterRuntimeRegistryBridge.ts`。
- 建议拆 task（治理/门禁，按优先级）：
  1. **击退门禁（建议在 236 开工前）**：新增 `test:monster-knockback-truth` 串接 `verify.py --mutations` 与 `generate.py --check`，收紧 `entryAndScheduler`/`naturalTweenOutcome` 的 schema 结构；
  2. **真值校验器分层**：UI schema 只校验 UI manifest，为行为/空间 manifest（含 `manifests/behavior/`）提供归属校验入口，消除 `check:ui-ground-truth` 的 7 项结构性红与「显式传入必错」的分裂状态——与 PG-017「行为合同 verifier」方向一致，建议挂在该 PG 的存量子项下而不是新建平行队列；
  3. **玄龟默认回归网**：为 14 个 `pet-turtle-*` 建立聚合入口并纳入常规检查（可对需要本地语料的组做显式标注与跳过语义），同时给 `requireTest` 增加「已被某入口执行」的断言。
- 与既有治理的关系：`PG-017` 仍在复盘中（230..235 公共责任 open，本报告不改变其状态）、`PG-018` 已归档（本次复核其关闭标准的派生门禁仍然成立，未发现复发信号）；`TASK-SLICE-236` 可用于承接本报告 M5 的门禁补齐，其余 Medium 不改变其禁止范围。

## 一句话总结

本区间把四族宠物与伤害反馈推向「源证据驱动」的一致方向且未发现行为性缺陷，真正的风险在验证结构：玄龟没有默认回归网、行为/空间真值没有归属校验器、击退真值链尚未接入任何自动门禁，建议在 236 消费该真值前先补这三处门禁。


## 整改复核与取舍

2026-09-26，按用户“判断值得做的并落实”要求，复核当前工作树后完成一批验证入口整改；未修改 `src/` 或执行 TASK-SLICE-236 的玩法实现。

| 原发现 | 当前判断与处理 | 边界 |
| --- | --- | --- |
| M1 旧 Ready 编号 | 值得顺手修，已修 5 处。定义不再复制当前编号，而是引用 task-board 状态表和派生推荐，避免下次切换再过期 | 不禁止文档合理引用历史 task；无额外状态门禁 |
| M2 玄龟默认回归缺口 | 优先做。`pet-turtle-lifecycle-tests` 加入默认系统回归，提供 `test:pet-turtle`；14 组测试按日常/本地条件登记，新增测试未分类会被检查拦住 | 其余 13 组依赖原证据/浏览器，不硬塞进干净仓库检查；完整 P1T 未重跑 |
| M3 本地条件不明确 | 值得做。新增 `tools/local-validation.md`，源输入缺失输出 `LOCAL_INPUT_MISSING`、非零退出并标明未覆盖；默认真值扫描仅 Git 在册，`--local` 明确扩展覆盖 | 保留本地语料边界，不把巨型原始数据全部提交到 Git；清单是主要前置，trace 引用的源文件/截图仍需存在 |
| M4 行为数据误套 UI Schema | 优先做。引入完整 Draft 2020-12 校验器，按声明/历史类型显式分派；228/229 校验交接结构和嵌套 UI，未知类型失败；接入 `check:code`，因此被 `check:all` 包含 | 结构通过不等于源行为、视觉或正式消费者通过 |
| M5 击退真值链无门禁 | 优先做。收紧 23 条入口/去重/调度字段和自然 Tween 类型，8 类坏字段被拒绝；增加 `test:monster-knockback-truth`，方向记录 locator/规范化哈希、生成链、counts 含义均由生成器产出 | 原 AIR 专项单独执行，普通检查只跑 tracked Schema；未宣称现代击退已实现 |
| M6 分段记录与 OOM | 不再按当前功能缺陷处理。本轮完整默认系统回归连续退出 0，含新增玄龟测试；文档固定完整本地门禁的临时 8GB 堆设置和恢复方式 | 未重跑完整 P1T；不抹掉 226 历史分段/OOM 记录，不把资源失败当通过 |

三条原报告描述需纠正：

- 整改前 `validate-ui-ground-truth.mjs` 已经在失败分支打印路径，因此“失败不指明文件”在当前树不成立。
- `requireTest` 会 `tests.add(testName)`，集合随后实际传入 runner；它不是“仅检查文件存在”。本次未重复改造设计门禁，只补默认回归及测试分层。
- `git ls-files` 证明 225 resource projection 当前在版本控制中；222 family 与 222a visual 才是本轮默认扫描排除的两个本地 manifest。不能把 `.gitignore` 注释直接当跟踪状态。

实际复验额外发现：AIR 的对象属性枚举顺序可变化，首次 `verify --mutations` 已通过而 `generate --check` 因 JSON 字段顺序失败，解析后内容完全相同。生成器统一排序序列化后，完整源专项重新通过；未放松数值比较、未手改原版观测。失败日志保留 `.tmp/review-knockback.log`，成功日志 `.tmp/review-knockback-retry.log`。

本轮验证：

- `check:structure`：0，8 项既有 warning，无本次目标文件 error。
- `check:ground-truth`：0，5 组工具测试（含 8 类击退坏字段）与 39 份 tracked manifest；显式 `--local`：0，41 份含本地数据。
- `test:monster-knockback-truth`：0，432 方向、1080 组/27000 态原运动、14 入口、4 去重、4 调度、3 自然 Tween 模式；五个实际源变异被拒绝，重新生成一致。
- `test:systems`：0，完整连续执行默认清单（原 87 组 + 玄龟生命周期 1 组）；玄龟实际输出 20 组五关/形态、双主人及生命周期通过。日志 `.tmp/review-systems.log`。
- `build`：0，日志 `.tmp/review-build.log`，保留既有大 chunk 提示。
- `check:workflow`：0，27 个在册 task/定义，唯一执行项仍为 TASK-SLICE-236。首次改文档引入 CRLF 导致标题严格检查失败，恢复原 LF 后通过，日志 `.tmp/review-workflow-retry.log`。
- `audit:problems`：0，004/017适用，集中记录于 `docs/workflow/problem-audit.md`；两者完整关闭合同未满足，不归档。017记录此轮有界方案与反证。

低优先级建议暂缓：大文件按下一次功能修改触发拆分，不为 warning 做大范围重构；`any` 重置清单、无上界诊断记录、生产数据目录迁移、魔法数字/旧 SDK 路径分别保留为局部后续候选。它们并非本轮已验证的玩家故障。此次也未添加泛化的“所有 Planned 文档不得引用历史 task”规则。

下一游戏执行项保持 `TASK-SLICE-236`。本次可作为一笔独立的验证工具整改提交；没有自动 commit/push。继续解释此评审可留在当前对话；开始 236 是新的实现范围，可另开对话。


### 用户后续要求：减少重复测试，只保留日常核心

这一步处理验证必要性与调度，不做内存分析或进程隔离。复核发现原默认88组混合基础行为、专项业务、资源目录/接线和家族细项；它们并非全都无用或互相替代，但不应每次局部修改全跑。

已将 `test:systems` 收敛为14组日常核心，清单在 `tools/system-test-suites.mjs`。保留大 `system-tests`，因为仍有独有基础战斗/成长/旧合同，没有证据允许整体删除；其他核心覆盖启动、save-schema/save-slot/双人保存、level-lifecycle、stage1-combat、incoming-settlement/environment、hero-party/monster runtime、pet-animation-session、turtle-lifecycle及formal-game-loop。其余74组仅移出默认执行，按相关源码/资源/真值变更触发，原断言不删。

`--core <专项名...>`合并且同名只跑一次；显式指定测试的设计门禁语义不变。`--full`仍是原88组，`check:all`使用完整代码检查而非悄悄变成核心；普通 `check:code`不再无条件扫描所有真值。修改原版证据/Schema时补对应真值检查，首次完整交付或相关变更仍履行原验收合同。

“只测一次”限定同批相关代码、测试、fixture、资源、配置、环境未变且已经通过。跨命令复用由执行者核对已有结果，不宣称工具已有自动依赖分析或永久成功缓存；不会为收尾再追加一个重复的全量命令。触发矩阵见 `docs/workflow/code-quality-gates.md`。

验证：9项工具测试与workflow通过，涵盖核心/完整清单、核心+专项去重、未知/冲突参数拒绝、真实CLI仅列计划、玄龟分层与真值坏字段；另有harness防全量暗降级负例。游戏src/public及既有游戏测试断言未变，复用本轮之前完整88组与build成功结果，不再重跑游戏全量。日志 `.tmp/review-test-reduction-tools.log`、`.tmp/review-test-reduction-workflow.log`。减少的是默认测试组数，不据此宣称实际耗时减少同等比例或全部风险都由核心覆盖。
