# LINE-STAGE-1-2 覆盖台账

本文是 `LINE-STAGE-1-2` 的权威覆盖入口。该线在 `LINE-STAGE-1-1` 完整关闭后按项目内容扩展路线获得唯一 `Active` WIP；当前先闭合恢复源包和 AS3 事实，不基于 Stage 1-1 外推未知内容。

## 当前范围

- 玩家可见目标：Stage 1-2 真场景资源、专属关卡流程和可玩的进入/完成闭环。
- 已知行为入口：`StageListener12`，以及场景中的 `fbEnter` 特殊入口。
- 视觉证据必须优先来自 `local-resources/regima/source/restored-swfs/`；旧提取集只作 AS3/历史对照。
- 复用边界：Stage 1-1 的正式入口、全灭失败、结果导航和 V3 解锁存档只能在证据相同处复用。

## 覆盖维度

| 维度 | 当前事实 | 状态 | 下一证据 |
| --- | --- | --- | --- |
| 场景布局 | `sl12`/墙体/门/出生点/刷怪标记尚未从恢复源包建立全集 | 待调查 | `TASK-SETTINGS-051` |
| 背景与地面 | `bg12` 与 Stage 1 公共地面的源包、character 和组合边界待确认 | 待调查 | `TASK-SETTINGS-051` |
| 普通关卡流程 | 已知由 `StageListener12` 驱动，刷怪/boss/门/通关细节待闭合 | 待调查 | `TASK-SETTINGS-051` |
| 特殊入口 | 已知 `fbEnter.colipse` 累计五次弹体命中后播放动画，玩家进入后调用 `MainGame.fbEnter()` | 部分确认 | 精查对象、门禁、目标与返回路径 |
| 正式流程与存档 | Stage 1-1 已有可复用候选；1-2 的解锁推进、失败和返回差异待确认 | 待调查 | `TASK-SETTINGS-051` |
| 运行时闭环 | 尚未接入现代实现 | 未覆盖 | 依据调查结果生成同线实现 task |

## 当前 task

`TASK-SETTINGS-051`：闭合 Stage 1-2 真资源、地图标记、普通关卡流程和 `fbEnter` 特殊入口证据，并生成一个同线最小实现切片。

## 关闭检查

- [ ] 真场景、背景、地面和专属交互资源可追溯且已接入。
- [ ] 地图标记、怪物/关卡推进和特殊入口全集有权威覆盖。
- [ ] Stage 1-2 进入、失败、普通完成和特殊入口/返回流程可玩。
- [ ] 自动检查和运行时验收通过。
- [ ] 无未完成的 `LINE-STAGE-1-2` task。
