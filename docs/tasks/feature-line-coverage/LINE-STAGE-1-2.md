# LINE-STAGE-1-2 覆盖台账

本文是 `LINE-STAGE-1-2` 的权威覆盖入口。该线在 `LINE-STAGE-1-1` 完整关闭后按项目内容扩展路线获得唯一 `Active` WIP；当前先闭合恢复源包和 AS3 事实，不基于 Stage 1-1 外推未知内容。

## 当前范围

- 玩家可见目标：Stage 1-2 真场景资源、专属关卡流程和可玩的进入/完成闭环。
- 已知行为入口：`StageListener12`，以及场景中的 `fbEnter` 特殊入口。
- 视觉证据必须优先来自 `local-resources/regima/source/restored-swfs/`；旧提取集只作 AS3/历史对照。
- 复用边界：Stage 1-1 的正式入口、全灭失败、结果导航和 V3 解锁存档只能在证据相同处复用。

## 覆盖维度

| 维度 | 当前事实 | 状态 | 下一证据 |
| --- | --- | --- | --- |
| 场景布局 | `sl12` character 53；前景 25；3 个 `ObsWall`、1 个 `FallDownWhenStandingWall`、5 停点、13 刷怪点、character 52 门与 character 22 特殊入口均已数据化 | 已确认/未接入 | `TASK-SLICE-125` |
| 背景与地面 | `bg12` = `assets/1.swf` character 135（4889.65×595.8）；复用 `floorBg1` character 1；根地面 → sl12 → bgContainer 动态背景层级已确认 | 已确认/未接入 | `TASK-SLICE-125` |
| 普通关卡流程 | 五停点依次 8/11/12/13/2 怪，总计 46；末批 Monster4+Monster2 双 boss，全灭显示门，按上普通胜利并解锁 1-3 | 已确认/未接入 | 场景接入后生成普通流程切片 |
| 特殊入口 | 五次有效弹体命中，每次有 1 秒防重复计数；播放至第 30 帧后驻留 72 帧，切到 Stage 5-1；不触发 1-2 胜利/解锁，也不专属返回 1-2 | 已确认/未接入 | 普通流程后生成特殊入口切片 |
| 正式流程与存档 | 可复用统一 1P/2P 全灭门禁、结果导航和 V3；普通门胜利解锁 1-3，特殊入口保持进度不变；失败重玩 1-2 或返回地图 | 已确认/未接入 | 后续同线流程切片 |
| 运行时闭环 | Stage 1-2 尚未接入现代实现 | 未覆盖 | `TASK-SLICE-125` |

## 当前 task

`TASK-SLICE-125`：选择性接入 Stage 1-2 真场景与显式地图布局，提供已解锁 1-2 的正式可达基础；不在同一 task 内实现波次、胜利或特殊入口状态机。

## 关闭检查

- [ ] 真场景、背景、地面和专属交互资源可追溯且已接入。
- [ ] 地图标记、怪物/关卡推进和特殊入口全集有权威覆盖。
- [ ] Stage 1-2 进入、失败、普通完成和特殊入口/返回流程可玩。
- [ ] 自动检查和运行时验收通过。
- [ ] 无未完成的 `LINE-STAGE-1-2` task。
