# LINE-STAGE-1-2 覆盖台账

本文是 `LINE-STAGE-1-2` 的权威覆盖入口。该线在 `LINE-STAGE-1-1` 完整关闭后按项目内容扩展路线获得唯一 `Active` WIP；当前先闭合恢复源包和 AS3 事实，不基于 Stage 1-1 外推未知内容。

## 当前范围

- 玩家可见目标：Stage 1-2 真场景资源、专属关卡流程和可玩的进入/完成闭环。
- 已知行为入口：`StageListener12`，以及场景中的 `fbEnter` 特殊入口。
- 视觉证据必须优先来自 `local-resources/regima/source/restored-swfs/`；旧提取集只作 AS3/历史对照。
- 复用边界：Stage 1-1 的正式入口、全灭失败、结果导航和 V3 解锁存档只能在证据相同处复用。

## 覆盖维度

| 维度 | 当前事实 | 状态 | 下一证据 |
| --- | --- | --- | --- |
| 场景布局 | character 25 前景、3 个 `ObsWall`、1 个 `FallDownWhenStandingWall`、5 停点、13 刷怪点、character 52 门与 character 22 特殊入口已由 `Stage12Layout.ts` 显式接入；角色脚底和怪物底边统一对齐水平墙顶 `y=501.0501981` | 已接入 | `TASK-SLICE-125` / `TASK-SLICE-128` 专项测试与运行时验收 |
| 背景与地面 | character 135 背景、复用的 `floorBg1` character 1 及根地面 → sl12 → bgContainer 组合边界已接入 | 已接入 | `TASK-SLICE-125` manifest/scene bridge |
| 普通关卡流程 | 五停点依次 8/11/12/13/2 怪，总计 46；镜头跟随后角色到屏幕右端 `x=920` 才命中当前停点并锁住推进；末批 Monster4+Monster2 双 boss，全灭显示门，按上普通胜利并解锁 1-3 | 已接入 | `TASK-SLICE-126` / `TASK-SLICE-128` 状态机、遍历系统、场景桥接与专项测试 |
| 特殊入口 | 五次可见弹体命中，每次有 1 秒防重复计数；真序列播放至第 30 帧后共享驻留 72 帧，一次性切到 Stage 5-1；不触发胜利/解锁，也不专属返回 1-2 | 已接入 | `TASK-SLICE-127` system/bridge、专项测试与 5-1 过渡边界 |
| 正式流程与存档 | 统一 1P/2P 全灭门禁、结果导航和 V3 已接入；普通门胜利幂等解锁 1-3，失败与特殊入口均不推进 | 已接入 | `TASK-SLICE-126` / `TASK-SLICE-127` |
| 运行时闭环 | 已解锁 1-2 可选 1P/2P，真场景、移动、波次/战斗状态、普通结果和特殊入口反馈均已接线；退出与跨场景清理幂等 | 已覆盖 | 专项/全量测试、生产构建和本地入口画面验收 |

## 当前状态

`TASK-SLICE-128` 已按用户运行时反馈修正 K 跳跃、地面基线和屏幕右端停点触发；`LINE-STAGE-1-2` 的真资源、布局、普通流程、失败/胜利存档和特殊入口覆盖仍保持闭合。

## 关闭检查

- [x] 真场景、背景、地面和专属交互资源可追溯且已接入。
- [x] 地图标记、怪物/关卡推进和特殊入口全集有权威覆盖。
- [x] Stage 1-2 进入、失败、普通完成和特殊入口/返回流程可玩。
- [x] 自动检查和运行时验收通过。
- [x] 无未完成的 `LINE-STAGE-1-2` task。
