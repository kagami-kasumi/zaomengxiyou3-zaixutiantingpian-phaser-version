# LINE-RELEASE-RUNTIME-LOAD 覆盖台账

## 当前范围

- 发布前降低首屏 JS 与目录载荷：非首屏场景按路由加载，Phaser/vendor 稳定拆包，运行时只消费机器真值目录的瘦投影。
- 保留完整机器真值、资源 provenance、离线可玩、场景 bundle 和错误可观察性；性能优化不能改变游戏行为或删除权威输入。
- 本线当前为 `Planned`，属于 `TASK_OUTLINE.md` 发布优化阶段，不抢占当前 Active 功能线。

## 覆盖矩阵

| 维度 | 当前状态 | 关闭要求 |
| --- | --- | --- |
| 生产基线 | 复评测得约 5MB 单入口，但尚无固化预算 | 可重复记录 raw/gzip、入口依赖、场景与目录贡献，并固定预算 |
| 场景代码 | 入口静态 import 全部场景 | 非首屏场景按路由动态加载，首入/重入/失败可恢复 |
| vendor | 未稳定拆分 | Phaser/vendor 独立缓存，chunk 边界由构建报告验证 |
| 目录载荷 | 全量真值目录进入运行时 | 完整目录保留为权威生成输入；运行时仅加载声明的瘦投影 |
| 生成治理 | 目录生成链缺少一致的 check/schema/hash | 瘦目录生成可重复，`--check`、schema/hash 与 npm 门禁能检出漂移 |
| 回归 | 图片 bundle 懒加载已有，JS 载荷无专项 | 启动、地图、功能页、代表关卡、离线、缓存和加载失败均有自动/运行验收 |

## 调度

1. `TASK-ARCH-177A`：Planned；复测生产基线，冻结依赖图、拆包/瘦目录合同与预算。
2. `TASK-ARCH-177B`：Planned；按 177A 实现场景拆包、vendor chunk、瘦目录生成与预算门禁。

## 明确排除

- 图片转码、纹理淘汰、Service Worker、CDN/发布平台迁移。
- 删除原版机器真值、资源 provenance 或离线运行能力。
- 以性能为由改变场景流程、玩法时序、视觉内容或错误语义。

## 关闭检查

- [ ] 当前生产入口体积、依赖与预算可重复测量。
- [ ] 非首屏场景与全量目录不再进入入口 chunk。
- [ ] Phaser/vendor、场景和瘦目录 chunk 边界稳定且可缓存。
- [ ] 生成器 check/schema/hash 与 bundle budget 门禁通过。
- [ ] 启动、地图、功能页、代表关卡首入/重入、离线与失败恢复无回归。
- [ ] 无未完成同线 task。
