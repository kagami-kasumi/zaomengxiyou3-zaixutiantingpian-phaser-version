# LINE-UI-NATIVE-SKILLS 技能页面 UI 原生化覆盖台账

## 用户确认范围

将已经接入真资源但仍覆盖现代外层 UI 的技能页面重做为原生化页面：原图片中的中文字、按钮、按钮状态和布局直接承担视觉与交互；透明命中区只处理事件，不绘制替代按钮。保留现有技能学习、升级、绑定、被动、P1/P2 owner、HUD 同步和 V4 存档规则。

本线服从 `PG-007 UI 原生化缺少统一门禁`，必须先闭合原生 UI 证据，再进入实现。

## 权威范围

- 技能总页：`OtherMat1.swf` character 250。
- 主动心法/技能页：`OtherMat1.swf` character 868。
- 五槽绑定页：`OtherMat1.swf` character 417。
- 被动技能页：`OtherMat1.swf` character 213。
- 原版入口与 owner：P1 `V`、P2 小键盘 `*`、地图 `showBuySkill`，以及既有共享 `FeatureUiHost`。
- 行为合同：现有技能树、学习/升级门禁、五槽顺序、被动等级、灵魂、HUD 刷新和 V4 保存。

## 覆盖矩阵

| 范围 | 当前事实 | 状态 | 关闭证据 |
| --- | --- | --- | --- |
| 技能总页 250 | 236 根页、240/244/248 原按钮、249 动态灵魂与角色选择器两帧已接入 | 已原生化 | `TASK-SLICE-143-visual-audit.md` |
| 主动页 868 | 865 十帧、50×3 技能状态、580/638、等级/成本与 `LV.n` child 已接入 | 已原生化 | 主动默认、学习成功与 P1/P2 截图 |
| 绑定页 417 | source、五槽 P1/P2 帧、拖放/点击等价、x_btn 提交与重载已接入 | 已原生化 | 绑定默认与重载截图、专项事务测试 |
| 被动页 213 | 五个 212 行、动态字段、207 按钮与现有 5 级业务上限已接入 | 已原生化 | 被动默认/成功截图、专项测试 |
| 动态信息 | 灵魂、等级、学习状态和五槽均进入原 TextField/addChild；永久摘要已删除 | 已完成 | 可见现代例外为空 |
| P1/P2 与正式流程 | owner、地图入口、关闭、HUD/V4 保存与重载通过 | 已完成 | `formal-skill-tests.ts`、940×590 证据 |
| 防复发 | PG-007 已形成首个完整技能存量迁移样本 | 已验证 | 原生资源门禁、静态拒绝现代覆盖、视觉审计 |

## 明确排除

- 不重写技能伤害、MP、冷却、学习成本、等级上限或五槽解析规则。
- 不扩展角色技能内容，不补造原版不存在的技能图标或按钮。
- 不顺带原生化宠物、法宝、背包或其他页面；它们由 PG-007 后续独立任务处理。
- 不修改或重新生成 `local-resources/regima/legacy-extraction/` 原始提取结果。
- 原版确实缺少现代可访问交互时，只允许最小现代等价，并必须标记、解释和单独验收。

## 调度

1. `TASK-SETTINGS-061` / `GOAL-011`：闭合四页原生文字、按钮、时间轴、命中区和动态槽位证据。
2. `TASK-SLICE-143` / `GOAL-012`：移除技能页面现代外层 UI，接回原生视觉与交互并完成正式流程验收。

本线已关闭；`GOAL-011` / `TASK-SETTINGS-061` 闭合证据，`GOAL-012` / `TASK-SLICE-143` 完成原生化实现与正式流程验收。后续功能线由用户重新调度。

## 关闭检查

- [x] 四页原生按钮、中文字、状态帧、命中区、注册点、矩阵和动态槽位证据闭合。
- [x] 全屏暗层、外层现代面板、现代标题和通用替代按钮全部移除或有逐项批准的现代例外。
- [x] 原图片中的字和按钮直接承担视觉；透明命中区不绘制替代内容。
- [x] 技能学习、升级、绑定、被动、P1/P2、HUD 和 V4 存档专项无回归。
- [x] 940×590 正式入口下四页 normal/hover/pressed/selected、拒绝/成功、关闭和重载通过。
- [x] console 无 error/warning，工作流、结构、系统、构建和 diff 检查通过。
- [x] PG-007 回写一个完整的技能页面原生化效果样本。
