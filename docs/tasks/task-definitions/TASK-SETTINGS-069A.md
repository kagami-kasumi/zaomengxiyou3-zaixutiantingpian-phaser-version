# TASK-SETTINGS-069A

任务类型：

- `TASK-SETTINGS`

功能条线：

- `LINE-PRE-STAGE-2-3-COMPLETION`（Active）

目标机制/切片：

- `M-018`、`M-023..M-025`、`M-034`、`M-035`、`M-047`、`M-049`、`VS-062`

规模预算：

- 主工作包：2
- 预计上下文压缩：0
- 独立验收批次：2

拆分触发：

- 若 `WuKong/Role1Effect/SpecialUI` 任一包内部还含两个不可共同验收的独立资源族，只拆当前角色子任务；不得读取 Role2..5 或进入实现。

输入资料：

- `hero-combat-visuals-index.md`、`Role1.as/Role1Shadow.as`、Role1 技能/弹体索引、三个候选恢复包、既有 Role1 标注与现代消费者。

输出产物：

- Role1 本体/装备、战斗 UI、普攻、全部已实现技能、影分身与附属对象的六段证据矩阵。
- 逐项复核现有 Role1 真资源，原位更新标注，并提供 `158A` 的 Symbol/帧序/几何/触发/生命周期输入。

完成定义：

- 行为完成与视觉完成分列；影响 `158A` 的未知为零，候选包不冒充精确 Symbol 事实。

UI 原生化合同：

- 显示列表清单：Role1 身体/衣装/武器/影分身、头顶状态、HUD 头像/条/五槽、按钮/命中区的 parent/depth/矩阵。
- 原版视觉基准：940×590 Role1 单人及一个合法双人组合的 idle/run/jump/attack/hit/death 和各技能关键帧。
- 允许的现代视觉例外：空清单。
- 逐状态验收：动作、起手/持续/命中/结束、方向、P1/P2 与 HUD 更新。
- 差异证据：原版/现代并排或叠图计划及对象/帧序/原点差异。

验收标准：

- `check:annotations`、`check:workflow`、`git diff --check` 通过。

禁止范围：

- 不实现动画，不调查其他角色或宠物，不用占位状态证明视觉完成。

状态更新：

- 更新 M-018/M-034/M-047、VS-062、本线覆盖、标注、看板与 PG 反馈；激活 `158A`。

推荐后续任务：

- `TASK-SLICE-158A`。

