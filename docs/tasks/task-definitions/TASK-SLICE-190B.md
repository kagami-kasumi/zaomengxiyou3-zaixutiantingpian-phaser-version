# TASK-SLICE-190B

任务类型：
- `TASK-SLICE`

功能条线：
- `LINE-PRE-STAGE-2-3-PRESENTATION`（Active；Split）

目标机制/切片：
- `M-036`、`M-037`、`M-052`、`VS-066`

规模预算：
- 主工作包：0
- 预计上下文压缩：0
- 独立验收批次：0

拆分触发：
- 189 消费者矩阵若列出两个以上独立页面皮肤/生命周期，必须每页拆一个同线 task；不把多页合并为现代通用可见层。

协作计划：
- 模式：单 agent
- 并行工作包：无
- 写入 owner：主 agent
- 归并检查点：实现前、验收前
- 方法观测：无

输入资料：
- 189 消费者/显示列表矩阵、verified manifest/基准、190A 数值投影接缝，以及矩阵精确列出的工坊/商城/其他页面消费者。

输出产物：
- 本父任务不直接实现。189 已证明强化、熔合、分解、打造拥有四套独立 verified 页面真值与事务生命周期，故拆为 `TASK-SLICE-190B1..190B4` 串行执行。

UI 原生化合同：
- 显示列表清单：逐页使用 189 冻结的根/子 Symbol、父子链、字段、深度和 hit area；页面皮肤不互换。
- 原版机器真值 JSON：每个消费页断言自身与 tooltip 的 verified truthId/状态集/完整性。
- 原版视觉基准：使用 189 按消费者冻结的版本、入口、状态和 940×590 基准。
- 允许的现代视觉例外：空清单。
- 逐状态验收：normal/hover/move-out、分页/分类、事务前后、P1/P2、返回/重开。
- 差异证据：每页单独并排/叠图/对象差异，不用 190A 截图代替。

组件化合同：
- 组件家族：189/190A 已试点的装备 tooltip hover/数据投影接缝，不共享页面皮肤。
- 权威 owner：`EquipmentCatalog` + 当前 equipment instance，嵌入式页不建第二数据/事务 owner。
- 共享行为：hover 生命周期、只读实例 view-model 与事务/分页更新触发。
- 页面保留项：每个 189 消费页的原 Symbol、字段、几何、深度、皮肤、命中区与返回生命周期。
- 消费者迁移矩阵：严格按 189 矩阵逐页迁移；若超过一个页面皮肤/生命周期就拆为独立 task。
- 防复发门禁：全消费者搜索私有属性拼装/第二坐标源/通用可见皮肤，并运行跨页数值同源和返回清理回归。

完成定义：
- `TASK-SLICE-190B1..190B4` 全部完成后归档本 Split 父任务；商城 49 商品无 `zblist` 装备，时装原版禁用 hover，只保留负回归，不生成第五个实现 task。

验收标准：
- 每个消费者专项、全系统、build、structure、annotations、workflow、diff check 与 940×590 正式路由零 console 通过。

禁止范围：
- 不执行 `LINE-SHARED-UI-COMPONENTS`；不修改装备事务/数值/存档，不扩大到非装备物品 tooltip。

状态更新：
- 更新本线覆盖台账、相关页面索引、`mechanics-index.md`、`vertical-slices.md`、task-board/history 与适用 PG 反馈。

推荐后续任务：
- `TASK-SLICE-190B1`
