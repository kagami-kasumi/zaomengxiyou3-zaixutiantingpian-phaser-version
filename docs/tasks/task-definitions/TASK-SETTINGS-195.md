# TASK-SETTINGS-195

任务类型：
- `TASK-SETTINGS`

功能条线：
- `LINE-PRE-STAGE-2-3-PRESENTATION`（Active；Planned）

目标机制/切片：
- `M-018..M-025`、`M-035`、`M-047`、`M-049`、`VS-068`

规模预算：
- 主工作包：2
- 预计上下文压缩：0
- 独立验收批次：2

拆分触发：
- 本 task 只建立同标准跨角色诊断和分区；一旦某角色需要新资源派生、桥实现或独立 940×590 校准，为该角色生成一个同线 0-compact 修复 task 并插入 196 前。

协作计划：
- 模式：单 agent
- 并行工作包：无
- 写入 owner：主 agent
- 归并检查点：不适用
- 方法观测：无

输入资料：
- `reverse-engineering-protocol.md`、`hero-combat-visuals-index.md`、五角色视觉/行为索引、069/158/163/164/173/174 证据与正式五关 Runtime。
- 五角色恢复源 SWF/SymbolClass/MovieClip 时间轴，现代 atlas/manifest/bundle、`HeroCombatVisualBridge` 及各 Role bridge/system。

输出产物：
- 五角色统一可测矩阵：本体/装备形态/普攻/技能对象的源帧数、帧率、持帧、循环/停帧、转移条件、注册点/矩阵、现代 clock/帧选择、bundle 就绪与运行捕获。
- 根因分类：解包缺帧、帧时序/持帧错误、转移抖动、clock/update 节拍、加载/解码卡顿、投影/几何跳变或原版就是低帧；每项带证据等级和反证。
- 只对受影响角色生成单角色修复 task；不强制五角色共用相同帧数/速率。

UI 原生化合同：
- 显示列表清单：复核每角色战斗 HUD/本体/装备形态/普攻/技能对象的根、depth、嵌套矩阵、动态 child 与生命周期。
- 原版机器真值 JSON：复核旧 manifests 对帧序/持帧/转移的覆盖；缺失的角色/动作必须由子 task 生成 verified 真值。
- 原版视觉基准：同一正式关卡、同动作脚本、同舞台 940×590 的五角色可对比录屏/帧捕获。
- 允许的现代视觉例外：空清单；低帧原版表现只能作为有证据的原版事实，不作现代例外。
- 逐状态验收：wait/run/jump/fall/普攻链/每已实现技能/受击/死亡、形态/装备切换、P1/P2、首次/重入。
- 差异证据：帧时间线对照、并排/叠图、注册点/边缘差异、帧间隔统计与可见对象差异。

完成定义：
- 用户观察到的角色间卡顿差异可稳定复现、定位且每个根因有一个有界处置；不得在无证据时归因为解包不全。
- 本 task 不修改 `src/`或视觉资源；子 task 生成后第一个受影响角色 task 成为后续，196 保持 Planned。

验收标准：
- 恢复 SWF 时间轴、现代帧目录/时钟与正式运行录制交叉；机器可复查帧数/时序对账、annotations、workflow、diff check 通过。

禁止范围：
- 不一次修改五角色，不重写行为/伤害/物理，不以动画补间或统一帧率自行补成“原版”。

状态更新：
- 更新五角色视觉索引、机制/切片状态、本线台账、task-board/history 并插入生成的角色子 task。

推荐后续任务：
- 执行本 task 生成的第一个受影响角色修复 task；全部完成后进入 `TASK-SLICE-196`。
