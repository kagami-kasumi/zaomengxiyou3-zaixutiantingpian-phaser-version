# TASK-SETTINGS-069D

任务类型：

- `TASK-SETTINGS`

功能条线：

- `LINE-PRE-STAGE-2-3-COMPLETION`（Active，当前为 Ready）

目标机制/切片：

- `M-021`、`M-023..M-025`、`M-034`、`M-035`、`M-047`、`M-049`、`VS-062`

规模预算：

- 主工作包：2
- 预计上下文压缩：0
- 独立验收批次：2

拆分触发：

- 若铲/弓资源不能在一个批次机械枚举，保留共享字段并只拆 Role4 双形态范围。

输入资料：

- 父索引、`Role4.as` 与附属对象 AS3、Role4 技能索引、`ShaShen/SpecialUI` 恢复包、标注与现代消费者。

输出产物：

- Role4 铲/弓本体/装备、战斗 UI、普攻、全部已实现技能与附属对象矩阵，以及 `158D` 输入。

完成定义：

- 双形态六段证据、资源身份和标注无影响实现的未知。

UI 原生化合同：

- 显示列表清单：铲/弓本体/武器/附属层、头顶状态、HUD/五槽的 parent/depth/矩阵/按钮/命中区。
- 原版视觉基准：940×590 双形态单人和合法双人组合的动作与技能关键帧。
- 允许的现代视觉例外：空清单。
- 逐状态验收：双形态动作、技能全生命周期、方向、P1/P2、HUD。
- 差异证据：并排/叠图计划与双形态对象/帧序/原点差异。

验收标准：

- `check:annotations`、`check:workflow`、`git diff --check` 通过。

禁止范围：

- 不实现动画，不合并铲/弓资源身份，不跨角色或宠物。

状态更新：

- 更新 M-021/M-034/M-047、VS-062、本线覆盖、标注、看板与 PG 反馈。

推荐后续任务：

- `TASK-SLICE-158D`。
