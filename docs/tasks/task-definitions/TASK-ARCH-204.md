# TASK-ARCH-204

任务类型：
- `TASK-ARCH`

任务模型：
- `常规任务`

逆向子类型：
- 不适用

逆向方案：
- 不适用

功能条线：
- `LINE-PRE-STAGE-2-3-PRESENTATION`（Active；Split）

目标机制/切片：
- `M-032`、`M-034`、`M-042`、`VS-012`、`VS-067`

关联具体系统设计：
- `docs/architecture/system-designs/pet.md`（当前有效；验收未退出）

本批设计验收 gate：
- 父任务不执行；207/208 先建立并通过首个完整参考家族 `P1R`，其余门禁只在经验证 Skill 逐族生成后继续

规模预算：
- 主工作包：0
- 预计上下文压缩：0
- 独立验收批次：0

拆分触发：
- 已再次触发：用户正式运行看不到自主攻击，证明 204B 的结构 gate 不能支持 204C..G 横向扩张；现改为 207 猴系完整逆向、208 猴系完整正式复现/Skill 沉淀，后续逐个家族按已验证 Skill 生成。

协作计划：
- 模式：单 agent
- 并行工作包：无
- 写入 owner：主 agent
- 归并检查点：各子 task 验收前
- 方法观测：无

输入资料：
- `docs/architecture/system-designs/pet.md`、203/204A/204B 实现、205 `pet-base-class.md`、206 设计裁决、用户运行反证及 193A/193B 猴系证据/实现。

输出产物：
- 一个从原版证据、现代实现到正式运行完整闭合的参考宠物家族；由该完整案例复盘出的经验证 Skill；随后逐族生成的剩余实现、迁移、清理和最终验收结果。

完成定义：
- 205、现代设计裁决及其保留/重排后的全部实现 task 均归档，`npm run check:system-design -- pet all` 按届时当前有效设计返回 0，宠物设计标记 `已完成/已退出`；在此之前父任务保持 Split。

验收标准：
- 以裁决后最终迁移 task 的 `pet all=0`、全系统/build、正式 P1/P2 旅程和旧路径清零为唯一技术完成证据。

禁止范围：
- 不恢复 204C..G/193E..R 的横向批处理，不在首个家族正式闭合前创建或强制使用宠物 Skill，不改宠物玩法数值、roster 或存档结构，也不预设改成深继承万能 `BasePet`。

状态更新：
- 205/206/204B 已完成但 204B 玩家可见结论被反证；当前执行 207/208。208 通过后只依据经验证 Skill 生成下一个单一家族任务；全部家族与消费者完成后才收束父任务。

推荐后续任务：
- `TASK-SETTINGS-207`。
