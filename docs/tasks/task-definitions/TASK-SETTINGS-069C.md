# TASK-SETTINGS-069C

任务类型：

- `TASK-SETTINGS`

功能条线：

- `LINE-PRE-STAGE-2-3-COMPLETION`（Active，当前为 Ready）

目标机制/切片：

- `M-020`、`M-023..M-025`、`M-034`、`M-035`、`M-047`、`M-049`、`VS-062`

规模预算：

- 主工作包：2
- 预计上下文压缩：0
- 独立验收批次：2

拆分触发：

- 若本体、盾/拉拽/追踪对象或 SpecialUI 不能共同验收，只拆 Role3 内部范围。

输入资料：

- 父索引、`Role3.as` 与附属对象 AS3、Role3 技能/弹体索引、`BaJie/SpecialUI` 恢复包、标注与现代消费者。

输出产物：

- Role3 本体/装备、战斗 UI、普攻、全部已实现技能与附属对象矩阵，以及 `158C` 输入。

完成定义：

- 六段证据和标注无影响实现的未知；资源包、Symbol 与行为关系可独立复核。

UI 原生化合同：

- 显示列表清单：本体/装备/状态对象、头顶状态、HUD/五槽的 parent/depth/矩阵/文字/按钮/命中区。
- 原版视觉基准：940×590 Role3 单人和合法双人组合的动作与技能关键帧。
- 允许的现代视觉例外：空清单。
- 逐状态验收：本体动作、盾/拉拽/追踪全生命周期、方向、P1/P2、HUD。
- 差异证据：并排/叠图计划与对象/帧序/原点差异。

验收标准：

- `check:annotations`、`check:workflow`、`git diff --check` 通过。

禁止范围：

- 不实现动画，不跨角色或宠物。

状态更新：

- 更新 M-020/M-034/M-047、VS-062、本线覆盖、标注、看板与 PG 反馈。

推荐后续任务：

- `TASK-SLICE-158C`。
