# TASK-ARCH-012 940×590 运行与性能证据

日期：2026-07-24

环境：同一 `npm run preview`（`127.0.0.1:4174`）、Codex 内置浏览器、940×590 viewport。

## 显示列表与视觉差异

- 冷刷新仍直接显示既有 `GameMenu` / `SaveInter` 壳层；没有新增、删除或替换启动菜单、存档槽、地图、功能 UI、HUD 或关卡可见对象。
- 加载等待只复用 Boot 已有进度文字和地图既有反馈文字；没有新增通用遮罩、弹窗或替代按钮。
- 地图、技能页、Stage 1-1 与 Stage 2-2 的原图、坐标、层级、裁切和交互区保持既有基准。

## 首屏资源与时间

- 治理前首屏 inventory：250 个资源，其中 248 张图片；包含 scaffold、战斗、功能 UI、地图和多个关卡资源族。
- 治理后首屏 inventory：5 个资源，其中 3 张图片，仅为构建 JS/CSS 与 `start-menu.svg`、`slot-panel.svg`、`confirm-dialog.svg`。
- 治理后同环境三次冷刷新：1243ms、788ms、646ms；中位数 788ms，低于 1.5 秒门槛。
- 首屏请求数量由 250 降为 5，下降 98%；治理前三次批量冷刷新在 60 秒观察窗口内未完成，治理后三次合计 2677ms，满足相对改善门槛。

## 首次进入、再进入与直达

- 首次进入天庭地图仅新增 6 张 `heaven-map` 图片；总资源数由 5 增至 11。
- 返回存档后再次进入地图，总资源数仍为 11，没有重复请求或重复注册。
- 首次进入技能页时地图保持可见并显示既有“正在载入技能页面…”反馈；加载后进入原生技能总页。
- 冷路径进入 Stage 1-1 时 inventory 为 85 个资源，包含目标 Stage 1-1 与公共战斗资源，不含功能 UI 或 Stage 2-2。
- `?qaStage=2-2-layout` 直达只加载 shell、公共战斗与 Stage 2-2；不含地图、功能 UI 或 Stage 1-1。
- 代表流程 console warning/error 为 0。

确定性失败、重试、并发 ensure、已加载幂等、场景销毁和无 owner/Boot 回填负向样例由 `tools/asset-bundle-tests.ts` 覆盖。
