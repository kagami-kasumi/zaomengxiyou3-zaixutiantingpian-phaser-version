# Goal 包看板

本文只保存未完成 Goal，是一次 `/goal` 的执行边界和交接状态权威入口。功能条线范围和状态以 `docs/tasks/feature-lines.md` 为准；功能条线 `LINE-*` 仍持有完整系统范围和单线 WIP；Goal 包 `GOAL-*` 把该条线切成可在一次 `/goal` 内完成并交接的有界执行单元；task 仍是最小验收单元。已完成 Goal 不在本看板留档，其完成事实和产物随绑定 task 归档到 `task-history.md`。

## 调度硬规则

- 存在未完成功能线时，必须且只能有一个 `Active` Goal，且必须属于唯一 `Active` 功能线。
- 一次 `/goal` 只承诺完成当前 `Active` Goal；Goal 完成后必须停止并交接，不在同一次 `/goal` 中隐式续跑下一 Goal。
- Goal 完成后直接从本看板移除；若功能线未关闭，同时激活或生成同线后续 Goal。不得保留 `Done` 行或“最近完成 Goal”摘要。
- Goal 默认只绑定一个 task。只有两个 task 共用同一产物、同一验证批次，且单独交接反而会丢失语义时，才可在 Goal 定义中说明理由后绑定最多两个 task。
- 新 Goal 默认预计 0 次上下文压缩。执行前必须做规模预检，核对 task 声明的主工作包、独立验收批次和拆分触发；超限时先拆分，不开始实质执行。
- 第一次 compact 即视为规模超限：只允许结束正在运行的检查、回写安全检查点并拆分剩余工作，不再读取新资料族、派生新资源或新增实现。
- Goal 不得同时跨越大量 AS3/资源逆向与完整玩法实现。需读取新资料族、切换明显不同子系统，或具有独立验收边界时，必须拆成下一 Goal。
- Goal 完成只表示一次交接包关闭，不代表功能线完成。功能线未关闭时，后续 Goal 仍须属于当前功能线。

## 状态定义

- `Active`：下一次 `/goal` 唯一可执行的 Goal 包。
- `Planned`：同线后续 Goal，或尚未激活功能线的候选 Goal。
- `Blocked`：Goal 本身有需要用户权限、材料或裁决才能解除的阻塞；不得因此执行其他功能线。

## Goal 总览

| Goal | 状态 | 功能条线 | 绑定 task | 交付边界 | 压缩预算 |
| --- | --- | --- | --- | --- | --- |
| GOAL-021 | Active | LINE-STAGE-2-2 | TASK-SLICE-150A | Stage 2-2 真场景、layout/traversal 与 9 个火焰机关接入 | 预计 0 次 |
| GOAL-022 | Planned | LINE-STAGE-2-2 | TASK-SLICE-150B | 五停点、25 刷怪点、54 怪与 1P/2P 普通流程 | 预计 0 次 |
| GOAL-023 | Planned | LINE-STAGE-2-2 | TASK-SLICE-150C | Monster16 八动作/六攻击、显门、胜利和 2-3 保存 | 预计 0 次 |
| GOAL-024 | Planned | LINE-STAGE-2-2 | TASK-SLICE-150D | 940×590 1P/2P 全流程校准、重载与功能线关闭裁决 | 预计 0 次 |

## Goal 生成检查

新建或拆分 Goal 时必须确认：

1. 有一个可独立描述、验证和交接的产物边界。
2. 默认只绑定一个 task，且 task 不同时要求新资料族逆向、大范围实现和端到端验收。
3. 规模预算声明主工作包 1..2、预计 compact 为 0、独立验收批次 1..2，并有可操作拆分触发；无法合理估计时先拆小。
4. 不得同时绑定新资料族/资源派生、多个 owner 和端到端运行校准；任意三类同时出现必须拆分。
5. 输出中包含当前状态、验证、剩余风险和后续交接，使新对话不依赖 compact 摘要；后续顺序由未完成 Goal 的状态表达，不另设“下一 Goal”字段。
