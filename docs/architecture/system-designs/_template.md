# <具体系统名称>设计

设计状态：当前有效。

验收状态：未开始。

验收退出：未退出。

## 目标与范围

- 目标系统：
- 要消除的重复/开发困难：
- 本批范围：
- 非目标：

## 现状定位

| 重复或困难 | 精确文件/符号 | 影响的消费者 | 本设计要如何收束 |
| --- | --- | --- | --- |

## 设计前完成度

| 能力 | 已有实现 | 完成判断 | 设计处置 |
| --- | --- | --- | --- |

## 选定设计

- 选定模式或组合：
- 选择理由：
- 要统一的公共流程：
- 差异注入方式：

## 模式角色与源码映射

| 模式角色 | 项目职责 | 目标文件/符号 | 允许依赖 | 禁止职责 | 实施状态 |
| --- | --- | --- | --- | --- | --- |

## 协作顺序

1. 正式入口：
2. 公共流程：
3. 差异调用：
4. 状态/结果输出：
5. 创建与销毁：

## 扩展规约

| 开发场景 | 允许新增/修改的位置 | 必须复用的入口 | 禁止做法 |
| --- | --- | --- | --- |
| 修改公共规则 | | | |
| 新增一种类型 | | | |
| 增加一个特例 | | | |

## 消费者与迁移批次

| 消费者 | 正式/测试 | 目标接入方式 | 旧路径 | 迁移批次 | 状态 |
| --- | --- | --- | --- | --- | --- |

## 禁止路径

| 禁止行为 | 禁止的文件/符号/模式 | 校验方式 | 允许例外 | 当前结果 |
| --- | --- | --- | --- | --- | --- |

## 硬性验证门禁

| Gate | 对应迁移批次 | 静态结构断言 | 必须执行的行为/正式测试 | 命令 | 当前退出码 |
| --- | --- | --- | --- | --- | --- |
| X1 | | | | `npm run check:system-design -- <system> X1` | 1（未实施） |
| all | 全部 | 执行全部 gate | 执行全部合同与正式回归 | `npm run check:system-design -- <system> all` | 1（未完成） |

规则：每批只有对应命令退出码为 `0` 才能记为通过；系统只有 `all` 为 `0` 才能 `已完成/已退出`。缺少类、方法、测试文件或正式接入都必须非零退出。

## 验收合同

| 规约 | 静态检查/测试/运行步骤 | 系统级完成预期 | 当前结果 |
| --- | --- | --- | --- |
| 角色存在与职责 | | | pending |
| 依赖与唯一入口 | | | pending |
| 禁止路径清零 | | | pending |
| 模式合同 | | | pending |
| 正式消费者接入 | | | pending |
| 迁移遗漏清零 | | | pending |

## 系统级剩余清单

- 未迁移消费者：
- 保留旧路径/兼容层：
- 失败测试：
- 未决偏差：

## 验收批次记录

| 日期/Task | 本批范围 | 静态检查 | 合同测试 | 正式运行 | 结论 | 系统剩余项 |
| --- | --- | --- | --- | --- | --- | --- |

## 验收退出记录

- 退出日期/Task：未退出。
- 最终证据：未完成。
- 退出后规则：普通开发不再读取本设计的验收机制，不再运行设计模式专项符合性检查；只有用户明确要求时才重开。
