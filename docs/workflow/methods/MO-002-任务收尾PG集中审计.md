# MO-002 任务收尾 PG 集中审计

状态：已归档。

裁决：采纳。归档日期：2026-08-09。

提出者/来源：用户提出用 MO 机制完成 PG 校验与归档，并将重复校验记录从单个 PG 文件抽离；2026-08-06 脚手架讨论。

负责人：每次适用工作的主 agent；协议级汇总由 workflow steward 负责。

最迟复核点：取得 3 个真实收尾样本后，且不得晚于 2026-09-15。

## 1. 方法与假设

方法：每次代码、架构、游戏 task 或工作流变更收尾时，通过 `problem-audit.md` 和 `npm run audit:problems` 统一生成活跃 PG 审计包；正常结果集中记录一次，允许同一份证据同时作为 MO 方法样本和 PG 关闭样本。满足全部 PG 关闭合同时，在同次收尾直接归档，不再人为延迟。

假设：集中执行与单点记录能够降低逐个 PG 重复扫描、重复日志和长期“观察中”滞留，同时不增加漏检、误归档或失去问题历史语境的风险。

本方法不把“MO 方法成功”改写为“PG 已关闭”。单个 PG 仍拥有问题特有的关闭标准、反证、方案版本和重开信号；集中审计只复用执行与证据。

## 2. 基线与指标

基线：任务收尾由执行者逐个打开活跃 PG，在多个问题文件重复写入相似的“扫描通过”行；PG-004 已积累大量跨任务重复记录，但仍长期处于运行观察状态。

主指标：

- 每个任务为 PG 反馈新增的日志行数与重复证据次数；
- 从当前 diff 到确定命中 PG 所需的读取/搜索次数；
- 审计后满足关闭标准却未归档的 PG 数量；
- 集中审计证据被 PG 关闭合同直接采用的数量。

护栏指标：

- 漏掉明确触发的活跃 PG 数必须为 0；
- 错误归档或遗漏反证数必须为 0；
- 归档 PG 的关闭标准、持久防线和重开信号缺失数必须为 0；
- 正常通过样本集中记录后，PG 的方案版本与历史反证仍必须可独立追溯。

## 3. 试验设计

- 取得至少 3 个真实样本，至少覆盖两类场景：游戏代码/架构变更、任务/工作流维护、逆向或 UI 验收。
- 每次样本先运行 `npm run audit:problems`，再由主 agent 语义判断实际命中项；工具输出不能代替语义判断。
- 每个样本只在 `problem-audit.md` 集中写一行；只有状态变化、反证、换案或归档时才同步更新单个 PG。
- 样本达到某个 PG 的最后一项关闭条件时，必须当场完成归档评估；满足全部条件则直接归档，否则记录精确缺口。
- 对照最近同类任务的逐 PG 反馈行数、读取次数和归档滞留情况；无法可靠计时时使用日志行数与重复证据数。

停止条件：出现一次错误归档；出现一次明确命中 PG 被集中审计漏掉且直到交接后才发现；连续两个样本的记录成本不降反升且没有提高归档及时性。

## 4. 采纳标准

- **采纳**：至少 3 个样本覆盖两类场景；重复反馈行数明显下降；至少一份集中证据被 PG 关闭合同采用或促成精确缺口判断；所有护栏指标为 0。
- **修订**：集中记录有收益，但候选生成、语义触发、归档评估或回写边界仍需调整。
- **停止**：命中停止条件，或 3 个样本后没有减少重复记录与归档滞留。

单个成功样本不能证明该方法应长期采纳；但如果同一份证据已经满足某个 PG 的全部剩余关闭条件，它可以直接促成该 PG 归档。

## 5. 观测记录

| 日期 | 任务/场景 | 方法版本 | 样本证据 | 指标结果 | 副作用 | 阶段结论/后续动作 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-09 | `TASK-SLICE-165D` 游戏代码、UI 逆向与任务归档 | V1 | 审计包列出当前 diff 与活跃合同；语义复核命中 PG-002/004/005/007/008/009/011，单行集中证据同时完成通过记录与逐项归档评估 | 集中日志 1 行、逐 PG 普通成功日志 0 行；漏检/误归档/反证丢失均 0；同一证据促成 5 个满足合同的 PG 归档，并为 004/011 给出精确未满足项 | 候选包仍较长，但没有造成误判；归档动作增加一次性文档写入，属于协议要求的及时出清 | 第三个真实样本覆盖游戏代码/UI/逆向，满足采纳标准；稳定方法已在 `problem-audit.md`、`problem-governance.md` 和收尾入口沉淀，同次归档 MO-002 |
| 2026-08-08 | 原版机器真值 JSON 工作流维护 | V1 | 集中审计语义命中 PG-004/005/007；正常结果只写 `problem-audit.md` 一行，并精确判断本次不是 PG-005/007 所需真实迁移样本 | 集中日志 1 行、逐 PG 普通成功日志 0 行；漏检/误归档/历史反证丢失均 0；关闭滞留误判 0 | 候选包仍输出全部活跃合同 | 第二个真实样本通过，覆盖逆向/UI 工作流维护；继续等待游戏代码样本和直接归档效果 |
| 2026-08-06 | PG 集中审计与 MO-002 脚手架维护 | V1 | `npm run audit:problems` 读取当前 diff、11 个活跃 PG 合同并生成审计包；语义复核只命中 PG-004，结果集中记录于 `problem-audit.md`；`npm run check:workflow` 验证入口、合同和负向门禁 | 集中日志 1 行；逐 PG 普通成功日志新增 0 行；漏检 0、错误归档 0、关闭合同/反证丢失 0；PG-004 精确得到“1/3 样本、暂不可归档”结论 | 候选包仍列出全部 11 个活跃合同，输出量未完全收敛；尚无跨游戏代码/任务场景证据 | 首个脚手架样本通过，证明双用途证据和集中记录可行但不足以采纳；后续至少取得 2 个、覆盖其他场景的真实样本，并观察是否需要事件级窄路由 |

## 6. 裁决与沉淀

裁决：采纳并归档。三个真实样本覆盖脚手架维护、逆向/UI 工作流和游戏代码/UI 迁移；每个样本只新增一行集中日志，没有向普通 PG 复制成功行，漏检、误归档、反证丢失均为 0。第三个样本让同一证据及时归档五个满足合同的 PG，同时保留 PG-004/011 的精确缺口，证明方法既减少重复记录，也改善归档及时性。

稳定规则已沉淀在 `problem-audit.md`、`problem-governance.md`、Agent 收尾入口和 `npm run audit:problems`：先生成候选包，再做语义匹配；正常结果集中写一行；状态变化/反证才回写独立 PG；满足全部关闭条件时同次归档。若集中审计漏掉明确触发项、错误归档，或连续两个真实样本记录成本不降反升且无质量收益，按 MO-002 定向重开。
