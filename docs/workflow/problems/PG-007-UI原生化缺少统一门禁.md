# PG-007 UI 原生化缺少统一门禁

状态：规则已落地，运行观察中。

来源：2026-07-23 用户在炼丹炉原生按钮整改后进一步指出：技能系统及后续系统不得把原版页面当背景后再覆盖一套现代 UI；已实现页面需要迁移，未实现页面需要提前规避。

## 1. 问题定义

项目已经能够从恢复 SWF 中取得原版页面、中文字、按钮、按钮帧和局部子页，但“接入真资源”尚未等价为“UI 原生化”。多个正式功能页仍把真资源作为背景，再使用 Phaser 矩形、现代标题、通用按钮和暗色面板重建一层视觉与交互。

本项目中的 **UI 原生化** 定义为：

- 原始 SWF 中的页面图片、中文字、按钮显示对象、按钮帧、原始布局和已有文本/图标槽位是视觉权威。
- 原图片中的中文字直接承担按钮视觉；允许叠加透明命中区处理事件，但不得重新绘制替代文字、边框、底色或选中标签。
- 原版按钮存在 up/over/down/selected 等状态时，现代实现复用对应原生状态，不用通用矩形按钮模拟。
- 动态数值、物品图标和状态进入原版预留槽位；不能因为现代数据更方便展示而新增覆盖原图的外层 UI。
- 原版确实没有对应表现时，现代补充必须单独标记为“现代设计选择”，记录证据缺口、最小视觉边界并经过用户验收。

该问题同时影响两类工作：

1. **存量迁移**：技能、宠物、法宝等已经形成现代覆盖层的页面，需要按系统逐个逆向和整改。
2. **增量防复发**：后续 UI task 若没有前置门禁，会继续把“加载了真背景”误写成“真 UI 已完成”。

不治理的后果是：每个系统先造一套临时现代 UI，用户验收后再返工；原版坐标、按钮状态和交互语义被覆盖；任务与功能线可能在视觉合同未满足时提前关闭。

## 2. 证据

- `src/scenes/feature-ui/FormalSkillPageView.ts` 当前包含 3 个 `scene.add.rectangle`、5 个 `scene.add.text` 和 9 组通用 `button(scene, ...)` 调用；其中包括 940×590 暗层、900×548 外层面板和现代标题。
- `src/scenes/feature-ui/FormalPetPageView.ts` 当前包含 3 个矩形、7 处现代文字和 11 组通用按钮。
- `src/scenes/feature-ui/FormalMagicWeaponPageView.ts` 当前包含 6 个矩形、11 处现代文字和 6 组通用按钮。
- `src/scenes/feature-ui/FormalWorkshopPageView.ts` 经 `TASK-SLICE-142` 后虽为 0 个矩形和 0 组通用按钮，但仍以扁平 119 容器叠加统一中心子页、现代 P1/P2 文字、线性背包文本和现代状态文案；原 AS3 实际动态创建 `BackPackElement`、25 个 `PackThings` 和角色选择 MovieClip。该反例证明静态计数、透明命中和“真背景已加载”仍不足以证明 UI 原生化。
- `StrengthEquipment.as` 已给出四子页、背包和角色入口的运行时 child/坐标，`BackPackElement.as` 已给出 5×5 动态格子，但此前实现/验收没有把它们组织成完整显示列表清单，也没有用原版运行画面做同尺寸叠图或像素差异。
- `docs/reverse-engineering/full-function-ui-index.md` 已把技能总页及三个子页记录为真资源并标记正式实现完成，但现有 view 仍用外层现代面板、标题和通用按钮承载主要交互，说明“真资源接入”和“UI 原生化完成”之间缺少独立验收门禁。
- 用户明确要求将该问题统一命名为“UI 原生化”，并要求同时解决已造 UI 的迁移与后续系统的预防。

以上计数只用于定位高风险页面，不把 `rectangle` 或 `text` 的存在自动判为违规。最终判断必须结合恢复 SWF、原版槽位、按钮时间轴和 940×590 运行画面。

## 3. 解决方案

### 3.1 建立 UI 原生化合同

在任务生成、逆向、实现和评审入口统一要求 UI task 记录：

- 权威源 SWF、SymbolClass、MovieClip、character id 和目标帧。
- 根页面与子页的注册点、嵌套矩阵、可见边界、940×590 坐标映射。
- 原生中文字、按钮实例、up/over/down/selected 状态和命中区。
- 动态文本、图标、列表、分页、确认/返回等原版槽位。
- 允许新增的现代视觉元素清单；未列出的现代标题、面板、标签、按钮和暗罩默认禁止。
- 原生化的自动验证与逐状态浏览器验收计划。
- 完整显示列表清单：root/child/depth/父子关系、注册点、嵌套矩阵、mask/filter/blend、TextField、按钮状态、动态 `addChild` 和命中区。
- 原版视觉基准：可追溯 Flash 截图/录屏或原 SWF 渲染的版本、入口、状态、舞台尺寸与裁切；现代截图不得反向充当原版基准。
- 同尺寸并排/半透明叠图、稳定区域像素或边缘差异、逐对象差异清单和字体栅格化容差说明。

### 3.2 存量页面迁移

先建立正式功能页存量矩阵，对每页分类：

- 已原生化。
- 真资源已接入但仍有替代覆盖层。
- 仅行为原型。
- 原版资源或交互证据未闭合。

技能系统作为炼丹炉后的第一个整改样本，仍拆为独立证据 task 和实现 task。宠物、法宝、背包等按各自系统生成后续小任务，不一次性重构全部 UI，不在 PG 文档中直接修改游戏代码。

### 3.3 增量防复发

- `task-generation.md`：UI task 必须包含“UI 原生化合同”和允许的现代例外。
- `reverse-engineering-protocol.md`：涉及 UI 时，六段证据链必须覆盖文字、按钮状态、命中区和动态槽位，而不只记录整页图片尺寸。
- `code-quality-gates.md` / `review-protocol.md`：把“真资源作为背景并覆盖替代 UI”列为阻塞验收的问题。
- `validate-workflow.mjs` 只校验任务/文档是否包含必填合同，不用静态关键词冒充视觉正确性。
- `validate-workflow.mjs` 必须用正/负样例拒绝缺少显示列表、原版视觉基准、现代例外或逐状态验收的 UI task；视觉本身仍由证据和人工/图像对照判断。
- 运行验收必须从正式入口逐页检查原生按钮的 up/over/down/selected、命中边界、动态槽位和返回流程。

### 3.4 Skill 边界

当前不以 skill 代替治理门禁。Skill 不能强制项目任务、评审和校验器遵守合同，也容易在项目规则变化后漂移。待炼丹炉、技能页及至少一个后续页面形成稳定样本后，可另行评估是否将成熟方法抽成跨项目的“Flash UI 原生化”skill。

### 3.5 调度边界

登记 PG-007 不改变游戏任务 `WIP=1`。当前 `LINE-STAGE-2-1` / `GOAL-009` 继续保持 Active；技能页整改只有在用户明确批准暂停或重排当前功能线后，才进入游戏 task/Goal 系统。

## 4. 测试方案

- 存量审计：逐个统计 `Formal*PageView.ts` 中现代矩形、现代标题、通用按钮和 extracted asset 的组合，并人工判定是否属于替代覆盖层。
- 证据门禁：检查 UI 逆向文档是否包含原生文字、按钮状态、命中区、注册点、矩阵、动态槽位、证据分级和反证条件。
- 显示列表门禁：检查 root/child/depth、嵌套变换、mask/filter、TextField、动态 child 与现代资源一一映射。
- 视觉基准门禁：检查原版基准的来源/状态/尺寸，以及同尺寸叠图、像素/边缘差异、容差和逐对象差异清单。
- 自动合同：验证原生资源 key、按钮状态资源、940×590 坐标、相邻命中区不重叠和透明命中区不绘制替代视觉。
- 回归测试：业务事务、P1/P2 owner、切页返还、关闭、保存和重载不得因视图原生化而变化。
- 运行观察：从正式入口逐页保存 940×590 的 normal/hover/pressed/selected 和关键业务状态截图，检查 console 无 error/warning。
- 工作流校验：运行 `npm run check:workflow` 和 `git diff --check`；实际代码整改另运行 `npm run check:structure`、相关专项、`npm run test:systems` 与 `npm run build`。

## 5. 测试结果

当前已完成规则落地，存量迁移仍待执行：

- 炼丹炉从“参考样本”退回存量反例：功能/路由已验证，但显示列表、右侧 5×5 背包、角色入口和原版视觉基准没有闭合，不能再标记为视觉原生化完成。
- 技能、宠物和法宝页均命中“真资源背景 + 现代覆盖层”的高风险模式，技能页被用户明确指定为下一整改对象。
- `reverse-engineering-protocol.md`、`task-generation.md`、`code-quality-gates.md`、`review-protocol.md`、Agent 入口和 `validate-workflow.mjs` 已接入显示列表、原版基准、现代例外和逐状态差异门禁。
- 技能两个 Planned task 已补齐 UI 原生化合同；尚未建立全量页面矩阵，尚无技能页或第二个存量页面整改结果。
- `npm run check:workflow` 已通过；UI 合同六字段正样例通过，逐字段删除的六个负向样例均被拒绝，逆向协议新增 UI 门禁/原版基准退化样例也被拒绝。

因此 PG-007 进入“规则已落地，运行观察中”，不能以协议/校验器完成或任何单个页面样本关闭。

## 6. 关闭标准

- “UI 原生化”定义进入任务生成、逆向、实现和评审门禁，且工作流校验能拒绝缺少原生化合同的新增 UI task。
- 逆向门禁明确覆盖完整显示列表、原版视觉基准、逐状态叠图/像素或边缘差异和可见对象差异清单。
- 正式功能页存量矩阵覆盖所有已实现和计划中的 UI 页面，并为每个未原生化页面留下游戏 task/排期或明确排除理由。
- 技能页完成原生文字/按钮/布局迁移，业务、双 owner、存档和正式路径回归通过。
- 至少再完成一个存量页面整改样本，证明方案可复用且没有演变为一次性炼丹炉特例。
- 至少三个后续 UI task 在增量门禁下没有新增替代外层 UI；命中现代例外时均有证据、最小边界和用户验收。
- 自动检查、系统测试、构建、940×590 逐状态视觉验收和 console 检查全部通过。
- Skill 是否创建不是 PG-007 的关闭条件；只有治理方法稳定且确认存在跨项目复用需求时才单独决定。

以下只能算部分缓解，不能关闭：

- 只删除暗色背景，但仍用现代文字/通用按钮覆盖原生中文字。
- 只把按钮改成透明命中区，却没有复用原版按钮状态或确认命中几何。
- 只有整页背景、静态源码计数、路由/事务测试或现代页面截图，没有完整显示列表和原版视觉基准。
- 只整改技能页，没有建立后续任务门禁。
- 只增加文档规范，没有存量迁移样本和增量防复发样本。

## 7. 适用触发与反馈记录

触发条件：

- 新增或修改 `src/scenes/feature-ui/*View.ts`、正式 HUD/菜单/功能页视图或 UI host。
- 新增、拆分、实现、评审或关闭任何带真 UI、原版页面、按钮、菜单、HUD、overlay 的 task/Goal/功能线。
- 接入恢复 SWF 页面、按钮、文字、时间轴帧或把 extracted asset 用作整页背景。
- diff 同时出现 extracted UI asset 与新的全屏矩形、现代标题、通用按钮或替代标签。
- 用户反馈页面“不是原生 UI”、按钮未直接使用原图、布局被现代外层 UI 覆盖。

效果检查：

- 窄查目标 view 的矩形、文字、通用按钮、透明命中区和原生资源组合，人工区分动态槽位与替代覆盖层。
- 检查对应 task/逆向文档是否包含 UI 原生化合同、允许的现代例外和逐状态运行验收。
- 检查存量矩阵剩余缺口、已整改页面数量以及最近三个适用 UI task 是否复发。

| 日期 | 任务/变更 | 适用性 | 证据 | 结论 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-31 | `TASK-SETTINGS-069` 五角色战斗 UI 拆分 | 适用 | 父索引明确角色动态层、头顶状态、HUD 固定层和输入命中区的 child/depth/矩阵/文字/按钮状态清单；原版 940×590 基准、单人五角色/合法双人、逐状态并排或叠图、对象差异和零现代可见例外均为子任务门禁 | 通过 | `069A..E` 不得以现有 HUD 已复现或技能逻辑测试代替逐角色显示列表；`158A..E` 必须逐状态消费真对象并记录差异 |
| 2026-07-26 | `TASK-SETTINGS-067` 关卡五入口视觉合同 | 适用 | 574 五按钮、共享 hit、P2 镜像、371 设置十个根 child、13 个按钮四状态、444 两帧/三按钮、页面根关系与 940×590 原 SWF 基准均已落盘；当前现代暗层/标题/边框/跨页/workshop 明确列为未批准差异而非“共享 host 完成” | 通过 | 156A/B 必须直接消费原状态并移除关卡可见通用 host；156C 以五关 P1/P2 逐状态差异验证，业务可达不得单独关闭 |
| 2026-07-25 | 共享 UI 组件化任务合同 | 适用 | `task-generation.md` 明确组件只共享行为/投影、页面保留原生 Symbol/皮肤/几何；`014A..F` 全部同时声明 UI 原生化与组件化合同，禁止通用现代皮肤 | 通过 | 执行各迁移 Goal 时仍逐页面提供原版基准和差异证据，组件复用不替代视觉验收 |
| 2026-07-25 | `TASK-SETTINGS-070` 背包显示列表复核 | 适用 | 复核 character 304/246/628 的根、四页签、按钮状态、分页、25 格、动态图标/数量/选中与命中区；428 项图标均为恢复 SWF 精确 provenance，3 项原版缺陷默认排除，新增可见现代例外为零 | 通过 | `TASK-SLICE-160` 必须逐状态消费该合同并补 940×590 并排/叠图；不得用通用占位或页面可达替代全量视觉闭合 |
| 2026-07-25 | `TASK-SETTINGS-070 / TASK-SLICE-160` 背包资源前置定义 | 适用 | 证据与实现 task 均包含 304/246 根、四分类、分页、25 格、装备槽、动态图标/数量/详情/选中态、P1/P2 和返回的显示列表合同；要求 940×590 首/中/末页基准、逐状态并排/叠图，缺失图标默认不得用通用占位或现代卡片替代 | 通过 | 执行 070 时先闭合资源与动态 child 映射；执行 160 时补真实页面差异样本，业务测试和页面可达不得单独关闭 |
| 2026-07-25 | `TASK-SETTINGS-066C` 设置页视觉合同 | 适用 | 148 根页、134 全舞台阻挡面、五个静态标签、五个 145/146 动态值 wrapper、144 关闭按钮四态、命中区、生命周期与 940×590 原版基准均有 restored SWF 派生证据；跨重启持久化不新增可见层，默认零现代可见例外 | 通过 | `155C` 不得以业务测试或整页背景单独关闭；须逐行 hover/循环、关闭 pressed 与重启前后并排/叠图 |
| 2026-07-25 | `TASK-SETTINGS-066B` 商城页视觉合同 | 适用 | 721 根页、717 商品卡、624 确认弹窗的显示列表，16 按钮四状态、六个 TextField、动态商品 child、命中区及 940×590 原版基准均有 restored SWF 派生证据；默认零现代可见例外 | 通过 | `155B` 不得以业务测试或整页背景单独关闭；须逐状态并排/叠图，停服充值只能保持禁用/说明，不得伪造在线能力 |
| 2026-07-24 | 前置体验 UI/动画任务生成 | 适用 | 地图四服务页、关卡五入口、角色战斗 UI 的证据 task 与 Split 父 task 均包含显示列表、原版 940×590 基准、现代例外、逐状态验收和差异证据；明确禁止以现代覆盖按钮、状态文字、占位 projectile 或整页背景关闭视觉 | 通过 | 后续每个逐页/逐角色子 task 继承合同，并分别留下实现与运行差异样本 |
| 2026-07-23 | PG-007 登记与存量基线 | 适用 | 技能/宠物/法宝 view 分别存在 3/3/6 个矩形和 9/11/6 组通用按钮；炼丹炉整改后为 0/0，用户明确要求技能及后续系统统一原生化 | 复发 | 建立原生化合同与全量矩阵；由用户决定是否暂停 Stage 2-1 并生成技能整改功能线/Goal |
| 2026-07-23 | 技能页面 UI 原生化任务生成 | 适用 | 新建 Planned `LINE-UI-NATIVE-SKILLS`，覆盖 250/868/417/213 四页；`TASK-SETTINGS-061` 先闭合原生文字/按钮/状态/槽位证据，`TASK-SLICE-143` 后移除现代覆盖层并验证业务/双 owner/V4 | 通过 | 保持 Stage 2-1 唯一 Active；待用户批准调度后以技能页形成首个 PG-007 存量迁移样本 |
| 2026-07-23 | 显示列表与视觉基准治理规则落地 | 适用 | 用户指出工坊虽无矩形/通用按钮但仍与 Flash 差异巨大；复核 `StrengthEquipment/BackPackElement` 证明动态显示列表证据存在却未被实现和验收消费。任务生成、逆向、质量、评审、Agent 入口和校验器现强制显示列表/原版基准/现代例外/逐状态差异合同 | 通过 | 工坊退回存量反例；后续以技能页和至少一个其他页面验证规则，不以协议落地提前关闭 PG-007 |
| 2026-07-23 | `TASK-SLICE-147` Stage 1-1 结果流程回归 | 适用 | task 定义包含门、结果 overlay、重玩/返回的显示对象边界、既有基准、零新增现代视觉例外和逐状态合同；实现未新增/替换任何可见 UI，仅保护既有 W/↑ → 结果接线 | 通过 | 本样本只证明回归任务遵守 UI 门禁，不宣告 Stage 1-1 结果页原生化；继续由技能 UI 线提供存量整改样本 |
| 2026-07-23 | `TASK-SLICE-148` Stage 1-1 普通门整改 | 适用 | 删除 frame/glow/DOOR 三项现代显示对象，直接复用其他关卡已接入的原版 W 门 image；专项静态门禁禁止旧文字和错误清怪条件回流 | 通过 | 这是既有原版资源直接复用样本；技能页仍须完成完整显示列表与逐状态视觉整改 |
| 2026-07-23 | `TASK-SETTINGS-061` 技能四页原生证据 | 适用 | 250/868/417/213 的 root/child/depth、TextField、按钮 up/over/down、角色 selected、主动图标三态、P1/P2 键槽、动态 addChild、被动五帧与原版 940×590 基准全部落盘；当前 view 的暗层/外框/标题/通用按钮/永久摘要逐项列为替代层，新增可见例外为空 | 通过 | 这是 PG-007 首个完整证据门禁样本，尚不是存量迁移完成；由 `TASK-SLICE-143` 移除替代层并补逐状态运行差异证据 |
| 2026-07-23 | `TASK-SLICE-143` 技能四页存量迁移 | 适用 | `FormalSkillPageView.ts` 删除矩形、现代标题、通用按钮和永久摘要；直接消费 250/868/417/213、7 组按钮三态、角色 selected、50×3 技能帧、五键槽与五被动行。专项/全系统/build、940×590 P1/P2/成功/拒绝/绑定重载及新标签页零 console 通过 | 通过 | 首个完整 PG-007 存量迁移样本成立；PG-007 仍保持观察中，等待至少一个其他页面整改和三个增量 UI task 样本 |
| 2026-07-24 | `TASK-SETTINGS-065` 新建存档人数/选角证据 | 适用 | 记录 character 1149 人数页与 901 角色页的 root/child/depth、TextField、五卡灰度/彩色四态、selected、P1/P2 marker、舞台命中列、940×590 裁切、零新增可见例外和逐状态验收输入 | 通过 | 形成第二个完整 UI 证据门禁样本；由 `TASK-SLICE-151` 提供实现与视觉差异样本 |
| 2026-07-24 | `TASK-SLICE-151` 新建存档增量原生 UI | 适用 | `SavePartyCreationView` 直接消费 1149/901、五卡三态与 P1/P2 marker，透明命中区承载原交互；没有现代标题、边框、文字列表、通用按钮或可见确认/取消。专项与 940×590 人数/P1/P2/selected/取消/重载零 console 通过 | 通过 | 形成首个完整增量 UI task 样本与第二类页面实现样本；PG-007 保持观察中，继续积累至少两个增量 UI task |
| 2026-07-24 | `TASK-ARCH-012` 场景资源分包任务生成 | 适用 | task 虽只改变加载时机，仍显式声明显示列表、原版 940×590 基准、零新增可见例外、逐状态验收和同尺寸差异证据；加载失败复用现有反馈区 | 通过 | 实施时不得借加载架构新增通用遮罩/弹窗或替代按钮；PG-007 保持观察中 |
| 2026-07-24 | `TASK-SLICE-152` 技能 owner/角色收敛 | 适用 | 沿用 250/868/417/213 完整显示列表与 `TASK-SLICE-143` 原版基准；只把 selector 实例集合和数据 owner 改为 V5 party，未新增标题、提示框、通用 owner 按钮或替代层。940×590 单/双 owner 与零 console 通过 | 通过 | 技能页未复发；形成增量 UI 行为修改样本，PG-007 继续等待其他页面整改与增量样本 |
| 2026-07-24 | `TASK-SLICE-153` 删除地图现代 chooser | 适用 | 沿用原天庭地图显示列表和 940×590 基准，删除现代遮罩、1P/2P 按钮与 chooser 状态；未新增确认层、标题、通用弹窗或替代按钮。静态门禁禁止相关对象/文字回流，运行证据显示节点直接进关 | 通过 | 地图 UI 未复发；PG-007 保持观察中，后续资源分包不得借加载失败新增现代遮罩 |
| 2026-07-24 | `TASK-SLICE-152` 技能 owner/角色收敛 | 适用 | 沿用 250/868/417/213 完整显示列表与 `TASK-SLICE-143` 原版基准；只把 selector 实例集合和数据 owner 改为 V5 party，未新增标题、提示框、通用 owner 按钮或替代层。940×590 单/双 owner 与零 console 通过 | 通过 | 技能页未复发；形成增量 UI 行为修改样本，PG-007 继续等待其他页面整改与增量样本 |
| 2026-07-24 | `TASK-ARCH-012` 场景资源分包实现 | 适用 | 冷刷新、地图、技能、Stage 1-1/2-2 沿用既有显示列表和 940×590 基准；等待只复用 Boot/地图现有反馈文字，失败停留当前画面，未新增遮罩、弹窗、按钮或替代层 | 通过 | 加载架构未造成 UI 原生化复发；PG-007 保持效果观察 |
| 2026-07-24 | `TASK-SLICE-154` 技能页用户反馈整改 | 适用 | 用户反馈发现 868/213 扁平基底仍携带动态 child，导致与运行时 child 重复；本次从基底移除重复实例，五角色/被动逐状态截图无叠字。新增 `P1技能/P2技能` 仅为用户明确批准、复用炼丹炉风格的现代例外 | 复发后修复 | 旧 `TASK-SLICE-143` “通过”不能证明扁平导出已正确拆静态/动态；后续 UI 审计增加“基底不得包含运行时再次创建的实例”检查 |
| 2026-07-24 | `GOAL-035` / `TASK-ARCH-013A` 灵魂字段迁移 | 适用 | `FormalSkillPageView` 与工坊 view 只把动态数值读取从嵌套字段改为同一玩家直属字段；没有新增、删除或替换显示对象、按钮、文字样式、资源、坐标或现代可见例外 | 通过 | 既有原生显示列表未改变；PG-007 保持效果观察，`GOAL-036` 浏览器旅程继续复验视觉与 console |
| 2026-07-24 | 技能页十张心法选择器反馈 | 适用 | 显示列表已记录 597/608，但实现只创建透明命中区和文字，未导出/加载/渲染两组各五帧真图；用户复验发现五角色全部缺图。本次补齐原资源、角色映射、原坐标、bundle 逐帧门禁与 940×590 证据 | 复发后修复 | UI 验收必须逐项核对显示列表对象实际可见，不得以命中区存在或相邻文字正确代替视觉对象覆盖 |
| 2026-07-24 | 技能/炼丹炉共享灵魂余额 | 适用 | 工坊扁平 PNG 把动态数字误留为 `1234567890`，技能页与工坊页又分别绘制/遗漏实时值；本次保留原图标和底栏，只用共享不透明动态文字槽覆盖原数字范围，未生成或重绘整张位图。940×590 双页同值且零 console | 复发后修复 | 用户批准的现代例外限定为共享动态数值槽；后续扁平基底审计必须区分静态装饰与动态 TextField，并验证同一领域值跨页一致 |
| 2026-07-24 | 共享灵魂余额原版视觉二次反馈 | 适用 | 用户指出首轮黑底遮盖“灵魂”标签且数值不像原版，推翻“黑底属于批准例外”的判断。FFDec 交叉确认技能 249 与工坊 103 均为 `FZCuYuan-M03` 白色无描边 TextField；最终派生无动态占位的 119 原生根并按源几何投影，940×590 双页无黑条 | 复发后修复 | 动态字段不得靠扩大不透明矩形清理扁平占位；应从派生底图移除动态实例，再以共享组件恢复原 TextField 样式和页面各自坐标 |
| 2026-07-24 | 共享灵魂余额字体与基线三次反馈 | 适用 | 用户指出浏览器字体投影的字体、字号和位置仍偏离原版；仅声明 `FZCuYuan-M03` 会发生字体回退，Canvas 文本度量也不等于 Flash。最终改为直接渲染 DefineEditText 103 的嵌入数字轮廓、原缩放、基线和 advance | 复发后修复 | 原版嵌入字体可恢复时不得以系统字体近似关闭原生化；动态文字至少要核对轮廓、缩放、基线、advance 与字段对齐 |
| 2026-07-25 | 地图四服务页根 UI 检查点与拆分 | 适用 | 已标注 990/721/148/85 的根/depth/几何与 940×590 normal 基准；明确根 SVG 不能代替按钮态、动态 child 或交互整页，四个证据/实现 task 均继承显示列表、现代例外、逐状态与差异门禁 | 通过 | `066A..D` 必须继续核对每个可见对象和动态 TextField；`155A..D` 分别留下运行差异样本 |
| 2026-07-25 | `TASK-SETTINGS-066A` 丹药页视觉合同 | 适用 | 990 全显示列表、969/1006 子件、968/973/989/997 四状态、五 owner 两帧、TextField、命中区和对象差异清单均有原 SWF 派生基准；默认零现代可见例外 | 通过 | `155A` 不得以路由、业务测试或整页截图单独关闭；须逐状态并排/叠图 |
| 2026-07-25 | `TASK-SETTINGS-066D` 任务页视觉合同 | 适用 | character 85 的双页签、五 tile、详情/进度、四奖励格、领取、分页、关闭、已领取动态图与嵌入字体均已记录 Symbol/depth/坐标/状态/命中；活动空页是原版事实，新增可见现代例外为零 | 通过 | `155D` 必须逐对象消费真资源并验证 selected/分页/领取/空活动/跨日；不得用现代列表或可达性代替原生化 |
| 2026-07-25 | `TASK-SLICE-160` 正式背包原生化 | 适用 | 304 根、246 分类、628 格、22 个原生按钮/格子状态和 428 真图标按原坐标/层级重建；对象差异与四张 940×590 状态证据已归档，零现代卡片/占位，console 为 0 | 通过 | P2/满包由确定性门禁覆盖；后续 UI 仍须同时提供运行差异证据 |
| 2026-07-25 | `TASK-SLICE-155A` 丹药页原生化 | 适用 | 按 990 完整显示列表组合 969/1006、四类原按钮、五 owner 与 25 真图标；动态 TextField/提示等价重建，零现代可见例外；940×590 逐状态、稳定区域差异与对象差异清单均归档 | 通过 | 单页 UI 未复发；商城等后续页面仍须独立逐对象和逐状态验收 |
| 2026-07-25 | `TASK-SLICE-155B` 商城页原生化 | 适用 | 721 根、717 九卡和 624 确认框完成静态/动态 child 分离；16 组原按钮三态与 49 真图标直接接入，动态 TextField/toast 等价重建，零现代可见覆盖；940×590 逐状态、稳定区域差异与对象差异清单归档 | 通过 | 商城单页未复发；设置、任务仍须各自逐对象和逐状态验收 |
| 2026-07-25 | 商城右下角共享灵魂余额反馈 | 适用 | 用户明确批准在 character 721 原页之外增加技能页/炼丹炉同款余额组件；首轮却从运行截图裁图，携带黑色舞台背景并再次遮字。最终改为直接提取 character 250 / shape 236 的 82×82 RGBA `PatternID_236_2`，按原矩阵坐标投影，940×590 复验无矩形边界 | 复发后修复 | 现代例外不等于允许截图裁片；共享 UI 子件必须追到独立透明显示对象，并由生成脚本和 alpha/尺寸门禁防止背景回流 |
| 2026-07-25 | `TASK-SLICE-155C` 设置 overlay 原生化 | 适用 | 直接复用 148/134 面板与 144 关闭三态；五个原 TextField 因 FFDEC SVG 中文字形为空而按原坐标/字体/字号/边界等价重建，动态值、hover、死控件和全屏阻挡逐项接回。940×590 并排、逐状态、RGB MAE 3.4953 与零 console 已归档，未新增现代标题/暗罩/通用按钮 | 通过 | 设置单页未复发；任务页仍须逐对象消费 85 显示列表，字体等价重建继续明确记录栅格容差 |
| 2026-07-26 | `TASK-SLICE-155D` 任务页原生化 | 适用 | character 85 底层地图 host、44/49、54/60/73、31/78/83、9、四奖励图与权威物品图逐对象接入；首次浏览器观察发现独立黑底偏差后恢复 MapMenu 底层，最终 940×590 日常/selected/空活动/末页/关闭零 console | 通过 | 没有现代列表、owner selector 或活动说明层；运行观察实际阻止了“业务正确但 host 层级错误”的关闭，PG-007 保持效果观察 |
| 2026-07-26 | `TASK-SLICE-156A` 五真 HUD pointer | 适用 | 574 原生 up/over/down 与共享 418 hit 直接接入，P2 按原矩阵镜像；没有新增可见标题、边框、tooltip 或替代按钮。940×590 双人 normal/hover/P2 打开原生背包和零 console 已归档 | 通过 | 156A 未复发；现代可见跨页 host 仍是 156B 明确待整改项，不能以本切片提前关闭 VS-060 |
| 2026-07-26 | `TASK-SLICE-156B` 371/444 与战斗 host 整改 | 适用 | 371/444 整帧、11 个原按钮四态和 366 三帧直接接入；combat origin 不再绘制暗层/标题/边框/跨页/workshop/通用关闭，只呈现当前原生页 | 通过 | 静态对象差异与专项无现代可见回填；五关逐状态、像素差异和 console 证据由 156C 补齐后再关闭 VS-060 |
| 2026-07-26 | `TASK-SLICE-156C` 五关 HUD 校准 | 适用 | 未新增显示对象；五个原按钮与 418 等价命中统一固定 HUD 坐标，五关复用同一原生层。940×590 normal/hover、正式节点、对象差异和现代例外已归档 | 通过 | 战斗暗层/标题/边框/跨页/workshop/通用 Escape 未复发；内置浏览器 canvas 输入限制公开保留，不反向伪造运行截图 |
| 2026-07-31 | `TASK-SETTINGS-069A` Role1 显示列表门禁 | 适用 | 冻结本体/装备同 cell depth、名字上层、影分身矩阵、P1/P2 HUD 镜像、505 头像/510 五槽/五按钮 owner，并定义 940×590 单人及合法双人逐状态并排/叠图计划；现代例外为空 | 通过 | 真 HUD 背景或逻辑测试不能替代本体/技能逐对象验收；158A 必须删除 placeholder/Arc/Text 回填后才能关闭 Role1 视觉 |
| 2026-07-31 | `TASK-ARCH-016A` 五关显示列表迁移合同 | 适用 | ADR 逐关冻结 floor/background/foreground/door/HUD/result 根、层级、矩阵、动态 child、命中区与 provenance；现存 placeholder/标题/QA/1-1 借门均未升级为获批例外，禁止新增现代门/暗层/结果页 | 通过 | 016B..D 必须沿用既有 940×590 基准逐状态验收；合同本身不替代运行视觉证据 |
| 2026-08-01 | `TASK-SLICE-158A` Role1 显示列表实现 | 适用 | 身体/装备同帧双层、影分身、技能对象与既有 574/505/510 HUD 逐对象映射；Role1 projectile 在通用 Arc/Text 前返回真 image，死亡不再用灰色占位；现代可见例外为空 | 通过 | Role1 增量样本未复发；Role2..5 必须分别重复该门禁 |
