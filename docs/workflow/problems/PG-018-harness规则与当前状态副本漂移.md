# PG-018 harness 规则与当前状态副本漂移

状态：已归档。
来源：2026-09-05 harness 结构评审；用户明确要求改善。

归档信息：2026-09-05，本批规则引用、派生快照、独立检查与兼容全量门禁闭环；持久防线为 check:harness 的 13 项测试、真实文档检查和 check:workflow。按第 7 节精确信号重开。

## 1. 问题定义

客户端规则副本与当前调度叙述需要手工同步，已出现分歧；全量校验通过不能排除这种分歧。独立 harness 检查与游戏专项检查缺少清晰命令边界。

## 2. 证据

整改前 CLAUDE.md:84 对 warning 强制拆分，AGENTS.md:13 允许说明后小修；看板当前推荐同时存在 214 与旧 211 的表述；执行队列保存旧 208A；compact 收束条目仍建议继续 compact。详见当日评审。

## 3. 解决方案

V1：结构决策引用 AGENTS 第 7 条；compact 统一安全交接检查点；从状态表生成当前推荐并精确校验，旧叙述移至治理日志；新增独立 check:harness，完整 check:workflow 保留所有原有检查能力。限定已发现的入口与当前快照，不迁移全部历史或重新定义游戏 task。

## 4. 测试方案

运行 check:harness 的有效、Blocked、旧推荐、双 Ready、跨线、旧指针、双治理 owner、旧队列 id、严格客户端规则、compact 继续执行、完整门禁遗漏样例；运行 check:workflow、audit:problems、git diff --check。生成两次应幂等；检查任务状态表不变。

## 5. 测试结果

check:harness 的 13 项测试通过，包括旧状态/冲突规则拒绝、同线其他 Blocked 不被误判成执行 owner、无源码/资源临时目录集成、断链拒绝、生成幂等和无效状态不写入。check:workflow 通过；保留原有 PlayerSlot 领域别名 warning，本批不涉及游戏源码。

首次完整检查发现旧校验器只识别内联 PG 命令，随后窄改为同时识别 check:harness 的确定性调用链，再次检查通过；没有重复执行 PG 或删除旧门禁。audit:problems 命中 PG-004/018；其他活跃 PG 无语义触发。

## 6. 关闭标准

当前规则引用一致；当前快照与状态表一致且自动拒绝历史残留；负向测试稳定拒绝错误；独立入口不执行源码/资源检查，完整入口保留原门禁；生成幂等且未改游戏排期。满足上述硬门禁即可归档，不额外要求观察期。

## 7. 适用触发与反馈记录

触发条件：修改上述结构规则引用、compact 收束、调度快照或 package.json 校验组合；用户报告相同分歧；check:harness 失败。

效果检查：运行 check:harness 与 check:workflow；核对当前推荐和唯一 Active/Ready；新增客户端先验证其引用边界。自然语言任意改写不能只依靠有限负向样例判定语义等价。

反馈：本批已满足第 6 节硬门禁，同次归档。归档后的精确重开信号为当前入口再出现冲突决策或旧执行项、派生快照门禁失效、完整门禁被遗漏。
