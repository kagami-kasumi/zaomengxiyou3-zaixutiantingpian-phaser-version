# PG-005 逆向证据链不完整却宣布闭合

状态：规则已落地，运行观察中。

来源：2026-07-21 用户复盘 Stage 1-2 初版表现问题，指出只对关卡做局部逆向、忽略既有 AS3 上下文会产生幻觉，并明确要求重构逆向流程。

## 1. 问题定义

既有流程只要求“定位对应 AS3”并更新机制索引，没有强制从关卡局部数据继续追踪共享输入、角色、物理、镜头和状态机，也没有要求合并 SWF 几何、注册点、坐标空间与运行观察。证据缺环时，合理猜测可能被写成“权威输入”或“已闭合”。

这会使资源数量和局部状态测试全部通过，但玩家最基本的按键、站位、镜头和触发时机仍与原版不符；跨对话后还可能因为只保留结论、不保留证据链而重复犯错。

## 2. 证据

- Stage 1-2 初版记录只接入 P1 `A/D/J/W`，而 `KeyBoardControl.as` 已明确 P1 `[S,J,K,W]`；K 跳跃被漏掉。
- 初版把原 MovieClip 注册点 `(100,350)` 当成现代占位角色脚底，未结合 `ObsWall` 矩阵、碰撞顶面与素材原点，导致角色和怪物未站在同一地面线。
- 初版使用无直接 AS3 证据的 `stopPoint.x - 90` 世界坐标阈值；重新追踪 `ViewControllor.as` 后才发现原版在屏幕 `900..980` 区间调用 `StopPoint.touch()`。
- `agent-protocol.md` 原规则只有“先定位对应 AS3”，没有局部证据、共享调用链、SWF 几何、行为合同、现代映射和双重验证的闭合要求。

## 3. 解决方案

- 新增 `reverse-engineering-protocol.md`，建立六段证据链和统一证据分级。
- 关卡逆向必须从局部类继续追踪共享运行时消费者；视觉/空间结论必须记录坐标空间、注册点、碰撞盒、矩阵和推导公式。
- 逆向产物必须落盘证据矩阵，区分确认事实、交叉确认、推断、未知和现代设计选择。
- 实现任务必须重读矩阵引用的窄证据，不得只依赖聊天或 compact 摘要；影响验收的推断/未知未清零时先补逆向。
- “已扒、权威实现输入、已闭合、已复现”增加分级关闭门禁；自动校验确保所有入口和执行协议引用新协议。

不要求逐行翻译全部 AS3，也不要求每个任务无差别全文读取大型源文件；仍以待证明问题驱动窄查。

## 4. 测试方案

- 运行 `node --check tools/validate-workflow.mjs` 和 `npm run check:workflow`。
- 校验新协议必须包含六段证据链、五种证据等级、证据矩阵、坐标语义、上下文交接和状态关闭门禁。
- 使用内置负向样例确认删除共享运行时调用链、证据矩阵或证据分级时校验失败。
- 检查 AGENTS、CLAUDE、README、文档地图、执行协议、任务生成、代码质量门禁和逆向 researcher 均路由到新协议。
- 用 Stage 1-2 三项失败作为历史反例回放；后续至少三个逆向/实现样本按新矩阵执行。

## 5. 测试结果

- `npm run check:structure` 通过，仅保留 8 个未修改旧大文件的既有 warning。
- `node --check tools/validate-workflow.mjs` 通过。
- `npm run check:workflow` 通过；协议、任务入口、代码门禁和逆向 researcher 路由一致，任务/历史与 372 条资源标注保持有效。
- 校验器内置的三个负向样例均被拒绝：删除共享运行时调用链、删除证据矩阵或删除“推断”分级，任一情况都无法通过逆向协议契约检查。
- Stage 1-2 历史反例已按六段证据链复盘，确认三项错误分别来自共享输入遗漏、SWF 几何/注册点语义缺失和共享镜头调用链未追踪。

规则和自动门禁已落地；尚需三个后续逆向/实现样本验证流程在真实任务中持续执行，因此保持运行观察中。

## 6. 关闭标准

- 所有逆向入口都使用同一证据协议，不再只要求“找到 AS3”。
- 自动校验能拒绝缺少共享调用链、证据分级或证据矩阵的协议退化。
- 至少三个后续逆向/实现样本留下完整证据矩阵，且实现交接重新读取其关键证据。
- 用户运行反馈与逆向结论冲突时，结论能立即降级、回审并更新，而不是继续维持“已闭合”。
- 至少一个视觉/坐标样本和一个跨共享系统调用链样本通过双重验证。

仅新增协议文档、仅增加 AS3 文件列表或只跑自动测试，都只算部分缓解。

## 7. 适用触发与反馈记录

触发条件：

- 新增或修改任何玩法/关卡/视觉逆向结论、`TASK-SETTINGS-*`、`mechanics-index.md` 的“已扒”状态，或实现代码声称还原原版行为。
- 出现用户运行反馈与既有逆向结论冲突、跨对话恢复逆向上下文，或使用“权威实现输入、已闭合、已复现”等状态。

效果检查：

- 检查是否有完整证据矩阵、共享调用链、坐标语义、证据分级、未知项和双重验证。
- 检查实现是否重新读取落盘证据，是否存在无证据常量、把推断写成事实或只用局部关卡类宣布闭合。

| 日期 | 任务/变更 | 适用性 | 证据 | 结论 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-21 | Stage 1-2 初版历史回放 | 适用 | K、地面注册点和停点屏幕触发分别暴露共享输入、几何语义和共享镜头调用链缺环 | 复发 | 作为治理前基线；新协议落地后从后续样本重新计数 |
| 2026-07-21 | PG-005 与逆向协议首次落地 | 适用 | 六段证据链、证据分级、矩阵、关闭门禁和八处入口路由已建立；工作流校验通过，三个协议退化负向样例均被拒绝 | 通过 | 后续三个逆向/实现任务按证据矩阵积累真实运行样本 |
| 2026-07-21 | `TASK-SETTINGS-052` / `TASK-SLICE-129` Stage 1-3 | 适用 | `levels-index.md` 留下局部类、共享刷怪/镜头/输入调用链、SWF 矩阵/注册点、行为合同、现代映射和自动+浏览器双验证矩阵；地面公式误差由测试发现后回写证据 | 通过 | 形成首个协议落地后的完整关卡样本；继续观察两个逆向/实现样本 |
| 2026-07-21 | `TASK-SETTINGS-054..058` 任务生成 | 适用 | 新增五个逆向/盘点任务均要求六段证据、共享消费者、适用的资源几何/坐标、证据分级和双重验证；`M-048..052` 只标记部分已扒/未扒，没有把规划内容宣告为事实 | 通过 | 这是任务定义样本，不计入完整逆向/实现关闭样本；执行时仍须落盘矩阵并重新窄读证据 |
| 2026-07-21 | `TASK-SETTINGS-054` Stage 1 战斗校准证据 | 适用 | `stage1-combat-calibration.md` 逐段记录局部怪物/角色 AS3、共享攻防与保护/续航调用链、恢复资源与几何缺口、行为合同、现代 owner 和自动+人工验证；怪物精确 SWF character id 与逐次无敌均保持未知 | 通过 | 形成协议落地后的第二个完整逆向样本；`TASK-SLICE-130` 实现前须重新窄读该合同，并不得把现代预警几何写成原版事实 |
| 2026-07-21 | `TASK-SLICE-130` Stage 1 战斗实现检查点 | 适用 | 实现前重读六段证据；黄色/红色圆形预警明确标注为现代占位，怪物真素材/几何继续保持未知；自动回归与 1-2 运行样本已落盘，但 1-1/1-3 运行验证未齐，未宣告已复现 | 通过 | 完成剩余试玩后再归档；真素材定位前不得升级为像素级复现 |
| 2026-07-22 | `TASK-SLICE-130` Stage 1 战斗实现闭合 | 适用 | 1-2/1-3 代表运行与 1-1 三次完整通关补齐双重验证；现代占位反馈、跳跃/碰撞校正和审计 watchdog 均明确不冒充原版事实，怪物精确 SWF 几何继续列为未知 | 通过 | 本切片可归档；后续真素材任务仍须独立补齐资源和几何证据 |
| 2026-07-22 | `TASK-SLICE-134` 怪物重力与死亡奖励 | 适用 | `drops-index.md` 六段矩阵覆盖局部死亡入口、共享收益消费者、物理/寻路、恢复 SWF symbol、现代映射和验证；注册点未知与现代偏移明确分离 | 通过 | 自动合同已闭合；运行时观感留给用户复验，不把现代 50ms 帧率写成原版事实 |
| 2026-07-22 | `TASK-SETTINGS-055` 正式战斗 HUD | 适用 | `combat-hud-index.md` 从 `RoleInfo/GameInfo` 局部字段追到 `PhysicsWorld` 生命周期与怪物更新链，补齐 `OtherMat1/bossblood` 注册坐标、P2 镜像、键位映射、12 条资源标注、现代 snapshot owner 和双重验证；宠物完整摘要皮肤保持未知并排除于首切片 | 通过 | 形成协议落地后的 HUD/坐标样本；`TASK-SLICE-131` 实现前重读合同并按三关运行态补视觉验证 |
| 2026-07-22 | `TASK-SLICE-131` 正式战斗 HUD 实现 | 适用 | 实现前重读 `combat-hud-index.md` 的六段矩阵；按已证实的 940×590、P2 镜像、槽位映射和 800ms 合同实现，两个组合 SVG 保留来源 provenance；专项测试与 1-1/1-2/1-3 浏览器样本补齐双重验证，宠物完整摘要仍未越界宣称 | 通过 | 核心 HUD 可标记已复现；后续完整功能 UI 仍须独立补资源和交互证据 |
| 2026-07-22 | `TASK-SETTINGS-056` 启动与存档槽 | 适用 | `save-slots-index.md` 从 `SaveInter` 局部槽状态追到 `GameMenu/GMain/MemoryClass/MapMenu` 共享调用链，补齐三个恢复 SWF 精确 symbol、940×590 几何、原版/现代差异、状态机、迁移与双重验证要求；删档缺证据被明确列为现代选择 | 通过 | 形成非战斗主流程样本；`TASK-SLICE-132` 实现前按矩阵重读并验证六槽组合路径 |
| 2026-07-22 | `TASK-SLICE-132` 启动与存档槽实现 | 适用 | 实现前重读 `save-slots-index.md`；按已证实六槽/字段/路由和三个精确 symbol 实现，删档/显式损坏反馈继续标为现代选择；专项测试与 940×590 空/有效/损坏/删除浏览器样本形成双重验证 | 通过 | M-050 可标记已复现；天庭地图仍由 M-051 独立补齐资源、坐标和往返证据 |
| 2026-07-22 | `TASK-SETTINGS-057` 天庭地图选关 | 适用 | `heaven-map-index.md` 从 `SelectPLace` 节点事件追到 `GMain/MainGame/MemoryClass/GameWin/GameFail`，补齐 6 个恢复 SWF character、PlaceObject2 注册点、frame 状态、命中边界、940×590 裁切、现代 1P/2P 与 Stage 2-1 排除合同 | 通过 | 实现前重读矩阵；`s1_3` 离台 hover 异常不得被无证据修成原版像素事实 |
| 2026-07-22 | `TASK-SLICE-133` 天庭地图实现 | 适用 | 实现前重读 `heaven-map-index.md`；按已证实坐标/命中、单调进度、940×590 裁切和三关返回实现，1P/2P 覆盖层与 Stage 2-1 拒绝继续标为现代选择，`s1_3` 离台 hover 未被伪造修复；专项与浏览器样本形成双重验证 | 通过 | M-051 可标记已复现；完整 MapMenu 功能仍由 `TASK-SETTINGS-058` 独立盘点 |
| 2026-07-22 | `TASK-SETTINGS-058` 完整功能 UI 盘点 | 适用 | `full-function-ui-index.md` 从各 UI 局部类追到 `RoleInfo/MapMenu/KeyBoardControl/MainGame/User/MemoryClass`，记录 14 页入口/退出、owner、暂停、保存和 11 个 restored SWF character/边界；Strength/Resolution/Making 行为缺口保持“已定位”并生成同线逆向 task | 通过 | 共享 host 可直接实现；工坊三子页在 `TASK-SETTINGS-059` 清零未知前不得进入行为实现 |
| 2026-07-22 | `TASK-ARCH-008` 共享正式功能页 host 实现 | 适用 | 实现前重读 `full-function-ui-index.md`；只落地已证实 owner/入口/暂停/互斥合同，地图统一入口标为现代导航选择，具体页面继续显示待接入；专项与地图浏览器样本分级记录 | 通过 | host 基础可归档；页面、V4 存档和工坊未知仍由后续任务闭合，不升级为完整 UI 已复现 |
| 2026-07-22 | `TASK-ARCH-009` V4 双玩家功能存档实现 | 适用 | 实现前重读 UI 存档矩阵和现有 V1/V2/V3 owner；V4 只映射已存在的成长/技能/库存/装备/宠物模型，runtime/UI/临时状态明确排除，迁移与损坏边界有专项测试 | 通过 | 存档基础可归档；具体页面和工坊行为仍不得宣告已复现 |
| 2026-07-22 | `TASK-SLICE-135` 真背包/装备页实现 | 适用 | 实现前重读 `full-function-ui-index.md` FUI-02/03 与装备证据；只实现已证实四分类、25 格分页、六槽、owner 和保存合同，未闭合物品效果显式拒绝；真 304/246 资源、专项与 940×590 浏览器样本形成双重验证 | 通过 | FUI-02/03 可归档；完整 1.1 物品表、工坊三页与其余功能页不得据此宣告已复现 |
| 2026-07-22 | `TASK-SLICE-136` 真技能页实现 | 适用 | 实现前重读 FUI-04..07 与 `skills-input-index.md`；复用已证实树/学习/等级灵魂/五槽合同，原版拖拽仅以明确现代指针等价实现；250/868/417/213 真资源、专项与 940×590 浏览器样本形成双重验证 | 通过 | FUI-04..07 可归档；战斗技能数值、宠物/法宝/工坊页面未被越界宣告 |
| 2026-07-22 | `TASK-SLICE-137` 真宠物页实现 | 适用 | 实现前重读 FUI-08 与 `pets-index.md`；只实现已证实每页 5/最多 10、8 槽、owner、领域动作和 932 真资源，放生确认采用明确现代二次点击；浏览器受 URL 策略限制且未绕过，双 owner/runtime/重载以确定性专项补证 | 通过 | FUI-08 可归档；宠物战斗真素材、网络/活动入口和工坊/法宝页面未被越界宣告 |
| 2026-07-22 | `TASK-SETTINGS-059` 工坊行为逆向 | 适用 | `equipment-workshop-index.md` 逐段记录三局部页、119/PackThings/User/AllEquipment/MyEquipObj 共享链、198/177/152 矩阵、可观察事务、现代实例 schema 和双重验证；79 case 机械复核暴露 1 个不可达死分支，没有补成配方 | 通过 | 影响实现的未知已清零；`TASK-SLICE-138` 实现前重读矩阵并完成自动+运行双验证 |
| 2026-07-22 | `TASK-SLICE-138A` 工坊 host/Fusion 实现 | 适用 | 实现前重读 `equipment-workshop-index.md` 的 119/169 容器、owner 与返还合同；只将已有完整 Fusion 迁入正式 host，强化/分解/制作继续标为待接入；浏览器策略限制公开保留，未把自动测试冒充运行截图 | 通过 | FUI-10/11 可归档；FUI-12..14 仍须各自按证据矩阵实现和验收 |
| 2026-07-22 | `TASK-SLICE-138B` 装备强化实现 | 适用 | 实现前重读 `equipment-workshop-index.md` 的 198 几何、5×7 概率、灵魂、白名单、实例/V4 与返还合同；真 198、确定性专项和正式浏览器拒绝样本形成分级证据，当前存档无灵魂时未伪造运行成功 | 通过 | FUI-12 可归档；FUI-13/14 继续按各自证据与 Goal 实现，不从强化事务外推 |
| 2026-07-22 | `TASK-SLICE-138C` 装备分解实现 | 适用 | 实现前重读 `equipment-workshop-index.md` 的 177 几何、品质/类型/角色、后减宝石、神器概率、100 灵魂与返还合同，并窄读 `Resolution/AllEquipment` 一手 AS3；真 177 与确定性专项形成证据，浏览器 URL 策略限制公开保留且未伪造运行成功 | 通过 | FUI-13 可归档；FUI-14 继续按独立 Goal 实现，不从分解事务外推制作书 |
| 2026-07-22 | `TASK-SLICE-138D` 制作书实现 | 适用 | 实现前重读 `equipment-workshop-index.md` 的 152 几何、79 case/1 死分支、费用、材料、三宝石、返还与实例合同，并完整窄读 `Making.as` 一手 AS3；真 152、确定性专项和 940×590 地图入口/P1-P2/关闭返回浏览器样本形成双重验证 | 通过 | FUI-14 可归档；完整装备基础数值表继续公开，不从制作页越界外推法宝或端到端完成 |
| 2026-07-22 | `TASK-SLICE-139` 法宝页面实现 | 适用 | 实现前重读 FUI-09、`magic-weapons-index.md`、`SutraInterface.as`、`AllEquipment.initSutraEquipment/initRondomPro` 与 restored character 596；六段矩阵记录入口、共享生命周期、几何、材料分支、重置、保存和验证，并将实际“五行全集”与函数名暗示的随机意图区分 | 通过 | FUI-09 可归档；浏览器重载受策略限制已公开，端到端主循环与战斗真法宝资源不得据此宣告闭合 |
| 2026-07-23 | `TASK-SETTINGS-060` / `TASK-SLICE-141` 任务生成 | 适用 | 用户反馈与既有“现代工坊导航可接受”设计选择冲突后，没有直接改代码；先生成独立逆向 task，强制追踪 character 119/四子页的时间轴、注册点、矩阵、命中区、按钮帧和切页调用链，再由独立实现 Goal 执行双重验证 | 通过 | 这是任务定义样本，不计入逆向闭合；影响实现的未知清零前不得启动原生按钮整改或宣告真 UI 已复现 |
| 2026-07-23 | `TASK-SETTINGS-060` 原生按钮逆向闭合 | 适用 | `equipment-workshop-index.md` 从 119/四 DefineButton2 局部对象追到 `GMain/StrengthEquipment` 共享调用链，记录 PlaceObject2 注册点、四态内部矩阵、透明 hit bounds、940×590 裁切、枚举映射、扁平容器反证和自动+运行验证计划；SWF dump 与选择性 SVG/PNG 交叉确认 | 通过 | 影响 `TASK-SLICE-141` 的未知为零；实现前须重读矩阵，并以确定性坐标测试与 940×590 hover/pressed/selected 观察补齐双重验证 |
| 2026-07-23 | `TASK-SLICE-141` 原生按钮实现 | 适用 | 实现前重读 `equipment-workshop-index.md` 六段矩阵；仅使用已证实的 119 根坐标、95/99/109/113 up/over/down、透明 hit bounds、标签顺序和 owner 回强化合同，没有自行补视觉常量；几何专项与 940×590 P1/P2 hover/selected/切页/关闭观察补齐双重验证 | 通过 | 该导航合同可标记已复现；端到端主循环仍由 `TASK-SLICE-140` 独立验收 |
