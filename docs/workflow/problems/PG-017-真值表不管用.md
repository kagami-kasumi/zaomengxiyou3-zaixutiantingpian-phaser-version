# PG-017 真值表不管用

2026-09-25 / 226运动反例修正边界：shared ground helper改每次Sprite赋值截断后，25,920独立原生态无需后处理全部匹配，并注册严格门禁（拒绝诊断开关）。三个collider另起AIR独立采集完整9节点/PNG基准，Schema+原运动样本一致，正式几何JSON已生成；这只关闭局部计算与几何差异，不证明猴马ground消费者、动作/AI或全Scene。扩大联合门禁首次因旧dragon1 fixture缺readyRoster失败，按当前真实闭包适配重跑，失败记录保留。PG继续复盘中，原84尚未全核销。

2026-09-25 / 226旧碰撞外形与公共运动复用反证：原restored StageCommon实测Sprite3=31.1×30、Sprite4=35×70、Sprite=50×100，207/209旧SVG派生profile部分少0.05；不得因为verified标签便跳过源运行外形复核。原BaseObject/Pet运动25,920态与shared helper比较，1,224个猴3/4顶头后y不符（-9.899999…对Flash -9.85）；末坐标twip诊断25,920一致但尚无中间赋值及正式接线验收。现代两族此前没有groundMovement入口，旧helper测试不证明家族在地面运行。方案不足保持复盘中，226继续，未新增抢占项。

2026-09-25 / 226真实受击与移动反例：原门禁通过后，96个实际Runtime诊断仍复现QLFJ必成功roll进入hurt、根距离640错误follow、950提前warp及有目标1100漏warp。QLFJ补原reduceHp/PetInfo576 AIR分支与现代1152/72方向样本（42次无目标真实body发射），已接P1R/P1H；移动尚待原几何/运动动态和修复。旧helper/coverage名称不证明真实Session路由，诊断exit0仅代表采集完成。PG继续复盘中，无新抢占项，不关闭。

2026-09-25 / 双owner fixture边界反例：24例给pet.id加slot前缀可以证明唯一ID下生命周期，却覆盖不到真实即时捕获。沿catchNewPet→实时roster通知→Session.release补诊断，P1/P2实际都生成pet-monkey1-2，两真实Runtime共2弹体，释放P1后剩0且P2仍活。证据capture-owner-preflight.json，诊断退出0不是通过；共享修正列Planned234。初始seed/存档decode的P2前缀不能冒充所有写入入口唯一性保证，方案不足反馈保留，PG不关闭。

2026-09-25 / 226生命周期语义反例：旧coverage列名与绿色门禁未执行全部death/destroy trace。新增正式party函数体+两族生产Runtime/port的24例同时P1/P2，首跑在寿命100!=99失败；BasePet.as887..905及207 verified死亡合同要求单机首次dead扣一次。两族接入既有钩子后24例通过，包括其他slot弹体继续、替换旧事件拒绝与重复死亡不多扣；完整联合待终态。此为覆盖方案不足的实际反馈，非任务已完成，PG仍复盘中。

2026-09-25 / 226实际GPU反例：原PNG/顶点与联合门禁绿色后，850态WebGL实际画布仍有302态失败，集中马八种旧资源。原生909状态/3447显示对象/94栅格投影进入消费后，850态零alpha/预乘RGB残差；build、P1R/P1H、完整system-tests=0。Canvas654态另反证共享roundPixels导致宽高各+0.5；无取整fixture850态通过，生产未修，Planned233承接。旧coverage元数据不能证明hurt/death/destroy真实执行，已获只读审计反馈，226继续补验；PG复盘中，不归档。

2026-09-25 / 226暂停新生显示组合反例：猴暂停批P1R/P1H通过后，原hit5Hit与真实TweenMax三fps的60回调均在world暂停中执行；现代emitter已创建爆炸，但shared view只在Scene.update建图，0个图像对原attached=true失败（`.tmp/pet226-horse-paused-birth-before.log`）。Game poststep补同步出生后，又发现timer先于display更新导致源1、1、2、3被现代1、2、3、4替代；真实bridge函数消费现已修正顺序。60例/2388状态与19项马变异、build及完整P1R/P1H通过（`.tmp/pet226-horse-paused-birth-design.log`）。仍非全Scene/GPU或源释放后完整清理证明，PG保持复盘中，无新抢占项。

2026-09-25 / 226注册点翻转反例：原PNG、origin与flip标志的替身测试通过，仍不能证明实际渲染位置。直接执行已安装Phaser的MultiPipeline.batchSprite与TransformMatrix后，马sp方向镜像的四边形相对原Flash注册点偏移163坐标单位（`.tmp/pet226-horse-origin-before-quad.log`）；Phaser flipX绕纹理中心，原版负scale绕注册点。正式共享view改负scale并保留实际Phaser顶点断言，9216原EXIT态及相关暂停回归通过；新增恢复中心flip的生产变异，完整联合门禁待终态。该检查覆盖CPU渲染坐标，不冒充GPU像素或全Scene验收；方案不充分反馈保留，PG继续复盘中、226未完成，无新抢占项。

2026-09-25 / 226普通暂停组合反例：AoyiBuff局部1536对照和完整P1R/P1H通过后，13种马私有弹体原生world暂停探针补39936 enter态；实际private owner+display port诊断156自然例一致、156暂停例中144个清理不一致（马3 bz原世界tick62清理、现代tick81）。证据`docs/tasks/evidence/TASK-SLICE-226/horse-world-pause-preflight.json`，退出0仅采集成功。恢复时的自然显示相位、碰撞相位、逻辑TTL须分别对账，不能外推前置效果修复到全族；保持复盘中和226未完成，不新增抢占项。

2026-09-25 / 226身体与效果组合反例：冻结计数35例和P1R/P1H通过，但实际Monster30模型先火焰死亡、Stage11视觉随后选dead，导致已准备hit1回调丢失，死亡动作提前1步。20/24/30 fps诊断见`tools/pet-target-body-order-preflight.ts`；原BaseObject先body后effects及Monster30帧回调源码构成静态依据，尚未跑该组合原AIR/全Scene。方案不充分，保持复盘中，226内继续集成顺序；不得将多个独立正确模块的绿灯当成组合正确性，不新增治理抢占项。

2026-09-25 / 226真实启动反例：完整系统回归通过后，4174内置浏览器进入禁用存档的`?qaStage=1-1-role1`，实际报`Unknown monkey collision phase PetMonkey1Bullet2/122/-1`。main未配置fps，消费者读取Phaser默认targetFps，自动化只注入20/24/30遗漏真实启动值；不得扩碰撞profile或夹紧到末帧掩盖。226内补启动配置与持久设置测试，并以实际浏览器再次验证；冰效画布尚未触发，不能称视觉验收。PG保持复盘中，不新增抢占项。

2026-09-24 / 226受击消费反例：方案不充分，保持复盘中。实际共享pet hit写出knockback=(6,-5)并扣HP，但下一帧shared monster physics仍只有重力；此前event字段存在不能证明击退手感。复现入口tools/pet-monster-knockback-preflight.ts，诊断退出0不代表行为通过，未声称全Scene或原版轨迹。按既有正式消费者要求补目标位置/速度轨迹与负向变异，源单位/边界/顺序交给同线Planned230，不新增治理抢占项。226原84合同和未完成结论保留；旧技能数值公式也由2784原AS3算术核对揭示差异，新公式目前只接normal，不把helper通过当技能全链闭合。

2026-09-24 / 229源输入补强通过：665态/3190对象、387960碰撞、158912动态检测及43合同verified；四实际动态变异拒绝。独立复核发现孤立clipDepth被当普通shape的真值表达，已将全部clipDepth节点标mask并重生成；API visible与直接绘制明确区分。229归档、226恢复Ready；现代正式消费者及存量关闭条件仍未满足，PG不归档。

2026-09-21 / 228 V2.5空间输入补强：622显示态/1950递归对象、349164独立重建碰撞、46080实际host步、672奥义目标边界与41合同verified。继承漏项、JPEG解码、暂停Follow和source/tween边界在晋升前纠正；失败草稿未算通过。只完成猴源输入，不关闭现代存量整改/PG；229马Ready，226保持Blocked。完整来源/变异/限制见228交接。

2026-09-21 / 227 V2.5输入补强结果：原包AIR直接执行gate/AI/step与碰撞判定前缀，共3,408case被独立expected核对；10类实际编译的源变异全部拒绝且正常trace hash不变。现有Runtime/正式resolver的43例由独立源结果判失败（退出1），不以采集退出0或旧绿色gate覆盖反证。保留84合同/129方法，明确controlled服务sink、布尔碰撞输入与未重验生命周期边界；228/229承担真实空间/动态补证，226仍Blocked。此输入批补强通过，PG整体仍复盘中、未满足存量实现/全面性关闭条件，不归档。证据见 `docs/reverse-engineering/evidence/TASK-SETTINGS-227-monkey-horse-supplement.md`。

2026-09-21 / 226预检 V2.5补强选择：方案不充分，保持复盘中。源BaseBullet→BaseMonster要求实际相交，现代两族resolver却允许trackingTargetId直接命中；真实Runtime/正式resolver四形态×P1/P2的16例均错误扣远距目标HP。猴2/3/4未学习技能各49次选择失败且普攻0，说明旧家族测试只配置正向可施法样本仍可漏掉资格/fallback。证据和适用边界见 `docs/reverse-engineering/pet-monkey-horse-gap-audit.md`；反例生成器为 `tools/pet-226-preflight-probe.ts`（诊断退出0不代表通过）。V2.5选择保留原41/43合同与资源，只补源独立的非命中、资格/状态、相位expected及真实消费者变异；碰撞输入缺失时先补证，不能把删tracked/点碰撞或旧gate绿色当闭合。226Blocked、227唯一Ready；游戏补证和整改在task合同执行，不新增治理抢占项。尚未完成V2.5验证及存量整改，不归档PG。

2026-09-21 V2.4时序反证：方案不充分，保持复盘中。BasePet.as:159-165/354与猴马super调用已证连续timeCount取模，现代PetNormalAttackDecision仍在决策后重置1000ms；旧P1R/P1H及新组件测试只保留了现代实现行为，未检验固定相位。保留已有范围/hit/source verifier；本次选择补强真实调度trace与重置计时变异，不重建整套资源真值或再增通用校验层。游戏整改由224C后的TASK-SLICE-226承接，须先让旧行为被独立expected拒绝；排期不等于整改完成，无新增治理抢占项。

2026-09-14 216B2补强结果：五项环境反证由共享HP结算修复，20数值/动作/来源/owner、4真实hazard保护序列/并行来源、24浏览器真实正式冰火及DEVadapter/已加载火像素、13生产数值/来源/时机变异与全回归通过。历史失败保留，目标受控的浏览器仅证明结算适配而非原版几何或完整场景；现有magic-ring显式保护不泛化全isYourFather。216B恢复Ready，实际pnum producer/正式旅程及全面性/存量合同仍未齐，保持复盘。见 `docs/tasks/evidence/TASK-SLICE-216B2/handoff.md`。

2026-09-14 216B环境存量反证（方案不充分，保持复盘）：冰/火直接reduceHp的int/盾/Role3已在215源链，但现代共享环境入口直接浮点扣HP，实际5项失败见 `docs/tasks/evidence/TASK-SLICE-216B/environment-preflight.md`。原版专用father保护与现代普通hurt时间窗不能无证等同；冰target漏保护、火正式/DEV固定false，身份在桥丢失。子agent初查误判环境能力不存在，经实际调用链纠正。216B2解除结算前置，禁止pnum显示取整掩盖HP不符；不撤销215/A/B1、不新增平行治理队列，全面性/存量/正式消费者仍未闭合。

2026-09-14 216B1补强结果：先前四项结算反证已由共享结算修正；215冻结95+6、Role3 int及源盾溢出顺序由36组入口和216组实际TestScene消费者/Phaser碰撞状态对账，10类生产数值/顺序/owner/caller变异拒绝，全系统/build通过。旧失败JSON保留；浏览器仅控制攻击状态执行真实函数，不充当原版几何、整场景或pnum视觉证明。flat-defense明确保留现代一次扣除，未知不升级成源事实。216B恢复Ready，但实际producer/正式运行及本PG全面性/存量条件仍未齐，保持复盘。见 `docs/tasks/evidence/TASK-SLICE-216B1/handoff.md`。

2026-09-14 216B存量结算反证（方案不充分，保持复盘中）：215已经证实101转嫁95+6，但旧 `system-tests.ts:4125..4131` 仍断言96+6；TestScene两个真实caller在保护/盾前转嫁，实际边界probe显示拒绝/满盾仍扣pet6；Role3已有减伤未按int赋回，101×0.99产生99.99而非99。失败值与来源见 `docs/tasks/evidence/TASK-SLICE-216B/preflight.md`。本次不是215/216A真值或显示的反证，不撤销其verified/显示验收；反证指向存量现代测试无法校验已更新源合同。保留现有V2各版防线，增量补强由216B1以215原始expected、实际共享结算入口/两个caller、101/无敌/满盾/overflow/P1P2及顺序变异完成，禁用手写旧顺序模拟作为修复通过证明。216B恢复前不得以view输出95掩盖实际扣96，亦不得把现有玄龟能力标为无消费者。全面性/存量及正式消费者关闭条件仍未全齐，不归档PG，不新增平行治理队列。

状态：复盘中（V2 行为语义 verifier、208A 猴系整改与 210 马系第二样本已完成；等待视觉全面性和存量关闭合同）。

来源：2026-08-19 用户在宠物战斗 HUD 复验中发现头像位置与内容错误，并指出现有产物虽标记为“真值”，却没有满足真值法要求；用户要求将其单独登记为“真值表不管用问题”，后续在其他对话讨论。

## 1. 问题定义

2026-09-14 V2.3四阶范围补充：方案不充分，保持复盘中。219四效果的有限碰撞输入与授权不能覆盖PetDragonBullet4，213的48帧视觉事实也不能替代源HitTest。证据见 `docs/tasks/evidence/TASK-SLICE-214E/preflight.md` 和可重跑collision-preflight.json；原有213/218/219结论按其范围保留。220仅补一个trigger的源显示/碰撞oracle与独立精度合同，214E仍Blocked，完整家族/P1G和本PG长期存量关闭条件均未满足。执行步骤只在220定义中维护。

2026-09-14 220补强结果：本对象五定义、mask/bitmap、96状态与480相位编码独立核对；源场不读取oracle，11520原版例/78373320像素零差异，7采样及10产物变异拒绝。四权威产物再生成一致，214E恢复Ready，原始预检缺口解除。这里只通过单trigger输入，完整现代家族/P1G、通用全面性与存量合同仍未关闭，保持复盘中；未扩大219近似授权。精确来源与数据流见220/handoff.md。

当前机器真值流程可以产出 Schema 合法、带源哈希、可重复生成、`status: verified`、`unresolved: []` 且测试通过的 JSON，但这些形式条件仍可能无法约束实现正确性。生成器、manifest、基准和消费者测试如果共享同一套错误假设，就会形成自洽闭环：表是稳定的，测试也是绿的，正式画面仍然错。

“真值表不管用”不是指 JSON 文件没有被读取，而是它未成为独立于实现的有效判据，典型表现包括：

- expected 集合由生成器自身硬编码，缺失对象不会被发现；
- 动态 MovieClip 只记录容器联合 bounds，没有执行目标帧或解析帧内子对象；
- `verified`、`displayListMatched`、`stateSetMatched`、`unresolved` 由生成器直接声明，而不是校验结果推导；
- 多状态基准复用同一张静态图，未证明原版真的进入对应状态；
- 实现与测试共同读取同一份错误 manifest，测试只能证明“一致”，不能证明“正确”；
- 源哈希与可重复生成被误当成语义正确性。

本问题与已归档 PG-005 不同：PG-005 治理的是六段证据链缺环却宣布闭合；PG-017 治理的是证据和真值产物形式上齐全、甚至被直接消费，但验证结构仍是自证循环，无法发现语义错误。与 PG-007 的关系也相同：原生化门禁存在，不代表真值本身有效。

## 2. 证据

宠物战斗 HUD 是首个明确反例：

- `tools/generate-pet-combat-hud-ground-truth.mjs` 硬编码状态、显示列表与坐标，只把 character 657 记录为 `headmc` 容器，并把约 `104.8 × 93.6` 的时间轴联合 bounds 当作头像显示尺寸。
- 原版 `ShowPetInfo.show()` 对 `headmc` 调用 `gotoAndStop(PetInfo.getPetChinaName())`；character 657 是 42 帧动态头像选择器。以“灵猴”为例，目标帧放置的是 character 619，其实际可见图形和注册偏移并不等于 657 的全时间轴联合 bounds。
- 现代 `Stage1PetCombatHudView.ts` 取得宠物身体 atlas 后，把它强制拉伸到真值表给出的 `104.8 × 93.6`，因此画面出现错误宠物图和错误定位；用户截图直接反证了“verified”结论。
- 生成器直接写入 `status: 'verified'`、`displayListMatched: true`、`stateSetMatched: true` 和 `unresolved: []`，这些字段不是独立校验的产物。
- `validate-ui-ground-truth.mjs` 主要验证 manifest 是否符合 Schema；Schema 合法不能发现漏掉动态帧 child 或选错 character。
- 现有 HUD 测试检查 truth id、PNG/资源存在和禁止现代 Graphics 等结构条件，没有用独立原版帧事实断言“灵猴必须是 character 619、具有对应 bounds/偏移”。
- 逐状态原版基准没有证明动态 `gotoAndStop` 的目标头像被实际执行；静态壳体截图与同源硬编码坐标相互印证，构成自我验证。

直接原因不是“实现没有读取真值”，而是生成阶段使用了过粗的提取单位，把逐状态动态显示语义压成静态容器几何；同时，生成产物是否全面尚无独立结论。

2026-08-26 猴系正式复现形成第二个反例，而且反例发生在行为合同而不是视觉提取：

- 207 的 manifest 已正确记录四形态 `attackRange`，原版 `BasePet.myIntelligence` 也明确在攻击距离外追击目标；证据链并不缺环。
- 现代 `PetCombatRuntime` 只在 1200 范围内选目标，移动仍只跟随 owner；`MonkeyPetBehavior.basicAttack` 没有攻击距离门，静止 projectile 的 `trackingTargetId` 也没有被消费，因此宠物可以站在远处播放攻击效果而不追击、不中怪。
- 家族测试只验证 41 个 contract id 被导入，并把敌人直接放在 projectile 坐标；它没有把 `attackRange`、追击和伤害来源转成可执行断言。生成器变异也只覆盖 monkey4 的 `lj` 冷却，没有杀死攻击距离语义。
- `pet P1R` 只检查源码结构/正则和这些同源测试是否通过；运行审计只观察“特效可见、怪物 HP 下降”，没有记录目标初始距离、追击过程、projectile 命中和 pet-source damage 的同一条来源隔离 trace。
- 因此 `npm run test:pet-monkey-family` 与 `npm run check:system-design -- pet P1R` 都能在玩家反证仍成立时返回 0。此前“猴系完整正式复现、P1R=0”的现行结论无效，历史执行记录保留但由本反证覆盖。

这不是 PG-005 的证据链缺失，而是 PG-017 的同源 oracle 自证循环扩展到了“行为合同 → 现代运行时语义”。

## 3. 解决方案

PG-017 只保存问题治理合同，不再承载逆向任务的详细拆解步骤：

- **生成逻辑治理方向**：真值生成只属于独立逆向任务下的 `视觉真值逆向` 子类型。任务模型和渐进读取见 `docs/workflow/reverse-engineering-task-protocol.md`；精细生成拆解作为独立输入方案保存在 `docs/reverse-engineering/plans/ground-truth-fine-grained-generation.md`。代码逆向沿用既有流程，不读取该方案或新增专用文档约束。
- **生成职责**：视觉真值逆向 task 负责声明范围内的 SWF/AS3 状态解析、目标帧/时间片、递归 child、动态显示变化、几何和产物生成；漏做应有提取属于视觉真值生成缺陷。
- **校验器职责**：全面性校验器只判断最终生成物是否全面，不监督生成器内部提取步骤；其边界和待决问题位于 `docs/workflow/ground-truth-completeness-validator.md`，方案留到独立对话讨论。
- **V2 双 verifier 边界**：视觉全面性校验器继续只裁决视觉产物是否全面；新增的行为语义 verifier 只裁决已冻结行为合同是否在黑盒运行时成立。两者不得合并为一个泛化“测试通过”结论，详细合同见 `docs/workflow/behavior-contract-runtime-verifier.md`。
- **独立性定义**：独立性是数据流隔离，不是换一个 AI 当裁判。expected 必须在实现前由原版证据/manifest 冻结；actual 必须来自不调用实现内部 helper 的受控运行 trace；每个 contract id 必须映射到语义字段、场景与可观察事件。
- **结果用语**：当前不另设 `correct/incorrect` 结果维度；产物在声明范围内被证明全面时，即把全面性证明作为真值成立结论。
- **调度边界**：2026-08-21 用户要求在原 193E 前实施该实例；已创建并激活 `TASK-SETTINGS-201`，其后串行 `TASK-SLICE-202` 正式消费。201 不修现代 HUD，202 不反向修改真值生成逻辑；两者都不批量降级既有 manifest，也不进入全面性校验器通用方案。
- **首个实例**：`TASK-SETTINGS-201` 以 character 657 为有界 `视觉真值逆向` task，在 `逆向方案：` 字段链接精细生成方案；只处理 `gotoAndStop` 的 35 个实际物种/形态 fixture 和 P1/P2 投影。`gotoAndPlay` 样本仍须另建后续视觉真值逆向 task。
- **存量审计**：最终按风险审计既有 `verified` manifest，优先动态时间轴、帧内嵌套、滤镜/mask、文本度量和多状态 UI，不因一个反例无差别否定所有静态真值。

## 4. 测试方案

- 执行精细生成方案的视觉真值逆向 task 必须通过该方案自己的验收；PG 只核对 task、方案、产物和验收记录是否形成可定位链路，不复制一份步骤清单。
- character 657 首个 task 完成后，核对具体宠物状态不再使用根容器联合 bounds 或身体 atlas 代替头像事实。
- 全面性校验器的输入、判定方法、结论枚举和专项测试留到独立对话冻结；PG 只核对它是否保持“判断产物全面”的职责。
- 生成与全面性校验均落地后，核对正式消费者：移除或篡改关键真值字段时运行测试必须失败；仅 import truth id 不算消费证明。
- 行为合同必须具有字段级覆盖矩阵、前置条件反例、正向运行链和关键字段 mutation-kill。以猴系为首个反例：目标初始位于 `attackRange` 外时不得先创建攻击 projectile，宠物必须追击；进入范围后才允许攻击，并由 source-isolated trace 证明 pet attack id 导致目标 HP 下降。
- P1/P2、TestScene 与正式关卡都必须消费同一运行合同；“另一个 AI 写测试”、同实现 helper 计算 expected、把敌人生成在 projectile 坐标或只观察最终 HP 下降，均不构成独立验证。

## 5. 测试结果

当前已确认两个“形式验证通过、玩家结果错误”的反例。视觉侧的 character 657 已完成精细生成与正式消费，但通用全面性校验器仍未落地；行为侧的猴系 207 manifest 本身包含正确 `attackRange`，208 实现/测试/P1R 却没有消费其运行语义，玩家仍看到虚空攻击。PG-017 因此由“待治理”转为“复盘中”：此前方案只覆盖视觉产物全面性，未覆盖行为合同到运行时实际语义，方案不充分。2026-08-27 V2 verifier 已落地并让当前错误实现的 P1R 正确返回 1；下一步由 `TASK-SLICE-208A` 修玩法，语义 P1R=0 前不恢复第二家族验证。

## 6. 关闭标准

- 独立逆向任务的视觉真值子类型和精细生成方案被实际 task 消费，代码逆向保持原流程；真值流程明确区分可重复性、Schema 合法性、生成结果与全面性结论，`verified` 不再由生成器无条件自报。
- 宠物 HUD 的动态头像真值按实际状态帧递归生成，正式画面与原版状态一致，不再使用容器联合 bounds 或身体 atlas 代替头像状态。
- 独立对话完成全面性校验器方案并落地；校验器只裁决产物是否全面，不监督生成器内部提取步骤，也不另设 `correct/incorrect` 结果维度。
- 全面性校验器能发现动态 child 缺失、状态缺失、逐帧信息缺失和多状态静态基准复用，不再只验证 JSON 形状与文件存在。
- 至少两个后续真值任务完成“精细生成 + 全面性结论 + 正式消费者 + 运行差异”，其中至少一个包含动态时间轴。
- 对存量高风险 `verified` manifest 完成有界审计并记录保留、降级或修复结论。
- 用户或 reviewer 再次以正式画面反证真值时，流程能自动降级状态并阻止任务继续宣称闭合。
- 行为合同 verifier 能证明关键字段被运行时语义消费：所有合同 id 有字段级断言；距离外不攻击/追击、范围内攻击/命中、pet-source damage 与清理形成结构化 trace；关键字段变异可被杀死。
- 猴系反例修复后，P1/P2、TestScene、正式关卡的受控场景全部通过；旧 P1R 的结构/正则 gate 不再能单独宣布玩家可见闭合。

以下不算关闭：

- 只修宠物头像坐标或换一张正确图片。
- 只给 manifest 增加更多字段、哈希或截图，但动态状态仍以容器或联合 bounds 为提取单位。
- 只建立检查生成器内部步骤是否被调用的流程监督器，却没有判断最终产物是否全面。
- 只让实现读取更多真值字段，没有全面性结论。
- 只让测试 import 合同 id、复用实现 helper 计算 expected，或由同一个生成过程同时产出 expected 和 actual。
- 只增加另一个 AI、另一份测试文件或更多截图，但没有 expected/actual 数据流隔离、字段级语义映射和 mutation-kill。
- 因本次反例把所有旧真值一律判废，却没有按风险和语义逐项审计。

## 7. 适用触发与反馈记录

触发条件：

- 新增或修改 ground-truth generator、Schema、validator、baseline 生成器或正式 truth consumer。
- 目标含动态 MovieClip、`gotoAndStop`、嵌套时间轴、mask/filter、动态文本或多状态显示列表。
- manifest 标记 `verified/unresolved=[]`，但用户截图、原版运行或独立提取结果出现反证。
- 测试只检查 JSON 字面量、truth id、哈希、PNG 存在、Schema 合法或“实现与 manifest 一致”。
- 行为 manifest 已有攻击距离、目标、命中、伤害来源或生命周期字段，但测试只核对 id/存在性，或通过把目标摆到攻击效果位置来制造命中。

效果检查：

- 检查生成单位是否为声明状态下的目标帧/时间片和递归可见显示列表，而不是根容器或全时间轴联合 bounds。
- 对动态显示对象核对状态来源、真实目标帧、递归 child、矩阵、注册点、可见 bounds、时间语义和状态 fixture。
- 检查不支持的 SWF/AS3 语义是否显式进入 `unresolved`，以及 baseline 是否真正对应声明状态。
- 校验器相关变更只检查它是否判断产物全面；若转而监督生成器内部步骤、抽样范围小于产物声明范围却宣称全面，或重新引入单独的 `correct/incorrect` 结果维度，即判职责漂移。
- 行为 verifier 变更检查 expected 是否先于实现冻结、actual 是否来自黑盒事件/trace、每个合同字段是否有正反场景，以及关键字段变异是否真正导致失败；若只验证源码字符串、测试文件存在或最终 HP 下降，即判自证循环复发。

| 日期 | 任务/变更 | 适用性 | 证据 | 结论 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-27 | PG-017 V2 行为语义 verifier 治理项 | 适用 | Trace Schema；207 的 41 id 字段覆盖矩阵；四形态 × P1/P2 黑盒范围链；`attackRange`/命中帧/source owner mutation-kill；旧专项绿时真实 verifier 与 `pet P1R` 均为 1，逐条报告 `EARLY_ATTACK/NO_CHASE/NO_IN_RANGE` | V2 脚手架通过：自证循环已可被自动反证；PG 不关闭，当前失败属于被正确捕获的存量玩法缺口，视觉全面性关闭合同也仍未完成 | 移出治理执行队列，生成唯一 Ready `TASK-SLICE-208A`；整改达到语义 P1R=0、修订 Skill 后才恢复 209 |
| 2026-08-27 | `TASK-SLICE-208A` 猴系行为语义整改 | 适用 | 生产 Runtime 直接消费 207 frozen range；八条 P1/P2 trace 证明范围外无攻击且距离下降、入围后 action/projectile token、verified hit、pet-source HP decrease 与 hit cleanup 同链；三类 mutation-kill、`pet P1R=0`、五关旅程与全系统通过 | 行为侧首个反例整改通过，旧结构/正向 fixture 不再能单独宣布闭合；PG 不关闭，视觉全面性校验器、后续样本与存量风险审计仍缺 | 修订 `$pet-family-reverse` 并恢复 209 作为第二家族验证；后续若 range/hit/source 字段只被 import 而无负场景或 mutation-kill，按本 PG 复发处理 |
| 2026-08-26 | `TASK-SLICE-208` 猴系虚空攻击用户反证 | 适用 | 207 manifest/原版 BasePet 已有 `attackRange` 与追击事实；现代 Runtime 只跟 owner，普攻无距离门，tracking projectile 不追踪；专项把敌人放在 projectile 坐标且只核对合同 id；`test:pet-monkey-family` 与 `pet P1R` 仍返回 0 | 方案不充分，PG 转为复盘中；此前 208“完整案例/P1R=0”现行结论无效，P1R 降为 1。根因属于行为 oracle 自证，不重开 PG-005 | 执行队列抢占 209；先落地行为合同运行时语义 verifier，再生成猴系整改 task。MO-003 转修订中，Skill 暂停作为 209 执行路由 |
| 2026-08-24 | `TASK-SLICE-202` character 657 首个正式消费者 | 适用 | `PetCombatHudHeadAssets` 直接 manifest 消费、33 child/35 fixture 专属 bundle、frame/child/matrix/registration/visible-bounds 变异门禁、九物种零像素差、P1/P2 五关旅程与 940×590 零 console | 通过正式消费阶段：正式画面不再使用身体 atlas、联合 bounds 或硬编码头像定位，201 的精细事实成为可变异 oracle；191 静态范围有界保留。PG 不关闭：通用全面性校验器、两个后续完整样本和存量风险审计仍未完成 | 唯一 Ready 切换为 `TASK-ARCH-203`；PG 保持待治理，后续视觉真值样本继续按关闭合同累计 |
| 2026-08-24 | `TASK-SETTINGS-201` character 657 首个精细生成实例 | 适用 | `task-settings-201.pet-combat-hud-head`、独立 completeness report、35 个逐帧 baseline、灵猴联合画布差异叠图与 child/frame/matrix 变异自测 | 通过首阶段：生成单位已从全时间轴联合 bounds 改为状态 fixture × 目标帧 × 递归显示列表；expected/extracted 来源分离，`verified/unresolved=[]` 有全面性证据。PG 不关闭：正式消费者、通用校验器、两个后续样本与存量审计未完成 | 激活唯一 Ready `TASK-SLICE-202`，只实践到正式 HUD；PG 保持待治理，不把 201 单样本外推到所有 manifest |
| 2026-08-21 | 在 193E 前插入宠物真值/UI 实践任务 | 适用 | 用户明确要求重做宠物真值并实践到 UI；现有精细方案首个建议实例和 PG 反证均指向 character 657 | 将第一项按强制边界拆为 201 视觉真值逆向与 202 正式消费者；不把实现塞进逆向 task，不批量否定 191 静态范围 | 先执行唯一 Ready 201；完成并达到 verified/unresolved=[] 后才激活 202，全面性校验器仍在独立对话讨论 |
| 2026-08-20 | 将真值方案收窄为视觉逆向子类型 | 适用 | 用户确认机器真值只用于视觉逆向；代码逆向既有流程表现良好，不应被迫读取真值方案或增加文档约束 | 逆向任务分为代码逆向与视觉真值逆向；方案只向后者披露 | 同步任务模型、读取路由和门禁；本轮不迁移旧代码逆向 task |
| 2026-08-20 | 抽离逆向任务模型与精细生成方案 | 适用 | 用户指出详细拆解挤在 PG 中无法按任务读取，要求把逆向建模为独立任务、把拆解方案作为独立输入喂给任务 | PG 回归问题治理职责；生成方案改由 task 显式读取 | 建立逆向任务协议和独立生成方案；本轮不创建执行 task |
| 2026-08-20 | 冻结生成逻辑与全面性校验器职责 | 适用 | 用户确认问题包含“生成粒度过粗”和“产物缺少全面性判断”两部分，并裁决校验器不监督生成器内部提取操作，只判断最终产物是否全面 | 生成方案已冻结；PG 仍待治理 | 后续先按精细生成合同实施 character 657 切片；另开对话讨论全面性校验器 |
| 2026-08-19 | 宠物战斗 HUD 复验 | 适用 | character 657 动态帧语义被压平成联合 bounds；现代身体 atlas 被拉伸为头像；生成器自报 verified，结构测试仍通过，用户截图直接反证 | 确认“真值存在但不构成有效 oracle” | 登记 PG-017；后续独立对话讨论真值有效性合同与首个修复切片，本轮不改真值或实现 |

## 2026-09-05：青龙消费反证与 V2 补强

- 结论：方案不充分，保持复盘中；V2 既有范围/段数/MP/source 防线保留，但没有覆盖完整本体时钟及 baseline 的真实调用参数语义。
- 证据：`docs/tasks/evidence/TASK-SLICE-214A/truth-hold.json`；原 `test:pet-dragon-family-truth=0` 同时遗漏四本体非攻击持帧、六张 trigger 基准相对 doHit5 的 40px 偏移。
- 即时处置：213 manifest 当前 blocked；generator 在未解决 hold 时拒绝生成，handoff 拒绝非 verified。原生成与 handoff 各自实测退出 1，不再对该产物发出假绿。
- 补强方案：同线 TASK-SETTINGS-213A 只修该族两项证据缺口，增加全部动作时序和独立 source→fixture 几何负向断言；expected 必须来自完整原声明/AS3 结果，不用现代已消费范围缩减。
- 测试/关闭：213A 必须杀死缺 wait/walk/hurt/dead/hold 和 trigger y+40 变异，修正原版逐状态基准并恢复 handoff=0；214A/214B 后续提供现代消费与正式差异。此次不宣布 PG 或青龙家族关闭，不从 PG 执行逆向，执行合同在 task definition。

2026-09-05 213A 补强验证：完整bodyTimelines、剩余计数与elapsed区分、234源像素对账和六trigger根坐标已闭合，15类产物变异（含图片下移40px）全被拒绝。生成前独立candidate验证和handoff强制验证取代临时hold拒绝，truth-hold resolved。仅本族证据整改通过；现代正式消费者与存量全面性关闭合同仍未齐，PG保持复盘中。

## 2026-09-05：214C2 公共移动反证，V2.1待裁决补强

- 结论：方案不充分，保持复盘中。既有范围/来源/时钟产物门禁保留，但不能覆盖公共移动单位和环境积分；不撤销213视觉产物verified。
- 原假设与失败类型：把“复用公共移动且范围外追击成功”视作足以承接青龙；源与独立grounded一步探针显示24fps的18.75px与5px不符，属现代消费者语义及验证盲区。证据：`docs/tasks/evidence/TASK-SLICE-214C2/preflight-decision.md`、`movement-preflight.json`；探针退出1。
- 保留项：唯一Runtime/EntitySession/Registry、C1来源及生命周期防线、213/214A原版时序资源。废弃项：以公共helper已有测试绿灯推断其原版移动正确；不得复制dragon私有移动避开owner。
- 候选：继续局部补强速度常量成本低，但遗漏垂直/地面/墙、hosttick和search分支，不能关闭；保留设计角色并扩展公共Frame/环境移动端口成本更高，能让正式场景提供真实环境并统一验证。推荐后者，待用户裁决冻结公共接口扩展范围，不在PG里执行逆向。
- 新验证合同：独立源条件下逐tick地面位移、空中/落地/墙、每秒尝试/search边界、P1/P2原身/clone；实际正式trace与负向mutation必须能拒绝速度×90/二维追踪/无环境近似。原P1GS成功样本不替代新合同；当前未关闭PG或214C2。

2026-09-05 后续裁决更正：用户已允许修正。此前把触及公共Frame/移动接口本身视作必须批准的公共重设计，是agent对自拟任务限制的过度解释；该等待条件撤销。保持既有设计角色，在公共移动owner内修复速度换算及必要实现接缝，继续214C2完整合同。原失败探针事实保留，尚无修复通过结论。

2026-09-05 V2.1增量验证：公共速度换算已修复，历史失败/当前matched报告分离；新增运行时钟接缝测试覆盖同row独立keyFrameIndex，子代理发现遗漏并修正。一步速度及P1GS通过，只关闭此速度反证；环境物理、完整dragon1和正式视觉验证仍未齐，PG保持复盘中。

## 2026-09-05：构造结束值反证，V2.2 有界补强

- 结论：方案不充分，保持复盘中。既有 verifier 与消费者同用字段初值 0.8，未覆盖 BasePet 构造中的后续覆写；先前真值测试为绿不能证明最终概率正确。
- 证据：214C2/attack-rate-bytecode.json，恢复主包 BasePet 九次 0.8 后五次 0.7，BaseObject/dragon1..4 无直接覆盖。范围仅限此常量构造路径，不外推其他字段/家族。
- 方案 V2.2：生成器独立读取恢复主包 pcode 的顺序字面量赋值，拒绝跳转/分支和非字面量，核对本链直接覆盖；将四形态真值修正为 0.7，并增加旧 0.8 负向变异。
- 测试结果：test:pet-dragon-family-truth 与 test:pet-dragon-assets 为 0，345 基准与现有 44 合同保留；只是本反证整改，正式概率消费 trace、完整战斗和既有视觉全面性关闭条件仍未满足。后续由 214C3/214C4/214C5 继续，不从 PG 另起逆向执行。

## 2026-09-06：目标碰撞消费缺口，V2.3 有界补强

- 结论：方案不充分，保持复盘中。213的逐帧可见bounds/宠物collisionProfiles不能证明目标colipse与普通弹的复杂绘制交集；不得把“verified资源”升级为“精确命中输入齐全”。保留既有视觉、时钟和宠物几何结论，只降级C4目标碰撞就绪结论，不批量判废存量真值。
- 证据：`docs/tasks/evidence/TASK-SLICE-214C4/preflight.md`及`collision-preflight.json`。BaseMonster/HitTest要求像素绘制交集，首帧现代无损PNG仍有1598个透明像素；目标3种Sprite/12映射尚无本批可消费的空间真值。另发现成功命中后刷新伤害缓存，不能每次碰撞前重算当前伤害。
- 方案V2.3：由独立TASK-SETTINGS-218冻结三个目标Sprite/12映射、Flash采样语义及透明空隙/边缘/翻转/末帧/缓存刷新正负fixture；C4恢复后以真实source-isolated trace消费，禁止AABB或同源expected自证。执行步骤属于218合同，不从本PG恢复。
- 测试结果：预检脚本退出0，只复现源条件与11张既有RGBA哈希/alpha事实，不是战斗或全面性verifier。C4已Blocked，218唯一Ready；未改src/资源，未运行P1GC，不满足本PG关闭条件。

## 2026-09-13：V2.3 二三阶适用范围补充

- 结论：方案不充分，保持复盘中。218的有限原版采样已支持初阶交付，但没有覆盖214D四个效果；213视觉根bounds和214A逐帧PNG不能直接证明其HitTest输入齐全。保留V2.3原初阶结果，不把受限采样误标为全家族oracle。
- 证据：`docs/tasks/evidence/TASK-SLICE-214D/preflight.md`、`collision-preflight.json`；四对象76帧的尺寸/crop会变化，现有首帧mask与固定尺寸采样不能外推。子agent独立核对AS3无显式旋转，但没有据此推定嵌套显示/采样等价。
- 有界补强：219冻结四效果的递归显示/逐帧alpha映射及原版HitTest正负oracle，复用218目标输入；214D待其通过后消费并运行P1GD。步骤只在219定义中维护，不增加第二套治理执行队列。
- 219进行中原版反证：源字节与76帧显示树已独立核对，但4,864例候选相位场仍有4处单像素差异；原版对同一旋转图形整数平移再裁回也可相差1像素，故不能将固定相位场当作精确通用采样。当前验证器退出1，未提升verified，未解除214D；见`docs/tasks/evidence/TASK-SETTINGS-219/progress.md`。这属于既定补强尚未充分，不重开已通过的218初阶样本。
- 219最终裁决：用户明确批准碰撞近似；76帧源事实与原版基准verified，采样6,448例命中一致/104像素偏差及8变异独立列示，strict继续失败，原版未建模精度不冒充已证事实。有限输入补强完成、214D恢复Ready；PG长期/存量合同与全家族验收仍未关闭。详见219 handoff与approved-approximation。
- 当前结果：214D已消费219批准近似，二三阶真实战斗与P1GD通过；全家族和长期存量关闭合同仍缺，不归档本PG。

## 2026-09-13：214D源插入顺序反证与本批补强

- 方案仍不覆盖全部历史视觉语义：213左向九对象基准把doHit4世界偏移当成局部偏移进行镜像，集合位置相同但对象插入顺序改变重叠像素。主agent在生产174态逐像素比较中定位，未改生产美术来迎合错误基准。
- 修正唯一左向组合基准及214A对应资产查询投影，345状态总数不变；补入wave-insertion-order负向变异，174态/5视觉变异、9实现变异与6448原版碰撞消费均通过。source-contracts、runtime-traces、visual-diff、正式/TestScene双人证据位于214D。
- V2.3本批补强通过，但方案全面性/全家族及存量关闭仍不足，保持复盘中；不从PG创建第二执行队列，也不将批准近似标作精确AIR事实。

## 2026-09-14：214E负向测试污染正常trace，V2.4有界补强

- 问题与反证：变异runner将多个测试合并执行；前一个runtime测试可能通过并写入正常报告，后一个collision断言才拒绝变异。gate退出0只证明变异被拒绝，不能保证落盘trace来自正常实现。`214E/mutation-write-repro.json`复现first-mask退出1且覆盖原baseline，记录覆盖前后hash；复现后立即恢复原报告。
- 结论：既有报告写入隔离方案不充分，PG保持复盘中；不据此宣称生产二三阶伤害变化。历史D trace重新生成，生产代码不为迎合历史报告修改。
- 方案V2.4：三个青龙变异runner对子进程设置专用标记；测试报告writer在负向进程禁止写入正常证据。runner执行前后逐字节断言正常runtime/family报告未变，失败即gate失败；变异结果仍由父进程单独写入mutation-results。正常生产测试独立生成trace，44合同audit绑定这些正常报告hash。
- 测试与关闭合同：P1G重跑三组14/9/20变异并验证正常报告字节不变，随后完整系统重新生成正常报告、44合同audit及真值/资源门禁通过才关闭此有界缺口。不得将“先写坏、最后补跑”作为持久防线。
- 整体归档评估：本次青龙家族合同闭合不等于通用视觉全面性及历史存量清零，PG长期关闭条件仍不足，不归档；不创建第二执行队列。

V2.4本批实测结果：P1G=0，三组14/9/20变异的正常报告前后字节断言通过；全系统再次生成正常trace，44合同audit=0，真值/资源、structure/workflow及集中审计通过。此报告污染缺口已关闭，PG整体仍因通用全面性/存量条件不足保持复盘中。持久运行记录见214E/validation-results.json与214E-P1G.txt。


2026-09-25 / 226继续反例：源图片hash与HTTP根路径正常仍无法证明嵌套正式入口可加载，8浏览器case内768错误来自相对URL解析为HTML；生产URL修正后相同入口console0。原设计dead冻结CD被原BasePet/wrapper288态反证，192真实Runtime态全部不符后通过两族声明复用Session修正，原失败报告保留。仍为方案不充分/复盘中；死亡地面物理、完整84与Scene未闭合，不以局部回归或规划235作为关闭。

2026-09-26 / 226联动反馈：旧body/target隔离真值即使通过，也未约束原ground启用后的初始朝向、AI随机所有权与身体移动耦合。新1356原AIR组和1500现代组/154704态严格对账生产Session；raw Point与实际Sprite坐标的端点差异显式补证，未降低精度。既有局部fixture迁移至受控原collider地面，保留伤害/owner/清理断言；完整联合尚待，上批OOM不算通过。维持复盘中，不以本样本关闭所有问题。

2026-09-26 / 226最终验收：扩大P1R/P1H/P1G/P1T=0，新增正式目标投影后的P1R/P1H=0；1404原生联动组、1596现代组/165264态、24猴四实际伤害链及36马多目标组通过。原41/43合同逐项承接，230..235公共责任仍open，本地整改归档、230唯一Ready，完整家族/all/204/194/VS-067不关闭。build与默认全系统分段覆盖通过；失败OOM保留。 本项通过不推翻此前方案不充分反馈；正式消费者证据与公共open项保留，PG继续复盘中。


## 2026-09-26：评审所指出的自动校验入口缺口

问题与边界：当前 UI 扫描把行为真值套入 UI Schema；击退 23 条入口/调度记录允许任意对象；玄龟专项未进入默认回归。这是既有验证方案的入口与结构缺口，仍归本 PG 的复盘范围，不新增并行游戏任务，不声称因此解决正式消费者或视觉全面性。

本次方案：使用完整 Draft 2020-12 校验器按显式声明/历史类型表分派，默认只扫描 Git 在册 manifest；补 228/229 交接结构及嵌套 UI 校验；收紧 230 入口/调度/Tween/方向溯源，新增独立原版 mutation+再生成命令；玄龟无本地前置的生命周期测试加入默认系统回归，其余 13 组保留完整设计验收和显式输入说明。复验发现 AS3 字典枚举顺序不稳定，生成器改为排序序列化，不放松数值比较。

验证与关闭标准：路由与错误定位、未知类型拒绝、八类坏字段拒绝、默认扫描范围、14 组玄龟分层登记均须通过；版本化与本地真值分别明确报告覆盖范围，默认系统测试完整执行，源复验必须拒绝五个实际原版变异并通过再生成检查。结果与日志见 [本轮评审整改复核](../../评审/评审-2026-09-26.md#整改复核与取舍)。这里仅关闭本次入口/结构缺口；PG-017 第 6 节的全面性校验器、动态视觉和全部正式消费者条件未齐，维持复盘中。
