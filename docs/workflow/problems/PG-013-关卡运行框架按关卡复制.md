# PG-013 关卡运行框架按关卡复制

状态：方案落地，效果观察中。

来源：2026-07-31 用户从 Stage 1-1 光门/黑框差异继续追问“关卡类化”，指出不应只抽取单个光门，而应治理所有关卡共同职责按关复制，以及关卡承载角色、怪物或视觉内部规则的边界漂移。

## 1. 问题定义

现代实现已经抽出 `LevelLifecycle`、`LevelResultView`、角色移动、战斗、奖励和功能 HUD 等局部公共 owner，但仍没有完整、稳定的可玩关卡运行框架。Stage 1-2、1-3、2-1、2-2 分别复制 Scene 初始化/销毁、队伍恢复、镜头、玩家创建、World/GamePlay 接线、结果路由；Stage 1-1 则继续寄居在大型 `TestScene` 与多组 bridge 中。

同一关卡共同事实因此分散到 `Stage*Scene`、`Stage*WorldBridge`、`Stage*GameplayBridge`、`Stage*FlowSystem` 和资源 manifest。每新增一关仍需人工复制并重新接线基础设施，容易再次遗漏门视觉、失败、结果、保存、HUD 或销毁。反方向上，关卡也可能开始拥有英雄/怪物内部动画、AI、伤害和奖励规则，形成职责膨胀。

不治理会使后续关卡数量与重复 owner、回归路径和修复成本近似线性增长；局部抽一个 `TransferDoorView` 只能缓解一个症状，不能关闭问题。

## 2. 证据

- `Stage12Scene.ts`、`Stage13Scene.ts`、`Stage21Scene.ts`、`Stage22Scene.ts` 重复持有 party/playerCount/world/gameplay/playerViews/resultOverlay，并重复 init/create/update/shutdown 骨架。
- 四个 `Stage*WorldBridge` 分别创建 floor/background/foreground/transferDoor；四个 `Stage*GameplayBridge` 分别控制 `transferDoor.setVisible(flow.doorVisible)` 并把同义玩家输入适配到公共生命周期。
- Stage 1-1 的 `createTransferDoorView()` 直接借用 `Stage13AssetKeys.transferDoor` 并按 character 45 的 bounds 手动缩放；资源 provenance、可复用视觉定义和关卡运行 owner 混在一起。
- 五个 `Stage*FlowModel` 已继承 `LevelLifecycle`，证明通关终态可共享；但 `PG-012` 的黑框复发证明只抽局部生命周期不足以覆盖完整关卡模板。
- 原版 `PhysicsWorld.addSubObj()` 统一登记 `isTransferDoor`，`BaseHero.checkTransferDoor()` 统一检测所有关卡门；各关 SWF 嵌入不同 character 是资源打包事实，不等于现代行为 owner 必须按关复制。
- 原版仍有全局 `gc`、逐角色按键复制、`StageListenerXX` 和时间轴强耦合等债；现代架构只能保留可观察行为，不能把这些结构当模板。

## 3. 解决方案

1. 先审计五关全部共同职责和真正差异，冻结消费者矩阵、owner、迁移顺序与禁止边界。
2. 建立组合式 `PlayableLevelRuntime` / `LevelDefinition` 合同，公共运行框架统一队伍、镜头、玩家、HUD、移动/战斗调度、失败、出口、结果、保存、路由和销毁。
3. 关卡定义只声明世界边界、出生点、出口/解锁/路由和所需 adapter；`LevelEncounter` 只编排停点、波次、Boss 与机关。
4. 建立共享 `TransferDoorView` 行为/状态合同；透明资源、动画集合和原始 Symbol 差异由可替换 `TransferDoorVisualDefinition` 表达，关卡只引用 visual id 与位置。
5. 英雄/怪物的动画、AI、物理、伤害、奖励继续属于各自定义和系统；关卡只能引用类型/视觉 id，不能拥有其内部算法。
6. 使用组合、配置和窄 adapter，不建立同时承载世界、怪物、UI、资源、存档和路由的万能 `BaseLevel`。
7. 先迁移两关试点，再按 Stage 2 与 Stage 1-1/TestScene 分批迁移；保持兼容 facade，禁止一次性重写五关。
8. 首个后续新关卡必须直接消费公共框架；否则本问题只能进入效果观察，不能关闭。

## 4. 测试方案

- 静态消费者矩阵核对 Scene/World/Gameplay/Flow/视觉/保存 owner，禁止新增同义 Scene 生命周期、私有门显隐/完成提交和关卡内英雄/怪物算法。
- 纯系统测试覆盖 `LevelDefinition` 校验、初始化/重复销毁、失败/通关互斥、单/双人、默认/特殊出口、保存/路由幂等。
- 迁移批次逐关运行现有 flow、战斗、资源、存档、正式旅程与 bundle 测试；验证场景、波次、Boss、机关和数值不变。
- 940×590 复验代表性横向关卡与 Stage 1-1 的进入、战斗、Boss 死亡显门、W/上键、失败、结果、重试、返回和 console。
- 运行 `npm run check:structure`、`npm run test:systems`、`npm run build`、`npm run check:workflow` 与 `git diff --check`。

## 5. 测试结果

`TASK-ARCH-016A..D` 已完成合同和五关迁移。Stage 1-1 新增只读 Definition 与窄 `TestSceneStage11RuntimeAdapter`，共同镜头/队伍/功能入口/结果/保存路由/销毁进入 Runtime；纵向爬升、Boss、技能/宠物 sandbox 保留在遭遇 adapter。character 45→41/44 的 20 帧门由本关 bundle 持有，Stage 1-3 门借用与 `stage-1-common` 临时兼容已删除。五关专项、全系统、构建、门禁与 940×590 单/双人门结果、Stage 1-2 下一关和横向 Stage 2-2 均通过且零 console。存量治理已满足，待首个新关卡验证增量门禁后关闭。

## 6. 关闭标准

- 五个现有关卡全部消费唯一公共可玩关卡运行框架，关卡差异通过定义、遭遇和窄 adapter 表达。
- 公共队伍、玩家、镜头、HUD、移动/战斗调度、失败、出口、结果、保存、路由和销毁没有逐关同义 owner。
- 关卡不拥有英雄/怪物动画、AI、伤害、物理或奖励内部规则。
- 传送门共享行为/状态 owner；不同原始 Symbol/动画只作为可替换视觉定义，不按关复制逻辑。
- 五关确定性测试、全系统、构建与规定运行旅程无回归，静态门禁可阻止新增复制。
- 至少一个后续新关卡直接消费公共框架；仅迁移存量五关时进入效果观察，不提前关闭。

## 7. 适用触发与反馈记录

触发条件：

- 新增或修改 `Stage*Scene`、`Stage*WorldBridge`、`Stage*GameplayBridge`、`Stage*FlowSystem`、关卡入口/退出、玩家生成、门/出口、失败、结果、保存、路由或销毁。
- 在关卡文件中新增英雄/怪物动画、AI、物理、伤害、奖励或掉落算法。
- 新增任何正式关卡或特殊关卡运行路径。

效果检查：

- 搜索逐关重复 Scene 生命周期、门显隐、完成/保存/路由提交和实体内部规则。
- 核对新关卡是否声明 `LevelDefinition`、消费公共 Runtime，并只用有证据的窄 adapter。
- 回归五关消费者矩阵、架构专项与代表运行旅程。

| 日期 | 任务/变更 | 适用性 | 证据 | 结论 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-02 | `TASK-SLICE-158C` Role3正式关卡视觉增量 | 适用 | PlayableLevelRuntime统一创建Role1..3视觉；四个GameplayBridge改为共享syncHeroCombatVisual且总行数收缩，没有新增逐关动画owner、Scene生命周期、结果/保存/路由或销毁骨架 | 通过 / 增量未复发 | 后续角色视觉继续消费共享桥；首个新关卡直接消费Runtime的关闭样本仍待后续关卡 |
| 2026-07-31 | 用户反馈与问题登记 | 适用 | 四个正式 Scene/World/Gameplay 骨架重复；Stage 1-1 寄居 TestScene；门视图逐关创建；原版已有公共门登记/碰撞 | 已确认 / 待治理 | 执行 `TASK-ARCH-016A..D`，先合同审计再分批迁移 |
| 2026-07-31 | `TASK-SLICE-157C` Stage 1-3 视觉增量 | 适用 | 新增组合式 `Stage13MonsterVisualBridge` 只路由怪物视觉，没有把动画、AI、伤害、物理或奖励算法写入关卡；既有 Scene/World/Gameplay 骨架未扩大，但存量逐关运行框架仍存在 | 通过 / 存量未治理 | 157D 只做视觉回归；随后按 016A..D 治理完整运行框架，不在本 task 顺手重构 |
| 2026-07-31 | `TASK-ARCH-016B` 横向试点 | 适用 | Stage 1-2/1-3 共用 Runtime、Definition 校验与 TransferDoorView；Scene 私有结果/保存/路由/销毁 owner 已删除，专属内容仍留在窄 adapter；专项、全系统、构建、门禁和 940×590 零 console | 通过 / 治理进行中 | 激活 016C 迁移 Stage 2；PG-013 在 016D 五关完成前保持待治理 |
| 2026-07-31 | Stage 1-1 光门缺失纹理修复 | 适用 | 用户复验暴露 `TestScene` 借用 Stage 1-3 门 key 却未声明可达 bundle 的存量缺口；本次仅把既有普通门底图归入 `stage-1-common` 并补依赖闭包测试，没有新增逐关门逻辑或扩大 `TestScene` | 局部修复 / 存量未治理 | `TASK-ARCH-016A..D` 仍按原计划建立 `TransferDoorVisualDefinition` 与公共 Runtime；不得把本次资源可见修复当作 PG-013 已治理 |
| 2026-07-31 | 用户要求关卡类化前置 | 适用 | 黑绿缺失纹理再次证明局部生命周期/结果抽取不足；用户要求保留即时可见修复并将 016A..D 提至当前线最前，现唯一 Ready 改为 016A，158A 暂停 | 调度前置 / 待治理 | 按 016A→B→C→D 连续执行；016D 必须接入 1-1 character 45/41/44 并删除 Stage 1-3 门 key 与公共 bundle 临时兼容，然后恢复 158A |
| 2026-07-31 | `TASK-ARCH-016A` 合同与增量门禁 | 适用 | ADR 冻结五个合同、五关逐职责消费者/显示列表/逐状态矩阵和 016B→C→D 顺序；门禁自测阻止新增 Stage* World/Gameplay/Flow/Result 骨架并要求新 Scene 消费公共 Runtime；本 task 未修改运行时代码 | 通过 / 存量待治理 | 016B 迁移 1-2/1-3 并收缩遗留 allowlist；不得把合同完成当作 PG-013 已治理 |
| 2026-08-01 | `TASK-ARCH-016C` Stage 2 迁移 | 适用 | Stage 2-1/2-2 Scene 改为消费公共 Runtime 与各自 Definition；门统一为 TransferDoorView，结果/保存/路由/销毁私有 owner 删除；冰刺/火焰/Monster16/QA 未进入 Runtime；架构门禁已移除两 Scene 遗留例外，专项、全系统、build 与 940×590 正式/QA 零 console | 通过 / 治理进行中 | 激活 016D 迁移 Stage 1-1/TestScene；五关完成后 PG-013 才转效果观察 |
| 2026-08-01 | `TASK-ARCH-016D` Stage 1-1 与框架闭合 | 适用 | 1-1 Definition/Runtime adapter、自有 45/41/44 二十帧门、五关 Definition/Runtime 专项、`TestScene` 静态门禁与未来模板；全系统/build/structure/annotations/workflow/diff check 及 940×590 1P W、2P 上键、结果/下一关/横向代表均通过且零 console | 通过 / 方案落地 | 转效果观察；首个后续新关卡必须直接消费 Runtime 才关闭 PG-013 |
| 2026-08-02 | `TASK-SLICE-158D` Role4正式关卡视觉增量 | 适用 | PlayableLevelRuntime继续统一创建Role1..4视觉，TestScene只经共享HeroCombatVisual桥传递Role4动作；未新增逐关动画owner、Scene生命周期、结果/保存/路由或销毁骨架 | 通过 / 增量未复发 | 后续Role5继续消费共享桥；首个新关卡关闭样本仍待后续关卡 |
