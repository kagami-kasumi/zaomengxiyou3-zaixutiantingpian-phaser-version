# PG-009 启动资源加载边界缺失

状态：方案落地，效果观察中。

来源：2026-07-24 用户反馈冷刷新长时间不可见；`TASK-SLICE-151` 新增选角资源后，全局 Boot 预加载进一步放大首屏延迟。

## 1. 问题定义

当前 `BootScene` 同时承担启动壳层、全部正式关卡、怪物、HUD、功能 UI 和 QA 资源的加载 owner。任何新场景或新资源族只要继续追加到 Boot，都会增加所有玩家冷启动的请求、下载和图片/SVG 解码成本，即使本次流程根本不会访问这些内容。

这不是单张图片或一个常量导致的局部性能问题，而是资源声明、加载时机、场景生命周期和回归验证边界缺失：

- 启动、地图、功能 UI 和各关卡没有独立 bundle owner。
- 正式路由没有统一的“进入场景前确保资源就绪”合同。
- 新资源可以直接回填 Boot，没有自动门禁阻止首屏成本持续增长。
- 当前进度条只能反馈等待，不能消除不必要的全量加载。

不治理会导致每增加一个关卡、怪物或原生 UI 都继续恶化启动体验，并迫使后续任务重复实现临时延迟加载。

## 2. 证据

- `src/scenes/BootScene.ts` 在同一个 `preload()` 中遍历 `role1NormalAttackAssets`、`craftingAssets`、`fullFeatureUiAssets`、`skillNativeUiAssets`、`combatHudAssets`、`heavenMapAssets`、Stage 1-1/1-2/1-3、Stage 2-1、怪物 atlas/攻击帧和 Stage 2-2 QA 资源。
- `src/assets/AssetManifest.ts` 已按资源族导出定义，但没有把资源族映射为具有加载状态、依赖和路由 owner 的运行时 bundle。
- `TASK-SLICE-151` 新增 25 条选人数/选角色原生资源后，刷新阶段长时间只显示空画布；把该资源族移出 Boot 只能缓解新增回归，既有全局加载仍然存在。
- 2026-07-24 本机 preview 构建主 JavaScript 约 2.4 MB；冷刷新等待主要还包含大量与首屏无关的图像/SVG 请求和解码。
- 当前专项测试验证资源存在和场景合同，但没有断言“存档首屏前不得请求关卡/功能 UI bundle”，也没有新资源回填 Boot 的负向门禁。

## 3. 解决方案

由 `GOAL-033` / `TASK-ARCH-012` 建立场景级资源加载边界：

1. 定义稳定 bundle 目录，至少覆盖 `shell`、`heaven-map`、`feature-ui`、`stage-11`、`stage-12`、`stage-13`、`stage-21`、`stage-22`。
2. Boot 正式路径只加载可交互启动/存档壳层；DEV/QA 直达路径只额外确保目标 QA bundle。
3. 建立无业务规则的 bundle loader/coordinator，提供幂等 `ensure`、并发去重、已加载查询、失败反馈与安全重试。
4. 地图、功能页和正式关卡通过共享入口在 scene 启动前确保目标 bundle 就绪；返回再进不得重复注册或重复下载。
5. 为 bundle 覆盖和 Boot 边界增加自动门禁；新增场景资源必须归属 bundle，禁止直接扩大全局 Boot。
6. 记录同一验收环境的变更前后首屏时间、首屏请求资源族和进入代表场景的首次/再次加载行为。

明确不做：

- 不更换 Phaser、TypeScript、Vite 或渲染技术栈。
- 本治理不批量转码 WebP/KTX2、不重导 SWF、不重做 atlas；格式压缩若仍有必要，依据测量另拆任务。
- 首轮不实现激进纹理卸载或内存淘汰；先闭合加载时机与唯一 owner，避免把内存生命周期混入同一 Goal。

### 方案 V2：活动角色与入场/技能两级边界（2026-08-02）

用户正式旅程证明 V1 的“场景级 `combat-common`”对五角色大批量资源仍然过宽：五人合计约 273 MiB、三千多个文件，Role4 单人首次进场实测仍约 41.8 秒。该信号曾命中“复发/方案不充分”，本问题进入复盘并建立 V2：

1. `combat-common` 只保留五角色共享 HUD、设置、结果与拾取等小资源。
2. `combat-hero-1..5` 只持有入场立即需要的活动角色本体、装备、HUD 与普通攻击对象；关卡路由根据当前正式 `PartyConfiguration` 只 ensure 1–2 个活动角色包。
3. `combat-hero-1..5-skills` 分别持有技能、状态和 Shadow 等大序列；公共 `PlayableLevelRuntime` 在关卡已创建后按活动角色后台加载，避免阻塞玩家看到并控制关卡。
4. 本体与普攻 stable key 必须在场景启动前可用；技能子包继续保持唯一 owner、依赖和失败报告，禁止回填 Boot 或静默重复声明。

## 4. 测试方案

- 静态门禁：Boot 正式路径不得引用 Stage/Monster/完整功能 UI 资源族；所有 ready 运行资源必须归属一个可追踪 bundle。
- 纯逻辑测试：覆盖 bundle 首次加载、并发 `ensure` 去重、已加载幂等、单文件失败、重试和依赖顺序。
- 路由测试：覆盖启动存档、地图、功能 UI、Stage 1-1/1-2/1-3、Stage 2-1/2-2 和 DEV/QA 直达。
- 构建与回归：运行相关专项、`npm run test:systems`、`npm run build`、`npm run check:workflow` 和 `git diff --check`。
- 浏览器验收：940×590 冷刷新立即出现原始启动/存档壳层；首次进地图/功能页/代表关卡有明确加载反馈，返回再进不重复加载，console 无 warning/error。
- 性能记录：在同一 preview 环境记录三次冷刷新中位数；首个可交互壳层目标不超过 1.5 秒且相对治理前至少降低 70%。若验收机器无法满足，问题不得标记关闭，必须记录剩余最大资源族。

## 5. 测试结果

- `TASK-ARCH-012` 已建立 `shell/save-party/heaven-map/feature-ui/combat-common/stage-*` 唯一 owner、依赖与路由映射；Boot 只排队 shell。
- `AssetBundleCoordinator` / `SceneAssetBundleBridge` 已覆盖幂等 ensure、并发去重、已加载查询、失败/重试、同 scene 串行和 shutdown 拒绝。
- `asset-bundle-tests.ts` 的重复 owner、无 owner、Boot 非 shell 回填和路由负向样例全部通过；`npm run test:systems`、build、workflow 与 diff check 通过。
- 同一 940×590 preview 首屏从 250 个资源/248 张图片降为 5 个资源/3 张图片；三次冷刷新为 1243/788/646ms，中位数 788ms。
- 地图首次进入只新增 6 张图，返回再进总资源数保持 11；Stage 1-1 不加载功能 UI/Stage 2-2，Stage 2-2 DEV 直达不加载地图/功能 UI/Stage 1-1；console warning/error 为 0。
- V2 样本：Role4 单人 940×590 冷入口由约 41.8 秒降至约 6.5 秒，关卡画面、人物本体/HUD/普攻已可用；Role4 技能 897 帧不再阻塞入场并由角色技能子包后台加载。bundle 专项、角色专项、坐标专项和浏览器 console 均通过/为 0。
- V3 样本：用户仍感知 Stage 1-1 等待较长；复核发现单一 `stage-1-monsters` 还把未来两关怪物串入首关，其中 Stage 1-2 的 Monster4 hit2 两段约 38MiB。现按关卡拆为 `stage-1-monsters-11/-12/-13`，Stage 1-1/1-2 只取自身资源，Stage 1-3 按真实复用关系依赖三包；Role2/Role3 冷入口在 940×590 预览约 6 秒后已可见，console 为 0。
- 打包边界说明：Electron/桌面壳可以把 HTTP 传输和远端延迟变为本地读取，但不会消除数千序列帧的文件调度、PNG/SVG 解码、GPU 纹理上传、显存占用或错误的跨关串载。桌面打包只能作为部署优化；bundle/atlas、按需加载、预解码和实体生命周期复用仍须在 Web 版本先闭合。

## 6. 关闭标准

- Boot 正式路径只加载 `shell` 及其最小依赖；关卡、怪物、地图和完整功能 UI 不在首屏前请求或解码。
- 所有正式场景资源都有唯一 bundle owner；首次进入、并发进入和返回再进入合同由自动测试覆盖。
- DEV/QA 直达仍可用，且不会迫使正式启动加载全部 QA 资源。
- 新增资源回填全局 Boot 的负向样例会被自动校验拒绝。
- 冷刷新性能达到本问题测试方案中的相对与绝对目标，并留下请求资源族、三次测量和 940×590 运行证据。
- 相关专项、全系统、build、workflow、diff check 与 console 检查全部通过。

仅增加进度条、只延迟一个资源族或减少个别图片不能算关闭。

`TASK-ARCH-012` 已满足上述存量关闭标准；本问题转为效果观察。后续新增关卡、怪物、HUD、菜单或资源族仍须由自动门禁和本节触发条件持续采样，若 Boot 回填、重复请求、缺纹理或失败卡死复发则退回治理。

## 7. 适用触发与反馈记录

触发条件：

- 修改 `BootScene.ts`、`AssetManifest.ts`、bundle loader、scene 启动/切换入口或 Vite 资源配置。
- 新增关卡、怪物、HUD、菜单、功能 UI、atlas 或大批量序列帧资源。
- 冷刷新、首次进场、返回再进出现明显等待、重复请求、缺纹理或 loader 卡死。
- 任务声称优化首屏、资源分包、按需加载、内存或加载失败恢复。

效果检查：

- 搜索 Boot 是否重新直接遍历非 `shell` 资源族。
- 核对新增 ready 资源是否具有唯一 bundle owner 和路由消费者。
- 运行 bundle 逻辑/路由测试并对比首屏资源族、三次性能测量和浏览器 console。

| 日期 | 任务/变更 | 适用性 | 证据 | 结论 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-03 | `TASK-SLICE-165A` 战斗入口运行复验 | 适用 | 20 个 574 状态与 371/444 设置资源继续仅由既有 `combat-common` 提供；本次只修 pointer/启动顺序，未回填 Boot、未新增 bundle owner；Role1 QA 五页冷打开与 console 零 warning/error 通过 | 通过 | 165B 复核背包时继续保持页面资源按入口加载，不重复声明 431 图标 |
| 2026-08-03 | `TASK-SLICE-159` 最终正式旅程 | 适用 | 940×590唐僧/白龙槽从重载进入地图、三个懒加载服务页和Stage 1-1；活动角色资源、Stage 1-1怪物按关owner及P2背包依赖均完成，未串载未来Stage 1怪物，console warning/error为0；专项覆盖下一关共享普攻视觉生命周期 | 通过 | V2/V3分包效果样本成立，PG-009继续观察；后续Stage 2-3资源仍须按实际消费者建立唯一owner |
| 2026-08-02 | `TASK-SLICE-159` 正式旅程角色加载/坐标回归 | 适用 | 用户报告首次关卡超过一分钟；复测旧Role4约41.8s。V2拆为共享小包、活动角色入场包和活动角色技能后台包，Role4冷入口约6.5s；Boot仍仅shell，唯一owner/依赖、人物脚底、普攻挂点、专项与零console通过 | 复发后修复 | 方案V2落地后恢复效果观察；159最终正式旅程继续覆盖双人组合、重入和正常离场取消，若技能首次按键早于后台就绪再拆到loadout/技能级门控 |
| 2026-08-02 | `TASK-SLICE-159` Stage 1 怪物串载与跨关攻击视觉回归 | 适用 | 用户继续报告等待长及 1-1“下一关”后 Role2/3 攻击对象消失；Stage 1 单一怪物包串载未来关卡，四个后续 GameplayBridge 也从未创建普通攻击对象。V3 按关拆怪物 owner，并建立五关共享普通攻击 view lifecycle；Role2 左右远程对象与 Role3 实际按键可见，零 console | 复发后修复 | 方案 V3 保持效果观察；159 完整正式存档旅程仍需实际打通/下一关覆盖，若约 6 秒仍不可接受，下一步应合并大序列请求或转码大图，不得回填 Boot |
| 2026-08-02 | `TASK-SLICE-158C` Role3战斗资源接入 | 适用 | 2 atlas、397对象帧与HUD头像仅由combat-common持有；Boot仍只加载shell，SpecialUI字节副本未重复声明；专项、全系统、构建、单/双人冷进入与零console通过 | 通过 | 069D只派生证据不写manifest；158D继续验证唯一bundle owner与冷进入 |
| 2026-07-25 | 共享 UI 组件化任务登记 | 适用 | PG-011 与 `014A..F` 明确共享组件不得隐式跨 bundle、不得 Boot 回填；共享资产须登记 provenance、透明 alpha、稳定 key 和消费者 bundle，商城黑底截图裁片作为负向样本 | 通过 | 014A 冻结 bundle/manifest owner；B..F 每次迁移验证冷进入、返回再进和无串载 |
| 2026-07-25 | `TASK-SLICE-160` 全量背包图标接入任务定义 | 适用 | 新实现 task 明确要求真图标只有唯一 bundle owner、Boot 不回填全量背包资源、首次进入按需加载、返回再进幂等，并把 bundle 自动门禁和 940×590 零缺纹理/零 console 作为验收；本次仅重排任务，尚无运行时代码 | 通过 | 实现 160 时必须回写实际资源数、路由消费者和首次/再进样本；若单一背包 bundle 明显延迟则继续按分类/页拆分 |
| 2026-07-24 | 冷刷新黑屏反馈与选角资源延迟加载小修 | 适用 | 25 条选角资源移出 Boot 后新建流程恢复，但 Boot 仍集中加载地图、功能 UI、Stage 1/2 与怪物资源；进度条只能展示等待 | 方案不充分 | 登记 `GOAL-033` / `TASK-ARCH-012`，在存档队伍消费者收敛后、Stage 2-3 前治理 |
| 2026-07-24 | `GOAL-033` / `TASK-ARCH-012` 场景资源分包 | 适用 | Boot 仅 shell；稳定 bundle owner、依赖、正式/DEV/QA 路由、并发/失败/重试/shutdown 和负向门禁闭合；首屏 250→5 资源，三次中位数 788ms，地图再进 11 不变，代表场景无串载且零 console | 通过 | 存量方案落地，转效果观察；新增资源继续由 Boot/owner/路由门禁和浏览器样本防复发 |
| 2026-07-24 | `TASK-SLICE-154` 功能子页/角色级分包 | 适用 | 用户反馈技能首次进入仍需 3–5 秒；复核发现单一 `feature-ui` 继续加载所有功能页和五角色 150 图标帧。现拆为页面 bundle、技能公共 bundle 和五个各 30 帧角色 bundle；运行约 1.0–1.2 秒，专项禁止串载 | 复发后修复 | 场景级 bundle 不是功能页终点；后续大页面必须继续按实际入口/角色/标签评估子 bundle 边界 |
| 2026-07-25 | `TASK-SLICE-160` 背包资源接入 | 适用 | Boot 仍仅 3 个 shell 资源；428 图标、22 原生控件及背景/格子共 452 项只有 `feature-ui-backpack` owner，首次打开约 1.8 秒，返回再进幂等，bundle 专项、正式运行和零 console 通过 | 通过 | 背包资源没有回填 Boot 或串载其他功能页；后续若实际等待回归再按分类/页拆分 |
| 2026-07-25 | `TASK-SLICE-155A` 丹药资源接入 | 适用 | Boot 未回填；25 个丹药图标由 `inventory-items-immortality` 唯一持有并供背包/丹药依赖，丹药其余资源由 `map-service-immortality` 持有；bundle 专项通过，最终冷进入约 1.35 秒且零 console | 通过 | 共享小资源族避免重复 owner 和整包背包串载；后续地图服务页继续按入口分包 |
| 2026-07-25 | `TASK-SLICE-155B` 商城资源接入 | 适用 | Boot 未回填；49 个商城图标由 `inventory-items-shop` 唯一持有，`map-service-shop` 持有商城根/卡片/确认框与按钮，背包通过依赖复用而不重复声明；bundle 专项与正式冷进入零 console 通过 | 通过 | 商城资源 owner/路由边界稳定；后续设置、任务继续按入口分包 |
| 2026-07-25 | `TASK-SLICE-155C` 设置资源接入 | 适用 | 148 静态根与 144 三态共 4 项资源由既有 `heaven-map` bundle 唯一持有，Boot 未回填、无独立 scene 串载；bundle 专项、正式地图打开/重开/重载和零 console 通过 | 通过 | 小型地图 overlay 随地图 bundle 保持即时打开；后续任务页作为独立页面按入口分包 |
| 2026-07-26 | `TASK-SLICE-155D` 任务资源接入 | 适用 | `map-service-tasks` 唯一持有 character 85 与任务控件/共享奖励图，路由按入口懒加载并显式依赖 heaven-map/背包图标 owner；Boot 未回填，bundle/全系统与正式冷进入零 console 通过 | 通过 | 任务页没有重复声明 431 图标或串载到 Boot；共享资源仍由既有唯一 owner 提供，PG-009 保持效果观察 |
| 2026-07-26 | `TASK-SLICE-156A` HUD 入口资源接入 | 适用 | 20 个 574 按钮状态由 `combat-common` 唯一持有并随正式关卡加载，Boot 未回填；既有背包仍由页面 bundle 按点击懒加载，约 1.8 秒后打开且零 console | 通过 | HUD 小资源不重复分关卡声明，页面大资源继续保持按入口加载；PG-009 保持效果观察 |
| 2026-07-26 | `TASK-SLICE-156B` 关卡设置资源接入 | 适用 | 371/444、366 三帧及 44 个设置/帮助按钮状态全部由 `combat-common` 唯一持有，Boot/地图 bundle 未回填；manifest provenance 与 bundle 负向门禁通过 | 通过 | 设置随正式关卡即时可用，地图 148 仍由 heaven-map owner；PG-009 保持效果观察 |
| 2026-07-26 | `TASK-SLICE-156C` Stage 2 共享怪物资源回归 | 适用 | 正式 2-2 暴露复用 2-1 攻击几何却未加载；修复过程拒绝重复 owner，最终由唯一 `stage-2-monsters` bundle 持有 Monster9/10/19 图集、攻击帧与几何，2-1/2-2 共同依赖，Boot 未回填 | 复发后修复 | bundle/Stage 2 专项、build 与正式 2-2 进入通过；后续跨关卡复用怪物资源必须先建立共享 owner，禁止复制声明 |
| 2026-07-26 | `TASK-SETTINGS-068` Stage 1 怪物资源任务生成 | 适用 | 26 条资源仅到 derived-ready，未提前写入 Boot/manifest；157A..C 按关卡接入但要求共享只读动画描述，157D 专门审计五关唯一 owner、Stage 2 防回归、重入和零缺纹理 | 通过 | GOAL-060 实现时回写实际 bundle owner、冷进入/再进和 console；禁止按三关复制 atlas/攻击对象声明 |
| 2026-08-02 | `TASK-SETTINGS-069C` Role3资源owner输入 | 适用 | BaJie与SpecialUI/BaJie哈希完全相同；本任务只在Git忽略输出派生一次，未写Boot/manifest，并冻结158C由`combat-common`或等价唯一Role3 owner消费，禁止双包重复接入 | 通过 | 158C须验证唯一bundle owner、正式路径加载、重入和零缺纹理/零console |
| 2026-07-26 | `TASK-SLICE-157A` Stage 1-1 怪物资源接入 | 适用 | Monster30/3 两 atlas、三攻击对象 25 帧与几何 CSV 由唯一 `stage-11` bundle 持有；Boot、地图和 Stage 2 bundle 未回填，旧缺失原图占位族移除；bundle 专项与正式进入零缺纹理/零 console 通过 | 通过 | Stage 1-1 owner 边界稳定；157B/157C 按关卡接入，157D 再审计跨关共享与重入 |
| 2026-07-31 | `TASK-SLICE-157B` Stage 1-2 怪物资源接入 | 适用 | Monster2/4/7/8 四 atlas、九对象 122 帧与独立几何 key 由唯一 `stage-12` bundle 持有；Boot、地图、Stage 1-1/1-3 与 Stage 2 bundle 未回填，bundle/全系统与正式单/双人进入零缺纹理/零 console | 通过 | Stage 1-2 owner 边界稳定；157C 接入共享怪物时不得复制现有 owner，157D 统一审计与重入 |
| 2026-07-31 | `TASK-SLICE-157C` Stage 1-3 怪物资源接入 | 适用 | Monster5 atlas、四对象 24 帧与几何 cache key 由唯一 `stage-13` bundle 持有；Monster30/3/7/8 通过 `stage-11`/`stage-12` 现有 owner 依赖复用，没有重复 stable key 或 Boot 回填；bundle 专项与正式路径零 console | 通过 | 过渡依赖避免复制 owner；157D 统一审计五关 owner、重入和 Stage 2 防回归后再收束父任务 |
| 2026-07-31 | `TASK-SLICE-157D` 五关怪物视觉回归 | 适用 | Stage 1 七 atlas、16 对象与几何统一迁入 `stage-1-monsters`，三关只依赖共享 owner；Stage 2 保持 `stage-2-monsters`/`stage-22` 边界；逐 key owner、五关首次/重入、bundle/全系统/build 与 940×590 零 console 通过，Boot 未改 | 通过 | PG-009 保持效果观察；后续角色大批量动画仍须建立窄 bundle、禁止 Boot 回填，并继续验首次/重入 |
| 2026-07-31 | Stage 1-1 光门缺失纹理修复 | 适用 | `TestSceneViews` 引用 `Stage13AssetKeys.transferDoor`，但该 key 原由 `stage-13` 私有持有，`stage-11` 路由未加载，运行时回退为黑底绿斜线缺失纹理；现把普通 Stage 1 门底图移至唯一 `stage-1-common` owner，并增加 `stage-11` 依赖解析回归 | 复发后修复 | bundle 专项、build 与 diff check 通过；PG-009 保持效果观察，后续跨关共享视觉必须验证每个消费者的依赖闭包，不只验证唯一 owner |
| 2026-08-01 | `TASK-SLICE-158A` Role1 大批量序列接入 | 适用 | 3 atlas、四普攻与 249 技能帧统一由 `combat-common` 持有，Boot/地图/功能页未回填；五关通过既有依赖消费，QA 直达也经 `startSceneWithBundle` | 通过 | 唯一 owner 与路由闭包门禁通过；Role2 接入继续复用该窄 bundle 边界 |
| 2026-08-01 | `TASK-SLICE-158B / 162` Role2 大批量序列接入 | 适用 | 3 atlas、两普攻、464 技能帧及 Role2 HUD 头像统一由 `combat-common` 持有，Boot 未回填；首次 940×590 发现 PNG 头像误走 SVG loader，改为 image 并增加专项 kind 断言后单/双人完整加载且零 console | 复发后修复 | 唯一 owner 和路由闭包仍成立；后续混合 PNG/SVG bundle 必须逐项验证 loader kind，不只验证 stable key 存在 |
| 2026-08-02 | `TASK-SLICE-158D` Role4大批量序列接入 | 适用 | 4张当前装束图表、897个对象/状态帧及Role4 HUD头像统一由`combat-common`持有，Boot未回填；单/双人正式QA均完整加载且零console，专项核对PNG/SVG loader与唯一stable key | 通过 | 冷进入约40秒的897帧加载成本作为观察信号保留；owner/依赖闭包未复发，Role5不得复制bundle声明 |
| 2026-08-02 | `TASK-SLICE-158E` Role5大批量资源接入 | 适用 | 25张枪形表、归一化剑形动作、普攻/技能/状态对象及HUD共860文件、约58.5MiB统一由`combat-common`持有，Boot未回填；专项验证stable key、kind和唯一owner，940×590首次进入最终完成且无缺纹理/console错误 | 通过但保留性能观察 | 冷进入明显慢于Role4，资源边界未复发但请求/解码成本已成为PG-009观察信号；后续若用户路径回归须按活动角色拆窄bundle或合并序列请求，不得回填Boot |
